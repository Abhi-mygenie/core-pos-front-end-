// Application Configuration Constants

export const CONFIG = {
  // Card dimensions
  CARD_MIN_HEIGHT: "155px",
  
  // Default channel selections
  DEFAULT_CHANNELS: ["delivery", "takeAway", "dineIn"],
  
  // Default status filters
  DEFAULT_STATUSES: ["confirm", "cooking", "ready", "running", "schedule"],
  
  // Search configuration
  SEARCH_RESULT_LIMIT: 5,
  
  // Touch target sizes (accessibility)
  TOUCH_TARGET_MIN: "44px",
  
  // Button padding
  ICON_BUTTON_PADDING: "p-3",
  TEXT_BUTTON_PADDING_Y: "py-3",
  TEXT_BUTTON_PADDING_X: "px-6",
};
