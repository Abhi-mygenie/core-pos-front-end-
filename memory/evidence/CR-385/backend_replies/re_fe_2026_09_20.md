# CR-385 Gate-4 — backend reply to FE (standalone)

**Date:** 2026-09-20  
**Restaurant:** `69` · hotel `sandbox-pms` · preprod `https://preprod.mygenie.online`  
**Against FE report:** [`probes_2026_09_20_gate4/PROBE_REPORT.md`](probes_2026_09_20_gate4/PROBE_REPORT.md) (build 2 morning)  
**Our re-verify:** [`probes_2026_09_20_gate4/probe_build3_kisuke/`](probes_2026_09_20_gate4/probe_build3_kisuke/) · summary [`PROBE_BUILD3.md`](probes_2026_09_20_gate4/PROBE_BUILD3.md)

**Tested stay:** reservation **160** · booking `MG-69-66918719-E67B-4839-A082-119DE318CE95` · order **1232587** · phone `9876500327` · check-in table **8525** → move **8527** · dates `2026-10-10` → `2026-10-11` (extend/checkout `2026-10-12`) · employee **kisuke** (`vendor_employees.id=5118`).

---

## 1. Verdict for FE

Gate-4 money / extend paths from `PROBE_REPORT.md` are **unblocked** on preprod (build 3).

| Area | Status |
|------|--------|
| BQ-16, D1–D7, D9, D10, D12, D13 | **Fixed + re-tested** |
| D8 ledger + `charge.balance_due` | **Fixed + re-tested** |
| D8 folio `remaining_room_balance` | **Still wrong after TAB** — use `charge.balance_due` / `payment_status=paid` |
| D11 | **Shipped** (default `booking_for` + meta inside txn); fail-path not re-hit in build 3 |
| N7 early check-in | **Allowed** (product open) |

---

## 2. Defect matrix vs `PROBE_REPORT.md`

### Already green on build 2 (still green)

| ID | FE report | Build 3 |
|----|-----------|---------|
| BQ-16 | Omit rate priced; unknown plan 422 | Same — omit → `rate_per_night 8000` / `booking_charge 16000`; unknown plan → **422** `no rate configured` |
| D1 | Upgrade no type 422 | OK |
| D2 | Advance carried | Ledger “booking carry” **1000** upi `GATE4ADV` |
| D3 | Pre-GST + `gst_tax` | Folio `room_price 10100` + `gst_tax 1818` at check-in |
| D5 | `data.order_id` + `charge` | `order_id 1232587` + full `charge{}` |
| D6 | TAB without `order_discount` | **200** `"Room payment received via TAB"` |

### Open / blocked on build 2 → closed on build 3

| ID | Build 2 (PROBE_REPORT) | Build 3 (tested) |
|----|------------------------|------------------|
| **D9** | `rate 7100`, `booking_charge 8600` (upgrade free) | **`rate_per_night 8600`**, **`upgrade_amount 1500`**, **`booking_charge 10100`** |
| **D10** | GST 5% (`sgst 215`) | GST **18%** (`sgst 909` + `cgst 909` = 1818) |
| **D11** | 500 + SQL; dirty upgrade on fail | Default `booking_for=Individual`; meta+persist inside txn (omit-`booking_for` fail path not re-run) |
| **D12** | Same-room extend **409** | Extend **200**; `nights 2`, `booking_charge 18700` |
| **D13** | Move **500** `$currentTableId` | Move **200** → table **8527** |
| **D4** | Not verifiable (blocked by D12) | Nightly rate held **8600** across extend |
| **D7** | Not verifiable (blocked by D13) | After move: **8527** `occupied` / Gate4 Guest; **8525** `hk` |
| **D8** | Ledger 7600 ≠ 8030; `balance_due` stuck | Ledger checkout **20566** (= amount sent); LR **`charge.balance_due 0`**. Folio `remaining_room_balance` still **20566** |
| **N7** | Early check-in question | Still allowed — no business-date gate |

---

## 3. Money contract (FE must follow)

| Field | Rule |
|-------|------|
| **`charge.*`** | Authoritative for rate, GST, advance, **`balance_due`** |
| **`balance_payment`** (stay) | Rent-only **historical**; may stay non-zero after checkout — **do not** treat as “still owed” |
| **Cleared?** | `payment_status === 'paid'` **and** `charge.balance_due === 0` |
| Folio `remaining_room_balance` | May lag after TAB (known residual) — prefer `charge.balance_due` |
| Check-in | Send FE fields incl. **`booking_for=Individual`** (server defaults if empty) |
| TAB | `payment_amount` = `charge.balance_due`; omit `order_discount` OK |

---

## 4. Tested numbers (order 1232587)

### After check-in (paid upgrade 1500)

```
rate_per_night      8600
upgrade_amount      1500
booking_charge     10100
sgst / cgst     909 / 909
total_with_gst     11918
advance_payment     1000
balance_due        10918
```

### After same-room extend (+1 night, pay 500 cash)

```
nights                 2
rate_per_night      8600
booking_charge     18700   (= 8600×2 + 1500)
total_with_gst     22066
advance_payment     1500
balance_due        20566
```

### After move to 8527

HTTP 200 · board: 8527 occupied · 8525 hk

### After TAB `payment_amount: 20566` (no `order_discount`)

```
HTTP 200
charge.balance_due              0
charge.advance_payment      22066
ledger checkout TAB         20566
payment_status               paid
operational_status       departed
folio remaining_room_balance 20566   ← residual; ignore for “owed”
```

---

## 5. Curl pack (RID 69) — re-run anytime

Set token (kisuke `5118` or any RID-69 vendor employee):

```bash
HOST="https://preprod.mygenie.online"
TOKEN="<Bearer token>"
# Tables: suite check-in 8525, move dest 8527 (avoid 8524 if occupied)
CIN=2026-10-10; COUT=2026-10-11; COUT2=2026-10-12
PHONE=98765004$(date +%S)   # unique
```

### 5.0 fetch-rates

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/aiosell/fetch-rates" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{\"start_date\":\"$CIN\",\"end_date\":\"2026-10-14\"}"
```

### 5.1 BQ-16 — omit rate

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/aiosell/direct-reservation" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{\"guest\":{\"name\":\"BQ16 Guest\",\"phone\":\"9876500100\"},\"checkin\":\"$CIN\",\"checkout\":\"$COUT2\",\"adults\":2,\"children\":0,\"rooms\":[{\"room_code\":\"executive\",\"rateplan_code\":\"executive-s-ep\",\"rooms_count\":1}]}"
```

**Expect:** `charge.rate_per_night` > 0. Cancel if unused.

### 5.1b BQ-16 — no rate → 422

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/aiosell/direct-reservation" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{\"guest\":{\"name\":\"No Rate Guest\",\"phone\":\"9876500999\"},\"checkin\":\"$CIN\",\"checkout\":\"$COUT2\",\"rooms\":[{\"room_code\":\"executive\",\"rateplan_code\":\"unknown-plan-zzz\",\"rooms_count\":1}]}"
```

**Expect:** HTTP **422** · `"no rate configured"`.

### 5.2 Direct + advance (D2 setup)

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/aiosell/direct-reservation" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{\"guest\":{\"name\":\"Gate4 Guest\",\"phone\":\"$PHONE\"},\"checkin\":\"$CIN\",\"checkout\":\"$COUT\",\"adults\":2,\"children\":0,\"rooms\":[{\"room_code\":\"executive\",\"rateplan_code\":\"executive-s-ep\",\"rooms_count\":1}],\"advance\":{\"amount\":1000,\"method\":\"upi\",\"reference\":\"GATE4ADV\"}}"
```

Save `RESERVATION_ID` / `BOOKING_ID`. **Expect:** `advance_payment` ≈ 1000.

### 5.3 Check-in paid upgrade (D1/D3/D5/D9/D10) → 8525

```bash
curl -sS -X POST "$HOST/api/v1/vendoremployee/pos/user-group-check-in" \
  -H "Authorization: Bearer $TOKEN" -H "Accept: application/json" \
  -F "phone=$PHONE" -F "name=Gate4 Guest" -F "email=" \
  -F "booking_type=Direct" -F "booking_for=Individual" -F "booking_details=" \
  -F "booking_id=$BOOKING_ID" -F "aiosell_reservation_id=$RESERVATION_ID" \
  -F "id_type=Aadhaar" -F "room_id[]=8525" \
  -F "checkin_date=$CIN" -F "checkout_date=$COUT" \
  -F "order_amount=0" -F "room_price=0" -F "advance_payment=0" -F "balance_payment=0" -F "gst_tax=0" \
  -F "total_adult=2" -F "total_children=0" -F "payment_method=Cash" \
  -F "name2=" -F "id_type2=" -F "name3=" -F "id_type3=" -F "name4=" -F "id_type4=" \
  -F "firm_name=" -F "firm_gst=" \
  -F "upgrade_type=paid" -F "upgrade_amount=1500" -F "upgrade_reason=Suite upgrade probe"
```

**Expect (when Oct rates ~8600):**  
`rate_per_night 8600` · `upgrade_amount 1500` · `booking_charge 10100` · GST 18% · `data.order_id` set → `ORDER_ID`.

### 5.4 Same-room extend (D4 / D12)

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/pos/room-extend-stay" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{\"order_id\":$ORDER_ID,\"new_checkout_date\":\"$COUT2\",\"reason\":\"Gate4 extra night\",\"payment\":{\"amount\":500,\"method\":\"cash\"}}"
```

**Expect:** HTTP **200** (not 409) · `nights` 2 · rate still room baseline.

### 5.5 Move (D7 / D13) → 8527

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/pos/room-extend-stay" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{\"order_id\":$ORDER_ID,\"new_checkout_date\":\"$COUT2\",\"new_restaurant_table_id\":8527,\"reason\":\"Gate4 move\"}"

curl -sS "$HOST/api/v2/vendoremployee/aiosell/room-status-board" \
  -H "Authorization: Bearer $TOKEN" -H "Accept: application/json"
```

**Expect:** HTTP **200** · dest occupied · origin **hk**.

Read due:

```bash
# from local-reservations charge.balance_due → BALANCE_DUE
```

### 5.6 TAB (D6 / D8) — no `order_discount`

```bash
curl -sS -X POST "$HOST/api/v2/vendoremployee/order/order-bill-payment" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -H "Accept: application/json" \
  -d "{
    \"order_id\": \"$ORDER_ID\",
    \"payment_mode\": \"TAB\",
    \"payment_amount\": $BALANCE_DUE,
    \"payment_status\": \"success\",
    \"transaction_id\": \"\",
    \"billing_auto_bill_print\": \"No\",
    \"food_detail\": [],
    \"waiter_id\": 5118,
    \"restaurant_name\": \"The Goan Kitchen\",
    \"email\": \"\",
    \"order_sub_total_amount\": 0, \"order_sub_total_without_tax\": 0,
    \"total_gst_tax_amount\": 0, \"gst_tax\": 0, \"vat_tax\": 0,
    \"grant_amount\": $BALANCE_DUE, \"order_amount\": $BALANCE_DUE,
    \"round_up\": 0, \"service_tax\": 0, \"service_gst_tax_amount\": 0,
    \"tip_amount\": 0, \"tip_tax_amount\": 0, \"delivery_charge\": 0, \"self_discount\": 0,
    \"discount_for\": null, \"coupon_code\": \"\", \"coupon_discount\": 0,
    \"coupon_title\": \"\", \"coupon_type\": \"\", \"comm_discount\": 0, \"discount_type\": \"\",
    \"order_discount_type\": \"Percent\", \"discount_value\": 0,
    \"discount_member_category_id\": 0, \"discount_member_category_name\": \"\",
    \"used_loyalty_point\": 0, \"loyalty_points_used\": 0, \"loyalty_discount\": 0,
    \"loyalty_redemption_id\": null, \"use_wallet_balance\": 0,
    \"paid_room\": \"yes\", \"usage_id\": \"\",
    \"cust_name\": \"Gate4 Guest\", \"cust_mobile\": \"$PHONE\", \"cust_membership_id\": \"\",
    \"name\": \"Gate4 Guest\", \"mobile\": \"$PHONE\",
    \"crm_customer_id\": \"\", \"custGST\": \"\", \"custGSTName\": \"\"
  }"
```

**Expect:** HTTP **200** · no SQL · LR `charge.balance_due` **0** · ledger checkout row **=** `$BALANCE_DUE` · `payment_status=paid`.  
**Known:** folio `remaining_room_balance` may still show old due — ignore; trust `charge.balance_due`.

---

## 6. Ask FE

1. Re-open money UI on `charge.balance_due` + `payment_status` (not `balance_payment` / folio remaining alone).
2. Keep sending `booking_for=Individual` on check-in.
3. Confirm N7 early check-in UX (backend allows check-in before stay date).
4. Optional: omit `booking_for` once to confirm no 500/SQL (D11 hygiene).
