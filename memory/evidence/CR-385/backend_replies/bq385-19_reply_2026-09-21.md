# CR-385 BE → FE · BQ-385-19 / G4-07 · 2026-09-21 · RID 69

**Host:** `https://preprod.mygenie.online`  
**Related:** Walk-through #2 · [`n7-n8.md`](n7-n8.md) · D15/D16 ledger

---

## Verdict

**BQ-385-19 shipped (blame-safe).** When a calendar sold-night ledger exists, `GET …/local-reservations` rows include `charge.nights_detail` after reload — same shape as extend.

| Field | Rule |
|-------|------|
| Money (`booking_charge`, `sgst`, `cgst`, `total_with_gst`, `balance_due`) | **Unchanged** — list read does **not** reprice |
| `nights_detail` | Present only if stored ledger dates match stay **and** `sum(rates)+upgrade ≈ booking_charge` (±0.02) |
| No ledger / held / Direct-only / gate fail | Field **absent** → FE shows “N nights · avg” (Walk-through **yes** path still valid) |

Upgrade GST remains in `sgst`/`cgst` only — do **not** sum `nights_detail[].gst` for total GST (N11).

---

## What changed

- Persist path already: `raw_payload.pms_night_rates` (D15/D16).
- List path: after normal `charge` build, **overlay** display-only `nights_detail` from that ledger.
- Never `persist()` from list; never pass `night_rates` into list `build()` (that would force-recompute money).

---

## FE ask

1. Bill / Departures / In-House: if `charge.nights_detail` present → show per-night lines; else keep avg.
2. Totals always from `charge.*` money fields, not from summing night GST.
3. Walk-through **no** (wait for list detail) is unblocked once this is on server + probed.

---

## Probe expect (after deploy)

1. Calendar extend → LR same booking: money **equals** extend; `nights_detail` present and rates match.
2. Held extend → LR: **no** `nights_detail`.
3. Direct never extended → LR: **no** `nights_detail`.

### Live accept (2026-09-21 · RID 69)

| Mode | Result |
|------|--------|
| **calendar** order 1232633 / rid 205 | Extend = LR money `17500` / `1094` / `19688` · `nights_detail` both `8600@18 held` + `7400@5 calendar` |
| **held** order 1232634 / rid 206 | Extend = LR money `18700` / `1683` · **no** `nights_detail` on either |