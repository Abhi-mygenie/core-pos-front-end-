import re, json
BASE = "https://core-frontend-live-1.preview.emergentagent.com"
await page.set_viewport_size({"width": 1920, "height": 800})
caps = {}
def on_req(req):
    u = req.url
    if req.method == "POST" and ("direct-reservation" in u or "user-group-check-in" in u or "room-extend-stay" in u):
        key = "booking" if "direct-reservation" in u else "checkin" if "user-group" in u else "extend"
        caps.setdefault(key, []).append({"url": u, "ct": req.headers.get("content-type", ""), "body": (req.post_data or "")[:3000]})
page.on("request", on_req)
errors = []
page.on("console", lambda m: errors.append(m.text[:200]) if m.type == "error" else None)
out = {}
async def toast():
    await page.wait_for_timeout(1500)
    return await page.evaluate("() => [...document.querySelectorAll('[data-sonner-toast]')].map(t => t.innerText.slice(0,120))")
try:
    await page.goto(BASE + "/", wait_until="domcontentloaded")
    await page.wait_for_selector('[data-testid="login-email"]', timeout=30000)
    await page.fill('[data-testid="login-email"]', "owner@thegoankitchen.com")
    await page.fill('[data-testid="login-password"]', "Qplazm@10")
    await page.click('[data-testid="login-button"]', force=True)
    await page.wait_for_url(re.compile(r".*/(dashboard|pms).*"), timeout=40000)
    await page.goto(BASE + "/pms/front-desk-v2?tab=arrivals", wait_until="domcontentloaded")
    await page.wait_for_selector('[data-testid="fd-new-booking-btn"]:not([disabled])', timeout=60000)
    await page.wait_for_timeout(2000)
    bd = await page.evaluate("() => document.querySelector('[data-testid=fd-header-date]')?.innerText")
    print("PASS login; header date:", bd)
    # --- T1 booking
    await page.click('[data-testid="fd-new-booking-btn"]', force=True)
    await page.wait_for_selector('[data-testid="booking-form"]', timeout=10000)
    await page.fill('[data-testid="booking-guest-name"]', "QA P3 Suite")
    await page.fill('[data-testid="booking-guest-phone"]', "9876500401")
    ci = await page.input_value('[data-testid="booking-checkin"]'); co = await page.input_value('[data-testid="booking-checkout"]')
    print("dates", ci, co)
    await page.wait_for_selector('[data-testid^="booking-cell-suite-"]', timeout=20000)
    exec_free = await page.evaluate("() => document.querySelector('[data-testid=booking-type-executive-free]')?.innerText")
    print("executive:", exec_free)
    await page.click('[data-testid^="booking-cell-suite-"]', force=True)
    await page.click('[data-testid="booking-advance-toggle"]', force=True)
    await page.fill('[data-testid="booking-advance-amount"]', "1000")
    await page.click('[data-testid="booking-pay-upi"]', force=True)
    await page.fill('[data-testid="booking-pay-ref"]', "QAUTR3")
    await page.click('[data-testid="booking-save-btn"]', force=True)
    await page.wait_for_selector('[data-testid="booking-confirmation"]', timeout=40000)
    out["t1"] = await page.evaluate("() => ({total: document.querySelector('[data-testid=booking-bill-total]').innerText, adv: document.querySelector('[data-testid=booking-bill-advance]').innerText, bal: document.querySelector('[data-testid=booking-bill-balance]').innerText, txt: document.querySelector('[data-testid=booking-confirmation]').innerText.slice(0,140)})")
    print("PASS T1 booking:", out["t1"])
    await page.click('[data-testid="booking-done-btn"]', force=True)
    print("toast:", await toast())
    await page.wait_for_timeout(6000)
    await page.click('[data-testid="fd-chip-arrivals-today"]', force=True)
    await page.wait_for_timeout(1500)
    rid = await page.evaluate("() => { const r=[...document.querySelectorAll('[data-testid^=fd-row-]')].find(e => /QA P3 Suite/.test(e.innerText) && /^fd-row-\\d+$/.test(e.getAttribute('data-testid'))); return r ? r.getAttribute('data-testid').split('-')[2] : null; }")
    badge = await page.evaluate(f"() => document.querySelector('[data-testid=fd-row-{rid}-badge]')?.innerText")
    print("row id", rid, "badge", badge)
    # --- T4 check-in
    await page.click(f'[data-testid="fd-row-{rid}-checkin-btn"]', force=True)
    await page.wait_for_selector('[data-testid="checkin-form"]', timeout=10000)
    out["t4bill"] = await page.evaluate("() => ({total: document.querySelector('[data-testid=checkin-bill-total]').innerText, paid: document.querySelector('[data-testid=checkin-bill-paid]').innerText, bal: document.querySelector('[data-testid=checkin-bill-balance]').innerText})")
    opts = await page.evaluate("() => [...document.querySelectorAll('[data-testid=checkin-room-select] option')].map(o => [o.value, o.innerText])")
    print("rooms offered:", opts)
    pick = next((v for v, t in opts if v in ("8525", "8527", "8528")), None)
    assert pick, "no allowed room"
    await page.select_option('[data-testid="checkin-room-select"]', pick)
    await page.fill('[data-testid="checkin-collect-amount"]', "500")
    await page.click('[data-testid="checkin-pay-card"]', force=True)
    await page.fill('[data-testid="checkin-pay-ref"]', "QACARD3")
    await page.wait_for_selector('[data-testid="checkin-progress-ready"]', timeout=5000)
    await page.click('[data-testid="checkin-confirm-btn"]', force=True)
    print("checkin toast:", await toast())
    await page.wait_for_timeout(7000)
    await page.click('[data-testid="fd-tab-inhouse"]', force=True)
    await page.wait_for_timeout(1500)
    await page.click('[data-testid="fd-chip-inhouse-all"]', force=True)
    await page.wait_for_timeout(1000)
    for _ in range(20):
        loading = await page.evaluate(f"() => !!document.querySelector('[data-testid=fd-row-{rid}-balance-loading]')")
        if not loading: break
        await page.wait_for_timeout(1000)
    out["inhouse"] = await page.evaluate(f"() => {{ const b=document.querySelector('[data-testid=fd-row-{rid}-balance]'); return {{bal: b?.innerText, title: b?.getAttribute('title'), row: document.querySelector('[data-testid=fd-row-{rid}]')?.innerText.replace(/\\s+/g,' ').slice(0,200)}} }}")
    print("PASS T4 → in-house:", out["t4bill"], out["inhouse"])
    await page.screenshot(path="/app/memory/evidence/CR-385/phase3_qa/p3_inhouse_balance.png", quality=30)
    # --- P3-3 extend +1 night, collect 500 cash
    await page.click(f'[data-testid="fd-row-{rid}-extend-btn"]', force=True)
    await page.wait_for_selector('[data-testid="extend-form"]', timeout=10000)
    out["extend_before"] = await page.evaluate("() => ({total: document.querySelector('[data-testid=extend-current-total]').innerText, paid: document.querySelector('[data-testid=extend-current-paid]').innerText, bal: document.querySelector('[data-testid=extend-current-balance]').innerText, disabled: document.querySelector('[data-testid=extend-confirm-btn]').disabled})")
    y, m, d = map(int, co.split("-")); import datetime
    new_co = (datetime.date(y, m, d) + datetime.timedelta(days=1)).isoformat()
    await page.fill('[data-testid="extend-checkout"]', new_co)
    await page.fill('[data-testid="extend-reason"]', "qa extend")
    await page.click('[data-testid="extend-collect-toggle"]', force=True)
    await page.fill('[data-testid="extend-collect-amount"]', "500")
    await page.click('[data-testid="extend-pay-cash"]', force=True)
    await page.set_viewport_size({"width": 1366, "height": 768})
    await page.wait_for_timeout(500)
    out["t6_extend"] = await page.evaluate("() => { const e=document.querySelector('[data-testid=extend-bill]'); return {sh: e.scrollHeight, ch: e.clientHeight}; }")
    dups = await page.evaluate("() => { const c={}; document.querySelectorAll('[data-testid]').forEach(e => { const k=e.getAttribute('data-testid'); c[k]=(c[k]||0)+1; }); return Object.entries(c).filter(([k,v]) => v>1).map(([k]) => k); }")
    print("T6 extend-bill", out["t6_extend"], "X-10 dups:", dups)
    await page.screenshot(path="/app/memory/evidence/CR-385/phase3_qa/p3_extend_form_1366.png", quality=30)
    await page.set_viewport_size({"width": 1920, "height": 800})
    await page.click('[data-testid="extend-confirm-btn"]', force=True)
    await page.wait_for_selector('[data-testid="extend-result"], [data-testid="extend-server-error"], [data-testid="extend-conflict"]', timeout=40000)
    out["extend_result"] = await page.evaluate("() => ({err: document.querySelector('[data-testid=extend-server-error]')?.innerText, conflict: document.querySelector('[data-testid=extend-conflict-message]')?.innerText, total: document.querySelector('[data-testid=extend-bill-total]')?.innerText, sgst: document.querySelector('[data-testid=extend-bill-sgst]')?.innerText, cgst: document.querySelector('[data-testid=extend-bill-cgst]')?.innerText, paid: document.querySelector('[data-testid=extend-bill-paid]')?.innerText, bal: document.querySelector('[data-testid=extend-bill-balance]')?.innerText, nights: [...document.querySelectorAll('[data-testid^=extend-nights-][data-testid$=-source]')].map(e => e.innerText)})")
    print("P3-3 extend result:", out["extend_result"])
    await page.screenshot(path="/app/memory/evidence/CR-385/phase3_qa/p3_extend_result.png", quality=30)
    if out["extend_result"].get("total"):
        await page.click('[data-testid="extend-done-btn"]', force=True)
        print("extend toast:", await toast())
        await page.wait_for_timeout(8000)
        out["after_extend_row"] = await page.evaluate(f"() => document.querySelector('[data-testid=fd-row-{rid}]')?.innerText.replace(/\\s+/g,' ').slice(0,200)")
        print("row after extend:", out["after_extend_row"])
        # --- P3-4 shorten back
        await page.click(f'[data-testid="fd-row-{rid}-extend-btn"]', force=True)
        await page.wait_for_selector('[data-testid="extend-form"]', timeout=10000)
        await page.fill('[data-testid="extend-checkout"]', co)
        await page.fill('[data-testid="extend-reason"]', "qa shorten")
        btn_txt = await page.inner_text('[data-testid="extend-confirm-btn"]')
        await page.click('[data-testid="extend-confirm-btn"]', force=True)
        await page.wait_for_selector('[data-testid="extend-result"], [data-testid="extend-server-error"], [data-testid="extend-conflict"]', timeout=40000)
        out["shorten"] = {"btn": btn_txt, **(await page.evaluate("() => ({err: document.querySelector('[data-testid=extend-server-error]')?.innerText, total: document.querySelector('[data-testid=extend-bill-total]')?.innerText, bal: document.querySelector('[data-testid=extend-bill-balance]')?.innerText, nights: [...document.querySelectorAll('[data-testid^=extend-nights-][data-testid$=-source]')].map(e => e.innerText)})"))}
        print("P3-4 shorten:", out["shorten"])
        if out["shorten"].get("total"):
            await page.click('[data-testid="extend-done-btn"]', force=True); print("shorten toast:", await toast()); await page.wait_for_timeout(6000)
    # --- P3-6 HK round trip
    hk_before = await page.inner_text(f'[data-testid="fd-row-{rid}-hk-btn"]')
    await page.click(f'[data-testid="fd-row-{rid}-hk-btn"]', force=True)
    print("HK toast:", await toast()); await page.wait_for_timeout(7000)
    hk_after = await page.inner_text(f'[data-testid="fd-row-{rid}-hk-btn"]')
    await page.click(f'[data-testid="fd-row-{rid}-hk-btn"]', force=True)
    print("HK2 toast:", await toast()); await page.wait_for_timeout(7000)
    hk_final = await page.inner_text(f'[data-testid="fd-row-{rid}-hk-btn"]')
    out["hk"] = [hk_before, hk_after, hk_final]; print("P3-6 HK:", out["hk"])
    # --- settle via old departures
    order_id = await page.evaluate(f"() => {{ const r=document.querySelector('[data-testid=fd-row-{rid}]'); r?.click(); return null; }}")
    await page.goto(BASE + "/pms/departures", wait_until="domcontentloaded")
    await page.wait_for_selector('[data-testid="dep-tab-upcoming"]', timeout=40000); await page.wait_for_timeout(2000)
    await page.click('[data-testid="dep-tab-upcoming"]', force=True); await page.wait_for_timeout(1500)
    btns = await page.evaluate("() => [...document.querySelectorAll('[data-testid^=dep-row-]')].filter(r => /QA P3 Suite/.test(r.innerText)).map(r => r.innerText.replace(/\\s+/g,' ').slice(0,160))")
    print("departures rows:", btns)
    oid = await page.evaluate("() => { const r=[...document.querySelectorAll('[data-testid^=dep-row-]')].find(r => /QA P3 Suite/.test(r.innerText)); const b=r?.querySelector('[data-testid^=dep-checkout-btn-]'); return b ? b.getAttribute('data-testid').replace('dep-checkout-btn-','') : null; }")
    print("order", oid)
    await page.click(f'[data-testid="dep-checkout-btn-{oid}"]', force=True)
    await page.wait_for_selector('[data-testid="collect-payment-panel"]', timeout=40000); await page.wait_for_timeout(2500)
    out["pos_balance"] = await page.evaluate("() => document.querySelector('[data-testid=checkout-room-balance]')?.innerText")
    print("POS room balance:", out["pos_balance"], "| FD balance was:", out["inhouse"]["bal"])
    await page.click('[data-testid="payment-cash-btn"]', force=True); await page.wait_for_timeout(800)
    await page.click('[data-testid="complete-payment-btn"]', force=True)
    print("settle toast:", await toast()); await page.wait_for_timeout(5000)
    left = await page.evaluate("() => [...document.querySelectorAll('[data-testid^=dep-row-]')].filter(r => /QA P3/.test(r.innerText)).length")
    print("PASS settle; QA rows left on departures:", left)
    await page.screenshot(path="/app/memory/evidence/CR-385/phase3_qa/p3_settled.png", quality=30)
except Exception as e:
    print("FAIL", repr(e)[:400])
    await page.screenshot(path="/app/memory/evidence/CR-385/phase3_qa/p3_fail.png", quality=30)
finally:
    json.dump({"out": out, "caps": caps, "console_errors": errors[:20]}, open("/app/memory/evidence/CR-385/phase3_qa/p3_mutating_run.json", "w"), indent=1)
    print("console errors:", len(errors), errors[:3])
    print("caps keys:", {k: len(v) for k, v in caps.items()})
