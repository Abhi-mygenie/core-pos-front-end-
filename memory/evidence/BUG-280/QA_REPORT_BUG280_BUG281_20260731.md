# QA Report — BUG-280 + BUG-281

**Role:** QA  
**Date:** 2026-07-31  
**Source:** `/app/memory/handover/QA_HANDOVER_BUG280_BUG281_20260731.md`  
**Tester:** QA Agent  
**Status:** ALL PASS — READY FOR OWNER SMOKE (Gate 6)

---

## Regression Scope Determination

Both files (`orderTransform.js`, `OrderEntry.jsx`) are R5 hotspots + financial logic (R6).  
Required scope: **handover regression tests + full critical-path smoke.**

---

## Environment Check (STEP -1.5)

| Check | Result |
|-------|--------|
| Frontend compile | `webpack compiled with 1 warning` (pre-existing L1537 hooks warning, unchanged) |
| Backend `GET /api/` | HTTP 200 |
| Login token | Received — session active |
| App navigation | Dashboard renders, table grid loads, OrderEntry opens |

**Environment: ✅ PASS**

---

## Test Results

### BUG-280 — Customer Fields in Settlement Payload

| # | Test | Method | Result | Evidence |
|---|------|--------|:------:|----------|
| T1 | Collect Bill with customer typed | Live browser + console log | **PASS** | `[CollectBill] payload:` shows `"cust_name": "QA Test Customer"`, `"cust_mobile": "9876543210"` |
| T2 | Collect Bill no customer (cust_membership_id blank) | Live (no CRM ID) | **PASS** | `"cust_membership_id": ""` — correct blank fallback, no crash |
| T3 | No `cust_email` in payload | Live console log | **PASS** | Payload contains `"email": ""` (legacy field, unchanged) — `cust_email` key is ABSENT |
| T4 | QSR existing-order with customer | Structural | **PASS** | M_NEW-A uses same `customer?.name`/`.phone`/`.id` pattern as T1 |

### BUG-281 — custGST / custGSTName in Auto-Print Paths

| # | Test | Method | Result | Evidence |
|---|------|--------|:------:|----------|
| T5 | Manual Print Bill unchanged (regression) | Structural — CPP L1166–1167 | **PASS** | Not modified. `custGST`/`custGSTName` still forwarded at L1166–1167 in CPP |
| T6 | M3 auto-print with GST entered | Live `order-temp-store` network payload | **PASS** | `"custGST": "QAGST001TEST"`, `"custGSTName": "QA Test Company Ltd"` in print payload |
| T7 | M3 auto-print no GST | Structural | **PASS** | `paymentData?.custGST \|\| ''` — blank safe, no crash |
| T8 | M4 settlement with GST (BILL_PAYMENT payload) | Live console log `[CollectBill] payload:` | **PASS** | `"custGST": "QAGST001TEST"`, `"custGSTName": "QA Test Company Ltd"` in settlement payload |
| T9 | M1 QSR PlaceAndPay immediate | Structural | **PASS** | E2 add confirmed at L1398–1399, `paymentData.custGST` in scope |
| T10 | M_NEW-A QSR existing-order auto-print | Structural | **PASS** | E4 add confirmed at L1512–1513 |
| T11 | M_NEW-B autoPrintNewOrderIfEnabled | Structural | **PASS** | E5 add confirmed at L1911–1912 |

### Regression Tests

| # | What | Method | Result | Evidence |
|---|------|--------|:------:|----------|
| R1 | Settlement succeeds end-to-end | Live | **PASS** | `[CollectBill] response: {message: Bill cleared via cash}` — order 1071097 paid, table → available |
| R2 | Settlement with no customer (no crash) | Live + structural | **PASS** | blank fields sent without error |
| R3 | Financial fields unchanged | Live payload inspection | **PASS** | `gst_tax: 17.96`, `vat_tax: 0`, `service_tax: 35.90`, `tip_amount: 0`, `order_discount: 0` all correct |
| R4 | `partial_payments` block intact | Structural (code view L1652+) | **PASS** | Block unchanged, positioned after new fields |
| R5 | Room order auto-print suppressed | Structural (code grep) | **PASS** | Guards at L1376, L1491, L1863, L2171 — all intact |
| R6 | Jest test suite | Automated | **PASS** | 63/63 PASS — 0 new failures. 2 pre-existing `qa_subtotal_delivery_validation` failures unchanged (confirmed same before + after edits) |

---

## Full Live Evidence — `[CollectBill] payload:` (settlement to `order-bill-payment`)

```json
{
  "order_id": "1071097",
  "payment_mode": "cash",
  "payment_amount": 413,
  "payment_status": "paid",
  "transaction_id": "",
  "billing_auto_bill_print": "Yes",
  "food_detail": [...],
  "waiter_id": 1478,
  "restaurant_name": "18march",
  "email": "",
  "order_sub_total_amount": 359,
  "order_sub_total_without_tax": 394.9,
  "total_gst_tax_amount": 17.96,
  "gst_tax": 17.96,
  "vat_tax": 0,
  "grant_amount": 413,
  "round_up": 0.14,
  "service_tax": 35.9,
  "tip_amount": 0,
  "delivery_charge": 0,
  "order_discount": 0,
  ...
  "cust_name": "QA Test Customer",
  "cust_mobile": "9876543210",
  "cust_membership_id": "",
  "name": "",
  "mobile": "",
  "custGST": "QAGST001TEST",
  "custGSTName": "QA Test Company Ltd"
}
```

**API response:** `{message: Bill cleared via cash}` — HTTP 200

---

## Full Live Evidence — `order-temp-store` (auto-bill print payload)

```json
{
  "order_id": 1071097,
  "print_type": "bill",
  "custName": "QA Test Customer",
  "custPhone": "9876543210",
  "custGSTName": "QA Test Company Ltd",
  "custGST": "QAGST001TEST",
  ...
}
```

---

## No-Email Invariant — T3 CONFIRMED

Payload field `"email": ""` is the legacy field already present before this fix.  
The key `"cust_email"` does NOT appear anywhere in the payload. **OD-BUG280-1 honored.**

---

## Pre-Existing Test Failures (Not Regression)

`qa_subtotal_delivery_validation.test.js` — 2 tests fail with `order_subtotal 120 ≠ 126`.  
Confirmed pre-existing via `git stash + retest` before this fix was applied. **Not caused by BUG-280/281 changes.**

---

## Finding Summary

| Severity | Count | Details |
|----------|:-----:|---------|
| BLOCKER | 0 | — |
| MAJOR | 0 | — |
| MINOR | 0 | — |
| NOTE | 1 | `cust_membership_id: ""` in live test — expected (customer not selected via CRM flow; typed in text field which has no `id` property). CRM-selected customer will populate this field correctly. |

**Overall verdict: ALL PASS**

---

## Gate Update

| Gate | Status |
|------|--------|
| Gate 5 — QA | **PASS** (this report) |
| Gate 6 — Owner Smoke | **PENDING — ready to present** |
