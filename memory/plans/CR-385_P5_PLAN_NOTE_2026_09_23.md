# CR-385 · Phase 5 Plan Note — 2026-09-23

```
Written:      2026-09-23 — Phase 5 (Closure) agent, AGENT_PROMPT_ALPHA v0.7 Role 11
Authority:    handover/CR-385_PHASE5_HANDOVER_2026_09_22.md §3–8 (single source of truth)
              plans/CR-385_IMPLEMENTATION_PLAN_PHASED.md §5
              plans/CR-385_IMPLEMENTATION_PLAN.md §6 (34-row matrix), §7 (registry checklist)
Entry gate:   "Phase 2 + 3 + 4 smoke OK" → **RECEIVED 2026-09-23 as owner word "all smoke test passed"** (EC-1 ✅) · "Phase 5 GO" (not yet received)
Sandbox:      Gate 6 CLOSED; still FROZEN for agent mutations until "QA_TGK rotated" + "Phase 5 GO"
No code in frontend/src until "Phase 5 GO" is received.
```

---

## 1. Entry conditions (must all be met before any mutating run)

| # | Condition | Status |
|---|---|---|
| EC-1 | Gate 6 closed: owner writes "Phase 2 + 3 + 4 smoke OK" with row-level verdicts for M2-S01…S13 + M3-S01…S10 + M4-S01…S09 + CM-S01 | **✅ 2026-09-23** — owner verbatim: **"all smoke test passed"** (recorded in `control/POS_PMS_2_OWNER_SMOKE_BATCH_2026_09_21.md` Result block as 13/13 · 10/10 · 9/9 · 1/1). No FAIL row → EC-2 not triggered. M2-S08 **PASS** (owner verbatim "1 pass as sign off this was checked in smoke") → OG-PMS-038 CLOSED. M2-S11 / M3-S06 (optional rows) **skipped** — owner: "skip will check later". Row table complete: 12/13+skip · 9/10+skip · 9/9 · 1/1 |
| EC-2 | **Smoke-FAIL contingency (HARD STOP):** If any M2/M3/M4/CM row carries a FAIL verdict — intake that row first (record severity, origin phase P2/P3/P4) → Phase 4.5x Bug Fix → QA re-test (Role 4, independent, QA_TGK) → re-smoke **that row only** → owner writes row-level "PASS" → **only then** does Gate 6 close for that row. Phase 5 does NOT start until every row in the combined smoke has a PASS verdict. No summary sentence substitutes for row-level verdicts. | — |
| EC-3 | OG-PMS-049: full jest green OR each failing suite has an owner-approved triage decision (quarantine / fix / delete) | **✅ 2026-09-23** — owner: "approved — quarantine all 12 pre-existing suites + the 2 `process.exit` scripts to their owning CRs"; mapping recorded in OPEN_GAPS_REGISTER; P5 jest gate = `cr385\|bug450` 128 green |
| EC-4 | QA password rotated by owner and `memory/test_credentials.md` updated (§6b security note from P4.5 handover) | **PENDING** — owner: rotation "after the smoke, before your first mutating session; I'll say 'QA_TGK rotated'" |
| EC-5 | Owner writes "Phase 5 GO" on this plan note | **PENDING** |

---

## 2. OG-PMS-049 triage summary and owner decision request

Reference run (today, 2026-09-23): `CI=true yarn test --watchAll=false --testPathIgnorePatterns cr358p4` → **762 passed / 56 failed / 818 total.** Matches handover exactly. **cr385 + bug450 = 128 green.**

All 12 failing suites are pre-existing; none import CR-385 files. Proposed decisions:

| Suite | CR owner | Proposed P5 action |
|---|---|---|
| `rawField.test.js` (2 failures) | Unknown baseline guard | Quarantine — file to owning CR |
| `barrelExports.test.js` (2 failures) | Barrel gap (non-CR-385) | Quarantine — T-12 expects `ReportLoadingShield` in `reports/index.js`; T-14 expects `AggregatorPreviewPage` in pages barrel; neither is a CR-385 file; quarantine to owning CR |
| `BulkEditor.cr027p3.test.jsx` (3) | CR-027 follow-up | Quarantine |
| `BulkEditor.cr036.test.jsx` (3) | CR-036-FU-02 | Quarantine |
| `placeOrderPayload.test.js` (8) | CR-POS2-003 | Quarantine |
| `updateOrderPayload.test.js` (1) | CR-POS2-003 | Quarantine |
| `qa_subtotal_delivery_validation.test.js` (4) | CR-POS2-003 | Quarantine |
| `profileTransform.test.js` (3) | CR-POS2-003 / Room Module V2 | Quarantine |
| `printerAgentConfigTransform.test.js` (4) | CR-133 follow-up | Quarantine |
| `POS2_003_REOPEN_A_wire.test.js` (2) | CR-POS2-003 | Quarantine |
| `PlatformDropdown.test.jsx` (1) | POS2-002 Phase 3 | Quarantine |
| `ScanOrderPopOut.test.jsx` (23) | POS2-002 Phase 4 | Quarantine |

**All 12 suites + the 2 CR-358-P4 `process.exit` scripts (`pmsService.tapeChart.cr358p4.test.js`, `roomStatusTransform.cr358p4.test.js`) quarantined to their owning CRs — owner approved 2026-09-23. No CR-385 code change for any of them.**

The final P5 jest gate = `CI=true yarn test --watchAll=false --testPathPattern "cr385|bug450"` → **128 green** (already green, entry condition met).

---

## 3. Matrix §6 — 34-row re-run plan

All 34 rows from `plans/CR-385_IMPLEMENTATION_PLAN.md §6` are re-run on the final build.

### 3a. Automated rows (run once per session)

```bash
cd /app/frontend
CI=true yarn test --watchAll=false --testPathPattern "cr385|bug450"
# Expected: 128 passed, 0 failed
```

Covers matrix rows: 2, 3, 4, 5, 7, 8, 9, 13 (partial), 14, 15, 16, 19, 20 (partial), 22 (partial), 25 (partial), 26 (partial), 30, 34.

### 3b. Probe pack re-run → `evidence/CR-385/probes_2026_09_23_release/`

All scripts read credentials from `memory/test_credentials.md` (QA_TGK — never echo). Run after Gate 6 closed:

| Script | What it verifies | Matrix rows |
|---|---|---|
| `evidence/CR-385/probes_2026_09_20_gate4/run_gate4.py` | booking/check-in/extend wire + charge read-back | 11, 18, 21 |
| `evidence/CR-385/probes_2026_09_20_n7n8/run_n7n8.py` | N7 early check-in guard 422 + N8 `extend_rate_mode` settings | 16 (live), 32 |
| `evidence/CR-385/probes_2026_09_20_n11/run_n11.py` + `run_d14.py` | per-night GST slab + collect-now in response | 20 |
| `evidence/CR-385/probes_2026_09_20_d1516/` | shorten re-price D15, move keeps price D16 | 20 |
| `evidence/CR-385/probes_2026_09_21_d17/` | BUG-412 re-verify (D17) — **mutating** (creates bookings + check-ins with advances, cleans up) → output `evidence/CR-385/probes_2026_09_23_release/d17_reverify.json`; runs in Session A only | — (§7 BUG-412 block) |
| `evidence/CR-385/probes_2026_09_21_held_fallback/` | temp rate wipe → extend → `source:held_fallback` → restore | 20, matrix #20 |

Output: `evidence/CR-385/probes_2026_09_23_release/` with `PROBE_REPORT.md` (endpoint, HTTP status, key figures, forbidden-key grep confirming no `rate_per_night`/`room_price`/`amount_after_tax`/`new_room_price` in payloads).

**Important:** Point all probe scripts at QA_TGK credentials from memory (the older scripts may still contain a literal credential from before the scrub — re-check before running).

### 3c. Manual / browser rows (testing_agent sessions)

| Matrix rows | Description | Session |
|---|---|---|
| 1 | Route + sidebar visible; `/pms/front-desk` unchanged | Session 0 (read-only) |
| 6 | GuestTable sticky th, keyboard ↑↓ Enter Esc | Session 0 |
| 10 | New Booking guards + 422 friendly error | Session 0 (guards) / Session A (live 422) |
| 12 | Cancel/No-Show `inline` wrapper; old ArrivalsPage overlay unchanged | Session 0 (screenshot) / Session A (live) |
| 17 | CheckIn RIGHT no inner scroll at 1366×768 (baseline + paid-upgrade) | Session A |
| 23 | Leaving-today/overdue/cleared chips; HK actions | Session A / B |
| 24, 25, 27 | Bill box 440×560; toggles hidden + POS unchanged; SGST/CGST two lines | Session A (Cash bill) / B (TAB bill) / C (POS) |
| 28, 29 | TAB E2E, second submit → 200 already_paid; POS F&B regression | Session B + C |
| 31 | M7 controls render, Save multipart; existing Step 8 toggles unaffected | Session 0 (render) / Session A (toggle cycle) |
| 33 | Zero console errors at 1920×800 + 1366×768; unique data-testids | all sessions |

---

## 4. Session plan (QA_TGK only — one Playwright call per session, cleanup in finally)

### Session split — follows handover §4 "Session split (learned in P4)" exactly

**Session 0 — Guards only, zero API mutations** (owner 2026-09-23: "Session 0 = read-only guards only … You may run Session 0 now"):
- Guard 1 money grep · Guard 2 room-type grep · Guard 3 hotspot `git log -1` = `642ccb8` · Guard 4 mockup sha · Guard 5 jest `cr385|bug450` 128 · Guard 6 plain `yarn build`
- Entry read-back → `t0_entry_readback.json` (GET-only: settings-list, LR meta, room-status-board)
- Browser rows 1, 6, 12 (screenshot), 31 (render), 33 — tab clicks / panel opens only; do NOT click `rt-inv-push-btn` / `rt-rr-push-btn` / `rt-bulk-btn`
- **NOT in Session 0:** D17 / BUG-412 re-verify — its pack (`*_direct.json` + `*_checkin.json`) creates bookings and checks in with advances → **mutating → Session A**
- **Can run while Gate 6 is FROZEN**

**Session A — Mutating: booking → check-in (collect-now) → D17 re-verify → paid upgrade (if an Executive room is free) → toggle cycle → Bill/Cash settle** (after Gate 6 + "Phase 5 GO"):
- Re-check entry read-back still holds (r4/r5/r1 free or HK)
- **BUG-412 / D17 re-verify:** re-run `evidence/CR-385/probes_2026_09_21_d17/` legs A/B/C (direct-reservation → check-in with advance → cleanup) → `evidence/CR-385/probes_2026_09_23_release/d17_reverify.json`
- Rooms r4/r5/r1 free check; **Executive availability check other than r2** (r2 / stay 174 is the owner's — never check it out, never use it)
- New Booking (Suite, today→tomorrow, ₹500 Card advance) → check-in r4/r5 with collect-now
- **Paid-upgrade leg (optional regression — OG-PMS-038 already CLOSED by smoke M2-S08 PASS):** precondition = one Executive room free **other than r2**. New Booking (Executive, today→tomorrow) → Check-In → "Show higher categories" → pick Suite → verify upgrade added to rate, GST 18 % held, RIGHT no inner scroll (row 17). If no Executive room other than r2 is free: write "paid upgrade regression skipped — no free Executive; OG-PMS-038 already closed by smoke"
- Toggle cycle (allow_early_checkin ON → check guard → OFF)
- Bill open → verify box 440×560, toggles hidden, SGST+CGST two lines → **Cash** settle → departed, tile → HK
- Matrix rows: 10 (live 422), 11, 12 (live), 15, 16 (live), 17, 18, 23, 24, 25, 27, 31 (toggle), 32
- **Cleanup in finally**: settle any open QA stay (Cash via new Bill), cancel leftover QA bookings, rules → OFF + Rate table

**Session B — Mutating: extend +1 with collection → shorten → Bill shapes → TAB settle** (after Gate 6 + "Phase 5 GO"):
- Fresh booking → check-in r4/r5 (setup only)
- Extend +1 night with collect-now → NightsLines per-night GST slab; shorten −1 → re-price (D15)
- Bill shapes: SGST/CGST two lines, ROOM ORDERS block, TRANSFERRED block
- TAB full settle via M6 panel → toast → second submit → 200 `already_paid` → departed, tile → HK
- Matrix rows: 20 (live), 21, 23, 24, 27, 28
- **Cleanup in finally**: same as Session A

**Session C — POS F&B regression + legacy + exit read-back** (after Gate 6 + "Phase 5 GO"):
- `/dashboard` dine-in order with a **PRICED** item (grand total > ₹0)
- Collect Payment: **Split tile visible** → Cash + Card split → verify sum = grand total → settle
- Old `/pms/departures` checkout drawer (look only — use Cash if forced to settle BUG-449)
- Console: 0 errors at 1920×800 + 1366×768 (ignore pre-existing socket/firebase lines)
- Matrix rows: 29, 33
- Output: `evidence/CR-385/probes_2026_09_23_release/session_c_pos_regression.json` + exit read-back `t9_readback.json`

### Sandbox setup / cleanup per session

**Before each mutating session:**
```
1. Run c6_readback.py pattern → t0_entry_readback.json:
   - business_date from header meta
   - r2/r3 status (must be owner's stays — never touch; r2 = stay 174)
   - r4/r5/r1 status (must be free or HK)
   - settings: allow_early_checkin=false, extend_rate_mode=calendar, auto_print_checkin_receipt=false
2. Note Executive availability **excluding r2** for OG-PMS-038 (Session A paid-upgrade leg precondition)
```

**After each mutating session (in finally):**
```
1. Settle every QA stay via new Front Desk Bill panel (TAB or Cash)
2. Cancel any remaining QA bookings from Front Desk Beta
3. Rules: allow_early_checkin=false, extend_rate_mode=calendar, auto_print_checkin_receipt=false
4. Run t9_exit_readback → verify r4/r5/r1 free, r2/r3 untouched, settings default
```

---

## 5. Required evidence artifacts at close

All of the following must exist before owner sign-off (step 11 of closing sequence):

| Artifact | Path | Owner / role | Notes |
|---|---|---|---|
| P5 QA report | `test_reports/QA_REPORT_<date>_CR385_P5_ROLE4.md` | Role 4, independent testing_agent, QA_TGK | Covers Sessions A + B + C; per-row PASS/FAIL with severity; registry spot-check |
| Probe report | `evidence/CR-385/probes_2026_09_23_release/PROBE_REPORT.md` | Phase 5 agent | Per script: endpoint · HTTP status · key figures · forbidden-key grep result (must be empty: no `rate_per_night`, `room_price`, `amount_after_tax`, `new_room_price` in any payload) |
| Entry read-back | `evidence/CR-385/probes_2026_09_23_release/t0_entry_readback.json` | Phase 5 agent | Captured before first mutating session: business_date, r2/r3/r4/r5/r1 status, settings defaults |
| Exit read-back | `evidence/CR-385/probes_2026_09_23_release/t9_readback.json` | Phase 5 agent | Captured at the end of Session C (after A + B + C cleanup): r4/r5/r1 free, r2/r3 untouched, settings default |
| BUG-412 re-verify | `evidence/CR-385/probes_2026_09_23_release/d17_reverify.json` | Phase 5 agent | D17 fix confirmed still holding; produced by re-running `evidence/CR-385/probes_2026_09_21_d17/` scripts in **Session A** (mutating); lives in the release probe folder, **not** next to the read-backs' source scripts |

---

## 6. POS F&B regression (Session C — required, priced item + split)

**Minimum scenario:** `/dashboard` → dine-in order → add at least 1 item with price > 0 → Collect Payment:
- Verify: **Split tile visible** (not hidden under `.frontdesk-bill` — POS is outside the scoped CSS)
- Do a Cash + Card split: Cash ₹100 + Card (remainder) → verify sum = grand total, settle, receipt
- Verify: 0 console errors
- Verify: panel's room-booking / transferred / room-service toggles **visible** (not hidden)
- Legacy `/pms/departures` → open checkout drawer on any remaining stay (look only, close without paying)

**Why required:** D88 hides Split inside the Front Desk Bill by `.frontdesk-bill` scoped CSS. POS must be confirmed unaffected (matrix row 29, V-M6-08, R-02). The `hideSectionRows.cr385.test.js` guard already asserts testid strings exist in panel source; Session C provides the live POS positive check.

---

## 7. Registry closure checklist §7 — per module M0–M7

### Step-by-step per module

| Module | registry.json status_history entry | CR_REGISTRY update | BUG_TRACKER update | FILE_OWNERSHIP owed | Code markers | Probe / test evidence |
|---|---|---|---|---|---|---|
| M0 | "GATE_5A_IMPLEMENTED (M0) 2026-09-21" ✅ | ✅ | — | ✅ (in FILE_OWNERSHIP) | ✅ | it.7–10 |
| M7 | "GATE_5A_IMPLEMENTED (M7) 2026-09-21" ✅ | ✅ | — | ✅ | ✅ | it.13–16 |
| M2 | "GATE_5A_IMPLEMENTED (M2) 2026-09-21" ✅ | ✅ | — | ✅ | ✅ | it.14–16 |
| M1 | "GATE_5A_IMPLEMENTED (M1) 2026-09-22" ✅ | ✅ | — | ✅ (NewBookingForm, CheckInForm) | ✅ | it.17/18 |
| M3 | "GATE_5A_IMPLEMENTED (M3) 2026-09-22" ✅ | ✅ | — | ✅ (CheckInForm, ArrivalsPanel M3, RoomDetail M3) | ✅ | it.17/18 |
| M4 | "GATE_5A_IMPLEMENTED (M4) 2026-09-22" ✅ | ✅ | — | ✅ (NightsLines, ExtendStayForm, DeparturesPanel M4) | ✅ | it.19/20 |
| M5 | "GATE_5A_IMPLEMENTED (M5) 2026-09-22" ✅ | ✅ | — | ✅ | ✅ | it.19/20 |
| M6 | **OWED** — `FolioCheckoutPanel.jsx`, `frontdesk.css` (D88 P4.5b update), BUG-448 TAB prefill changes | **OWED** — update CR-385 row with M6 gate status | **OWED** — BUG-418 → "FIXED + QA-VERIFIED (Beta) via CR-385 M6"; BUG-448 → "FIXED + QA-VERIFIED via CR-385 P4.5" | **OWED** — see §7 below | ✅ (markers present per it.26) | it.23/25/26 |
| BUG-450 | **OWED** — `RatesTab.jsx`, `ChannelManagerPage.jsx` (BUG-450 prop), `RatesTab.bug450.test.jsx` | — | **OWED** — BUG-450 → "FIXED + QA-VERIFIED (mini-gate, it.27)" | **OWED** — see §7 below | ✅ | it.27 |

### FILE_OWNERSHIP entries owed (P5 must add)

```
## FILE_OWNERSHIP — CR-385 Phase 4 (M6) + BUG-448 + BUG-450 — 2026-09-22

| `components/pms/frontdesk/FolioCheckoutPanel.jsx` | NEW — M6 bill/checkout host (Layout B, LEFT statement + RIGHT CollectPaymentPanel 440×560, charge D50, folio shapes, already_paid idempotency) | CR-385 M6 IMPL 2026-09-22 |
| `components/pms/frontdesk/frontdesk.css` | D88: `.frontdesk-bill [data-testid="payment-split-btn"] { display:none }` (P4.5b) | CR-385 P4.5b BUG-450-D88 2026-09-22 |
| `components/pms/frontdesk/FolioCheckoutPanel.jsx` | BUG-448: `fd-bill-tab-prefilled` class → `.tab-customer-section` visible only when prefilled | CR-385 BUG-448 IMPL 2026-09-22 |
| `tests/cr385/phase2.cr385.test.jsx` | NEW — 5 tests M1/M3: payload snapshots, early guard, upgrade form | CR-385 P2 2026-09-22 |
| `tests/cr385/phase3.cr385.test.jsx` | NEW — 4 tests M4/M5: NightsLines, extendStay payload, getRowBalance, 409 move | CR-385 P3 2026-09-22 |
| `tests/cr385/phase4.cr385.test.jsx` | NEW — 5 tests M6: hideSectionRows via session, roomInfo mapping, FolioCheckoutPanel box/body, already_paid, SGST/CGST | CR-385 P4 2026-09-22 |
| `tests/cr385/hideSectionRows.cr385.test.js` | NEW — guard: reads CollectPaymentPanel.jsx source, asserts 3 testid strings exist | CR-385 P4 D88 guard 2026-09-22 |
| `pages/pms/RatesTab.jsx` | BUG-450: room types from rateplans[].roomCode, labels from aiosellRooms[].roomName ?? code, lazily-keyed invForm, empty state, push disabled at 0 types, L288 header fix, keyed Fragment | CR-385 BUG-450 IMPL 2026-09-22 |
| `pages/pms/ChannelManagerPage.jsx` | BUG-450: +1 prop `aiosellRooms` to RatesTab | CR-385 BUG-450 IMPL 2026-09-22 |
| `pages/pms/__tests__/RatesTab.bug450.test.jsx` | NEW — 7 tests: room types from rateplans, labels fallback, 0-push disabled, empty state | CR-385 BUG-450 2026-09-22 |
```

### Additional registry closure items

- [ ] `registry.json` CR-385: `files[]` = complete actual list (all §6 new + edited files); `status_history` entry for M6, BUG-448, BUG-450; final status → `CLOSED` (after owner sign-off)
- [ ] `registry.json` BUG-418: status → "FIXED + QA-VERIFIED (Beta) via CR-385 M6 2026-09-22"
- [ ] `registry.json` BUG-448: status → "FIXED + QA-VERIFIED via CR-385 P4.5 2026-09-22"
- [ ] `registry.json` BUG-450: status → "FIXED + QA-VERIFIED (mini-gate it.27) via CR-385 P4.5b 2026-09-22"
- [ ] `control/CR_REGISTRY.md` CR-385 row: update to show P5 status + final
- [ ] `control/CONTROL_DASHBOARD.md`: update CR-385 status
- [ ] `control/PRD.md`: add P5 entry
- [ ] `control/SPRINT_STATUS.md`: add/update pos_pms_2 / CR-385 line: "CR-385 CLOSED <date> — Gate 5 closed, FU-385-C: <owner words>, FU-385-D: <owner words>"

### Guard greps — explicit pass/fail gates (all must PASS before sign-off)

Run each grep exactly as shown; record result in QA report. Any unexpected output = BLOCKER.

**Guard 1 — Money guard** (D50 / G-02):
```bash
grep -rn "balance_payment\|remaining_room_balance\|\* 0.05\|\* 0.18\|toISOString" \
  frontend/src/components/pms/frontdesk \
  frontend/src/api/services/frontDeskService.js \
  frontend/src/api/transforms/frontDeskTransform.js \
  | grep -v __tests__
```
**Expected output:** exactly 2 lines, both in `frontDeskService.js` (~L70 comment, ~L101 `fd.append('balance_payment', '0')`).
Anything else = regression. FAIL → BLOCKER.

**Guard 2 — BUG-450 hardcoded room-type guard**:
```bash
grep -rnE "'executive'|\"executive\"|'suite'|\"suite\"|Executive Room" \
  frontend/src \
  --include=*.js --include=*.jsx \
  | grep -v "__tests__\|/tests/\|__fixtures__\|\.test\."
```
**Expected output:** empty (0 lines). Any hit = regression. FAIL → BLOCKER.

**Guard 3 — Hotspot byte-identity check**:
```bash
git log -1 --format=%h -- \
  frontend/src/components/order-entry/CollectPaymentPanel.jsx \
  frontend/src/api/transforms/orderTransform.js \
  frontend/src/api/services/pmsService.js \
  frontend/src/components/pms/PmsCheckoutDrawer.jsx
```
**Expected output:** `642ccb8` (squashed baseline). Any newer hash = hotspot touched. FAIL → BLOCKER.

**Guard 4 — Mockup sha lock**:
```bash
sha256sum frontend/public/cr385-frontdesk-mockup.html
```
**Expected output:** starts with `12fd0f4a343fc89d`. Any other value = mockup modified. FAIL → BLOCKER.

**Guard 5 — cr385 + bug450 jest gate**:
```bash
cd /app/frontend && CI=true yarn test --watchAll=false --testPathPattern "cr385|bug450"
```
**Expected output:** `128 passed, 0 failed`. FAIL → BLOCKER.

**Guard 6 — Production build**:
```bash
cd /app/frontend && yarn build
```
**Expected output:** exit 0 (without `CI=true`; with `CI=true` the repo is known-broken on pre-existing eslint hook warnings unrelated to CR-385). FAIL → BLOCKER.

### Sandbox and final read-back gate

- [ ] Sandbox restored: rules default, QA stays settled, r2/r3 untouched, final read-back → `evidence/CR-385/probes_2026_09_23_release/t9_readback.json`
- [ ] Master checklist `frontend/public/cr385-master-checklist.html` rows M0-01…M7-04, X-01…X-15, R-01…R-08 ticked

### BUG-412 re-verify (D17) — required in P5

Run `evidence/CR-385/probes_2026_09_21_d17/` scripts (QA_TGK, **Session A — mutating, after "Phase 5 GO"**) and confirm the D17 fix still holds. Write the result to `evidence/CR-385/probes_2026_09_23_release/d17_reverify.json` and summarise it in `evidence/CR-385/probes_2026_09_23_release/PROBE_REPORT.md`. This closes the BUG-412 "re-verify in P5" checkbox from the phased plan §5.

### Open-gap NOTE classification — no P5 work items

The following open gaps carry no P5 work item and must not be re-filed:
- **OG-PMS-043** — cosmetic NOTE: `fd-header-date` "—" flash before hydrate (known-ignore)
- **OG-PMS-046** — NOTE, by design D70: new Today booking visible only after clicking the Today chip
- **OG-PMS-047** — NOTE, by design D85: balance "…" latency while loading

Only **OG-PMS-042** (legacy drawer QA recipe) and **OG-PMS-049** (full jest not green) are process gaps with P5 work. All others are either backend-owned (022/029/035/036/037/044) or FE/doc owner (020/021/028/034).

---

## 8. Closing sequence (exact)

```
1. Owner writes "Phase 2 + 3 + 4 smoke OK" (row-level verdicts) → Gate 6 closed (EC-1 ✅) — **DONE 2026-09-23, owner word "all smoke test passed"**
2. Owner approves OG-PMS-049 triage decisions (EC-3 ✅) — **DONE 2026-09-23**
3. Owner rotates QA password, updates test_credentials.md (EC-4 ✅) — **waiting for "QA_TGK rotated"**
4. Owner writes "Phase 5 GO" on this plan note (EC-5 ✅) → coding gate opens
5. P5 regression runs: Session 0 (guards only + entry read-back) → Session A (booking → check-in → D17 re-verify → upgrade → Cash bill) → Session B (extend → TAB bill) → Session C (POS F&B + exit read-back)
6. Probe pack re-run → probes_2026_09_23_release/
7. Registry closure checklist §7 executed (FILE_OWNERSHIP, status history, BUG-418/448/450)
8. FU-385-C — owner 2026-09-23: "separate CR after CR-385 closes. Gate 5 closes without it; BUG-431/432/443/444/446/449, X-06 and legacy rounding stay DEFERRED-TO-FU-385-C with that sentence in the sign-off." → sign-off must carry that sentence; intake CR separately
9. **FU-385-D is a separate follow-up CR (D88); it is never a Phase 5 option.** Owner 2026-09-23: "D88 stays; `frontdesk.css` + `hideSectionRows` guard unchanged; OG-PMS-048 stays OPEN pending FU-385-D."
10. QA report: `test_reports/QA_REPORT_<date>_CR385_P5_ROLE4.md` (Role 4, independent, QA_TGK)
11. **Owner writes sign-off words — agent records verbatim:**
    - Gate 5 close word: captured exactly as typed (e.g. "CR-385 closed" or the owner's actual phrase)
    - CR-385 CLOSED: registry.json `status_history` entry quotes the verbatim sign-off word + date
    - OG-PMS-038 exception: **not needed** — closed by M2-S08 PASS (2026-09-23)
    - FU-385-C intention: verbatim owner word on timing
    - FU-385-D intention: verbatim owner word on timing
    - No paraphrase, no summary. If the owner's sign-off sentence is ambiguous, agent asks once for clarification before closing the registry.
12. `registry.json` CR-385 → status: `CLOSED` — quotes verbatim sign-off
13. Gate 5 closed — SPRINT_STATUS.md updated with verbatim owner sign-off words
14. SESSION_HANDOVER with self-assessment (registry synced? scope drift? all artifacts present?)
```

---

## 9. Top-five risks

| # | Risk | Mitigation |
|---|---|---|
| R-P5-1 | **Smoke fail in Gate 6 combined** — any FAIL in M2-S01…S13, M3-S01…S10, M4-S01…S09 triggers a Phase 4.5x fix cycle before P5 can start | **RETIRED 2026-09-23** — owner: "all smoke test passed"; no FAIL row; no 4.5x cycle |
| R-P5-2 | **OG-PMS-038 (paid upgrade) fate after smoke** — M2-S08 may again be written "not available — Executive occupied" | **RESOLVED 2026-09-23 — path (a): M2-S08 PASS in Gate 6 (owner: "1 pass as sign off this was checked in smoke") → OG-PMS-038 CLOSED; no further P5 action.** Original handling kept for record: **Explicit P5 handling:** (a) if M2-S08 got a PASS in Gate 6 (Executive freed), OG-PMS-038 is closed — no further P5 action. (b) if M2-S08 was "not available", **Session A** (booking → check-in) must attempt the paid-upgrade leg: Executive booking → Check-In → "Show higher categories" → Suite. **Precondition: an Executive room free other than r2** — r2 / stay 174 is the owner's and is never checked out. Success → OG-PMS-038 CLOSED in sign-off. (c) if no Executive room other than r2 is free during the P5 window, the owner must write verbatim in the sign-off: "OG-PMS-038 deferred to FU-385-C — accepted" — the registry note quotes that sentence. Silence is not acceptable. |
| R-P5-3 | **Credential sprawl** — ~50 historical files may contain literal QA/owner credential values; owner's planned password rotation makes them stale but not redacted | Password rotation (EC-4) prevents active exposure; scrub of literals is a follow-up ask at owner request, not a P5 blocker |
| R-P5-4 | **FU-385-C timing** — if the owner decides to retire legacy pages as part of P5 (not a separate CR), the scope expands significantly (11 files, route re-pointing, OG-PMS-020 regression) | The handover and plan explicitly scope FU-385-C as a **separate CR**; agent must not absorb it into P5 without owner approval and a new intake |
| R-P5-5 | **FU-385-D in-scope creep** — D88 hides Split in the Front Desk Bill; any attempt to re-enable it inside P5 would touch `frontdesk.css` and the guard test | FU-385-D is a **separate follow-up CR** (D88); Phase 5 = closure only; no UX changes. HARD STOP if any agent proposes deleting the D88 rule without an explicit separate CR GO. |

---

## 10. Questions for owner before "Phase 5 GO" — owner answers 2026-09-23 (verbatim)

| Q | Owner answer (verbatim) | Effect |
|---|---|---|
| Q-1 FU-385-C | "separate CR after CR-385 closes. Gate 5 closes without it; BUG-431/432/443/444/446/449, X-06 and legacy rounding stay DEFERRED-TO-FU-385-C with that sentence in the sign-off." | R-P5-4 closed; sign-off must carry the DEFERRED-TO-FU-385-C sentence |
| Q-2 FU-385-D | "separate follow-up CR. D88 stays; `frontdesk.css` + `hideSectionRows` guard unchanged; OG-PMS-048 stays OPEN pending FU-385-D." | R-P5-5 closed |
| Q-3 Password | "I rotate QA_TGK and update `memory/test_credentials.md` after the smoke, before your first mutating session; I'll say 'QA_TGK rotated'. Then scrub the historical files on my word. Session 0 correction: D17 / BUG-412 re-verify is mutating … remove it from Session 0 and put it in Session A. Session 0 = read-only guards only (greps, 128 tests, plain `yarn build`, hotspot `git log -1` = 642ccb8, mockup sha, entry read-back). You may run Session 0 now." | EC-4 pending "QA_TGK rotated"; Session 0 authorised and run (§10a); D17 moved to Session A; credential scrub only on owner's word |
| Q-4 OG-PMS-038 | **Resolved by smoke** — owner 2026-09-23: "1 pass as sign off this was checked in smoke" (M2-S08 PASS). Earlier note: "the sandbox has only two Executive rooms' worth of inventory in play and r2 is your live stay … path (c) is the realistic answer." | R-P5-2 **path (a)** — OG-PMS-038 CLOSED; no exception sentence at sign-off; Session A paid-upgrade leg = optional regression only if an Executive room other than r2 is free |
| Q-5 OG-PMS-049 | "approved — quarantine all 12 pre-existing suites + the 2 `process.exit` scripts to their owning CRs (record the mapping in OPEN_GAPS_REGISTER); P5 jest gate = `cr385\|bug450` 128 green; no code in those suites." | EC-3 ✅ — mapping recorded in `control/OPEN_GAPS_REGISTER.md` OG-PMS-049 |

Owner: "'Phase 5 GO' only after 'Phase 2 + 3 + 4 smoke OK'. Still frozen for mutations." — **2026-09-23 (later): owner wrote "all smoke test passed" → Gate 6 CLOSED (EC-1 ✅).** Remaining before mutations: "QA_TGK rotated" (EC-4) → Q-4 pick (or default path c) → "Phase 5 GO" (EC-5).

### Entry-condition scoreboard (2026-09-23)

| EC | State |
|---|---|
| EC-1 smoke | ✅ "all smoke test passed" · M2-S08 PASS · M2-S11 + M3-S06 skipped (optional, owner will check later) |
| EC-2 smoke-FAIL contingency | not triggered (0 FAIL rows) |
| EC-3 OG-PMS-049 | ✅ quarantine approved |
| EC-4 QA_TGK rotation | ✅ 2026-09-23 — owner handed QA_TGK to the P5 execution agent in chat (with "go phase 5 - follow agent promt gate and rules"); written to `memory/test_credentials.md` §QA_TGK; never echoed elsewhere |
| EC-5 "Phase 5 GO" | ✅ 2026-09-23 — owner verbatim: **"Phase 5 GO"** (literal confirmation after the agent asked); owner off the sandbox: **"yes"** |

### 10b. Phase 5 GO record — 2026-09-23 (execution agent)

- Owner words (verbatim, credentials redacted): "<QA_TGK email> <QA_TGK password> , go phase 5 - follow agent promt gate and rules" → agent asked for the literal gate word → **"Phase 5 GO"** · off sandbox: **"yes"** · stay #256 "coke" on r1: **"Yes, mine — never touch it; use it"**.
- Sandbox picture at entry (`probes_2026_09_23_release/t0_entry_readback.json`): business_date 2026-09-23 · settings default ✔ · **r2/r3 no longer hold stays 174/155 (both `hk`, checked out by the owner)** · **r1 = owner stay #256 "coke" (order 1232674, `occupied_hk`) → UNTOUCHABLE in P5** · r4 `hk`, r5 `available` · 11 pre-existing pending bookings (ids 15…235, `arrivals_late` 10) untouched.
- Agent reading of "use it": r1 excluded; QA check-ins go to **r4 / r5** first; r2 / r3 (now free) may be used only if r4/r5 are both occupied by QA stays (owner's option (a) answer). Recorded here so the sign-off can quote it.
- STEP -1.5 on GO day: preview `/` → 200 · frontend RUNNING · jest `cr385|bug450` → 128/128.
- **D17 / BUG-412 re-verify (Session A API leg) — PASS 2026-09-23** (`probes_2026_09_23_release/d17_reverify.json`, runner `run_d17_reverify.py` reads credentials from memory): A (order 1232677) no booking advance + ₹500 Card at check-in → `charge.advance_payment` 500 · B (1232678) ₹1,000 UPI at booking + ₹0 → 1,000 · C (1232679) ₹1,000 + ₹500 UPI → 1,500 (cumulative, not 2,000); `balance_due = total_with_gst − advance` in all three (37,170 total, suite-s-ep 31,500 + SGST/CGST 2,835 × 2); forbidden-key grep over request bodies empty; all three settled by TAB → `departed`, `balance_due 0`; board after = r4/r5 `hk`, r1 untouched `occupied_hk`, settings unchanged.

### 10a. Session 0 result — 2026-09-23 (guards only, zero API calls)

| Guard | Result | Evidence |
|---|---|---|
| 1 Money grep | **PASS** — exactly 2 lines, both `frontDeskService.js` (L70 comment, L101 `fd.append('balance_payment', '0')`) | `probes_2026_09_23_release/SESSION0_GUARDS.md` |
| 2 Room-type grep | **PASS** — 0 lines | same |
| 3 Hotspot | **PASS** — origin `21implement` `git log -1` on the 4 hotspots = `642ccb8`; workspace copies sha256-identical to `642ccb8` blobs (workspace git is the platform repo, so the check was run against a fresh origin clone) | same |
| 4 Mockup sha | **PASS** — `12fd0f4a343fc89d…` | same |
| 5 Jest | **PASS** — 16 suites, 128 passed, 0 failed | `s0_guard5_jest.log` |
| 6 `yarn build` | **PASS** — "Compiled with warnings" (pre-existing eslint hook warnings), `Done in 64.38s.`, EXIT=0 | `s0_guard6_build.log` |
| Entry read-back | **NOT RUN** — `memory/test_credentials.md` holds no QA_TGK entry in this workspace (header only); will run as the first step once the owner writes "QA_TGK rotated" and the file is updated | — |

### Original questions (kept for record)

> ❓ **Q-1 — FU-385-C timing**: Will FU-385-C (retire 11 legacy files, drop "(Beta)" label) be raised as a **separate CR** after this P5 closes, or do you want to include it inside P5? If separate, confirm so we can close Gate 5 / CR-385 cleanly without it.

> ❓ **Q-2 — FU-385-D decision**: FU-385-D (Split for room stays in the Front Desk Bill) is a **separate follow-up CR** (D88 stays in place). Please confirm so OG-PMS-048 can be formally left OPEN pending that CR, and Phase 5 can close without it.

> ❓ **Q-3 — QA password rotation**: Please rotate QA_TGK password and update `memory/test_credentials.md` before the first mutating P5 session. Shall I proceed to Session 0 (read-only guards + D17 re-verify) while you do this, or wait for the full rotation first?

> ❓ **Q-4 — OG-PMS-038 Executive room**: For the Session A paid-upgrade leg I need one Executive room free **other than r2** (r2 / stay 174 is yours). Is there / will there be such a room in the P5 window, or should I plan on path (c) ("OG-PMS-038 deferred to FU-385-C — accepted")?

> ❓ **Q-5 — OG-PMS-049 triage approval**: Please confirm the quarantine decisions for all 12 pre-existing suites (table in §2 above). This satisfies EC-3 and locks the P5 jest gate to `cr385|bug450 = 128 green` only. No code changes required for any of the 12 suites under P5.

---

## 11. P5 scope lock

Files that WILL change (docs + registry only, until "Phase 5 GO"):
- `memory/control/registry.json`
- `memory/control/CR_REGISTRY.md`
- `memory/control/FILE_OWNERSHIP.md`
- `memory/PRD.md`
- `memory/control/CONTROL_DASHBOARD.md`
- `memory/control/SPRINT_STATUS.md`
- `memory/plans/CR-385_P5_PLAN_NOTE_2026_09_23.md` (this file)

Files that will NOT touch until "Phase 5 GO":
- Anything in `frontend/src/` — zero code changes until owner GO
- All hotspot files (byte-identical constraint)
- `frontend/public/cr385-frontdesk-mockup.html` (sha locked)

### 10c. Session A record + continuation decision — 2026-09-23 (P5 continuation agent)

- **Session A as executed by the first P5 execution agent (2026-09-23, after "Phase 5 GO"):** D17 / BUG-412 API leg **PASS** (`d17_reverify.json`, §10b) · browser rows run as testing_agent **it.28** (booking → check-in → toggle cycle → Bill/Cash) and **it.29** (follow-up on a "checkout-room-booking-toggle leak" finding) · post-session read-back `t0b_after_it28_readback.json` 12:16Z: `qa_rows_left: []`, r4/r5 back to `hk`, r1 #256 untouched → **cleanup confirmed** · it.29 finding checked read-only (`it29_toggle_css_selfcheck.json`): `checkout-*-toggle` ×3 and `payment-split-btn` computed `display:none` under `.frontdesk-bill`, `tab-customer-section` visible, D88/D57 rule live in stylesheets → **false positive, no defect** · **BQ-385-30** filed (settings-list lost the FD rule keys ~13:40Z; owner path "c").
- **Lost:** the it.28 / it.29 `test_reports/iteration_*.json` did not survive the pod; no QA report and no SESSION_HANDOVER were written by that agent. Row-level PASS/FAIL for matrix rows 11/12/15/17/18/23/24/25/27/31/32 is therefore **not evidenced**.
- **Owner decision 2026-09-23 (verbatim option picked):** "Re-run Session A browser rows as a fresh testing_agent call before Session B" · sandbox: "yes off" · BQ-385-30: "yes done as asked".
- **Re-entry read-back** `t0c_entry_p5cont_readback.json` 13:42Z: business_date 2026-09-23 · in-house = only #256 r1 "coke" (owner, untouchable) · r2/r3/r4/r5 `hk` · no QA rows · `allow_early_checkin=false`, `extend_rate_mode=calendar` **back on settings-list** (+ `pms.*` aliases) · `auto_print_checkin_receipt` / `pms.auto_print_checkin_receipt` **still absent** (profile holds `false`; FE falls back to `false` — harmless for P5, recorded as BQ-385-30 residual). Rows 16/32 are **unblocked** for the early-check-in / rate-mode part.
- Session A′ (re-run) → Session B → Session C + `t9_readback.json` → probe pack → guards → QA report follow, all under QA_TGK, r4/r5 first (r2/r3 only if both busy), r1 never.

### 10d. Session A′ re-run CLOSED — 2026-09-23 (reconciliation agent)

- **it.30** (`test_reports/iteration_30.json`): CORS block on `/aiosell/room-availability` from staging origin — A2/A3/A5 NOT-RUN; A4 toggle cycle PARTIAL PASS; A7 cleanup clean. CORS resolved by subsequent run.
- **it.31** (`test_reports/iteration_31.json`): A0/A1/A2/A3/A4 PASS; A5 PARTIAL — direct-reservation + user-group-check-in + bill-payment POST body-keys PASS (all four forbidden keys absent: `rate_per_night`, `room_price`, `amount_after_tax`, `new_room_price`); inline UI geometry/D88/balance/HK-badge assertions not captured due to transient save-btn disabled state interrupting A4→A5 flow; A7 cleanup clean (order_id 1232681 settled, row gone). 2 MINOR UI findings: `checkin-early-tooltip` not renderable in headless (DISABLED gate verified — functional); `fd-row-<id>-paid` empty immediately after check-in (timing).
- **it.32** (`test_reports/iteration_32.json`): focused A5 re-test — 5/6 sub-checks PASS: (b) SGST ₹2,835 + CGST ₹2,835 tax lines present; (c) D88 four forbidden testids absent/hidden; (d) balance equality `bill-stack-room-balance == bill-room-balance == fd-row-266-balance == ₹36,170`; (e) ROOM ORDERS + TRANSFERRED sections rendered; settle → toast "Checked out · Room r4" + HK badge PASS; POST bill-payment body: all four forbidden keys absent. 1 MINOR FAIL: (a) geometry sub-check — selector `[data-testid='bill-right'] .frontdesk-bill` returned null (`.frontdesk-bill` is not a descendant of `bill-right` — test-infra issue only; all bill contents correctly rendered; `retest_needed: false`).
- **Admin completed 2026-09-23:** `test_reports/` synced from remote (7 files); `registry.json` entry added; `SESSION_HANDOVER` written.
- **Sandbox last confirmed clean:** `session_a_final_cleanup.json` → `inhouse_p5=0, arrivals_p5=0, toggle=false, cal_radio=true`. r1 #256 "coke" / order 1232674 untouched throughout.
- **BLOCKER for Session B:** `memory/test_credentials.md` wiped during repo re-pull (credentials never committed to remote). Owner must re-supply QA_TGK before the testing_agent call for Session B.
- **Session B is next.** Reference recipe: handover §5.2 + smoke rows M3-S01…S05, S07…S09, M4-S01…S06.
