# QA Plan — CR-163 + BUG-388
## PMS Sprint pos_pms_1: Gate 5b QA + Regression

**Plan Date:** 2026-09-13
**QA Agent:** ROLE 4 (AGENT_PROMPT_ALPHA v0.7)
**Status:** PLAN ONLY — execution pending

---

## Precondition Check

| Item | Registry EXIT GATE | Handover exists | Coverage | Proceed? |
|---|---|---|---|---|
| **CR-163** | ✅ GATE_5A_IMPLEMENTED, sprint pos_pms_1, 5/5 EXIT GATE in status | ✅ `QA_HANDOVER_CR163_2026_09_13.md` | 3/3 files covered | ✅ YES |
| **BUG-388** | ✅ §4 of handover confirms EXIT GATE: ALL 5 PASSED | ✅ `QA_HANDOVER_BUG388_2026_09_09.md` | 2/2 files covered | ✅ YES |

---

## Regression Scope Determination

Per AGENT_PROMPT_ALPHA rules:

| Item | Files Changed | Hotspot / Financial? | Regression Required |
|---|---|---|---|
| **CR-163** | `SplitRoomItemsModal.jsx`, **`OrderEntry.jsx`**, `roomService.js` | `OrderEntry.jsx` = **R5 HOTSPOT** | Handover regressions (R-1..R-3) + **2 cross-flow tests** |
| **BUG-388** | **`CheckInPage.jsx`**, `pmsService.js` | GST calculation = **R6 FINANCIAL** | Handover regressions (R-388-01..03) + **full critical-path smoke** |
| **Cross-item** | Both items touch PMS check-in + room order flow | Both in pos_pms_1 | 2 cross-item interaction tests |

---

## Execution Sequence

Run in this order (oldest pending first, financial before UI):

```
BLOCK 1 — BUG-388 (financial — run first, highest risk)
  TC-388-01 → TC-388-05 (GST slab + totals)
  R-388-01, R-388-02, R-388-03 (regressions)
  SMOKE-388: full critical-path (login → check-in → payload verify)

BLOCK 2 — CR-163 (UI + flow)
  V-01 → V-06 (UI: picker renders, cards, labels, disabled state)
  V-07 → V-12 (network: payloads, Dine-In board, GAP1, no-duplicate)
  R-1, R-2, R-3 (regressions)
  XF-163-01, XF-163-02 (cross-flow: OrderEntry hotspot)

BLOCK 3 — Cross-item regression
  CI-01, CI-02 (interaction between CR-163 + BUG-388 + CR-162)
```

---

## BLOCK 1 — BUG-388: GST Base Fix on Check-In

**What was fixed:** `CheckInPage.jsx` + `pmsService.js` — advance payment now included in GST base so the correct slab is applied. `balance_payment` formula corrected to not double-subtract advance.

**Risk: R6 FINANCIAL — full critical-path smoke required.**

### Test Cases

| TC | Steps | Expected | Severity |
|---|---|---|---|
| **TC-388-01** | Login → `/pms/check-in` → Walk-in → Room ₹7,500 + Advance ₹100, 1 night | GST strip: **18% slab**, CGST ₹684, SGST ₹684, Total GST ₹1,368, **Total incl. GST ₹8,968** | BLOCKER |
| **TC-388-02** | Same → confirm check-in | Network payload: `gst_tax: 1368`, `balance_payment: 8868` | BLOCKER |
| **TC-388-03** | Walk-in → Room ₹8,000 + Advance ₹0, 1 night | 18% slab, GST ₹1,440, Total ₹9,440, balance ₹9,440 | MAJOR |
| **TC-388-04** | Walk-in → Room ₹5,000 + Advance ₹0, 1 night | 5% slab, GST ₹250, Total ₹5,250, balance ₹5,250 | MAJOR |
| **TC-388-05** | Walk-in → Room ₹7,400 + Advance ₹200, 1 night | gstBase=7,600 → 18%, GST ₹1,368, Total ₹8,968, balance ₹8,768 | BLOCKER |

### Regressions

| R | Test | Why |
|---|---|---|
| **R-388-01** | Room ₹7,400 + advance ₹0 → 5% slab, GST ₹370, balance ₹7,770 | Zero-advance path: `gstBase = orderAmount` (unchanged from before fix) |
| **R-388-02** | `gst_tax` field present in payload (not '0.00' or missing) | BUG-386 fix must still hold |
| **R-388-03** | Non-WalkIn (Direct/Online booking): same GST formula | Booking type guard in `pmsCheckIn` unchanged |

### Critical-Path Smoke (R6 — MANDATORY)

| SMOKE | Flow | Expected |
|---|---|---|
| **SMOKE-388** | Login → Dashboard loads → `/pms/check-in` → fill room details + advance → GST strip shows correct slab → confirm → network payload verified | Full flow completes, no crash, payload values correct end-to-end |

### Known Skip

> ⚠️ **BUG-389 in effect:** Room ₹7,500 + advance ₹0 → gstBase=7,500 still hits 5% slab (backend slab2.min=7500.01 unresolved). Do NOT fail TC for this case. It's a backend config issue tracked separately.

---

## BLOCK 2 — CR-163: Move Items Destination Picker

**What was built:** `SplitRoomItemsModal.jsx` destination picker (Path A: existing free table, Path B: create "Room r2"). `OrderEntry.jsx` Path A/B dispatch. `roomService.js` `switchOrderTable()` + `targetTableId`.

**Risk: R5 HOTSPOT (`OrderEntry.jsx`) — handover regression + 2 cross-flow tests.**

### Pre-conditions

- Room r2 (order on table_id 8526) must have at least 1 non-marker food item before starting
- "Room r2" table (id: 8553) already exists from probe session — V-12 confirms no duplicate created
- Tables 1/2/3 (ids 8529/8530/8531) must be `engage=No` (free) for Path A

### Test Cases — UI (V-01..V-06)

| TC | Steps | Expected | Severity |
|---|---|---|---|
| **V-01** | Login → open room r2 in Dashboard → click "Move Items" | Modal opens; "Select Destination" section visible with "MANDATORY" badge | BLOCKER |
| **V-02** | Same modal | Table 1, Table 2, Table 3 cards visible in 3-column grid; no room tables, no walk-in | MAJOR |
| **V-03** | Click "Table 1" card | Green border, #F0FDF4 bg, green dot indicator | MINOR |
| **V-04** | Select 1 food item, do NOT pick destination | Move button: grey background, label "Select Destination", not clickable | BLOCKER |
| **V-05** | Select item + pick Table 1 | Button green, label "Move 1 to Table 1" | MINOR |
| **V-06** | Select item + pick "Create Room r2" row | Row: dashed border → solid green, button "Create & Move 1 Item" | MINOR |

### Test Cases — Network + Flow (V-07..V-12)

| TC | Steps | Expected | Severity |
|---|---|---|---|
| **V-07** | Path A: confirm with Table 1 selected | DevTools: `split-room-order` POST body has `target_table_id: 8529` | MAJOR |
| **V-08** | After V-07 confirm | Dashboard Dine-In: Table 1 shows new order | MAJOR |
| **V-09** | Path B: confirm "Create Room r2" | DevTools: `table-config/store` POST fires with `table_no: "Room r2", rtype: "TB"` | MAJOR |
| **V-10** | After V-09 confirm | DevTools: `order-table-room-switch` fires; `all-table-list` returns "Room r2" with `engage: Yes` | MAJOR |
| **V-11** | After any split | Refresh source room r2 order → moved item absent or qty reduced | BLOCKER |
| **V-12** | "Room r2" table (8553) already exists → trigger Path B | DevTools: NO second `table-config/store` call; existing table reused (OD-163-05 A) | MAJOR |

### Regressions

| R | Test | Why |
|---|---|---|
| **R-1** | Open a dine-in table order → verify NO "Move Items" button | `onSplitItems` returns null for non-room — must still be true |
| **R-2** | Per-item "Transfer" button on room r2 order → transfer to an occupied walk-in | `TransferFoodModal` untouched — verify it still fires and moves item |
| **R-3** | CR-162: "+ Pay" on room order → Record Payment modal → enter amount + mode → confirm | `roomService.js` modified — `recordPartialPayment` must still save with `payment_type: interim` |

### Cross-Flow Tests (OrderEntry.jsx R5 hotspot — 2 required)

| XF | Flow | Expected |
|---|---|---|
| **XF-163-01** | Login → open a dine-in table order → place a new food item → settle via Collect Bill | Normal dine-in order flow unaffected by CR-163 changes to `OrderEntry.jsx` |
| **XF-163-02** | Login → open room order → per-item "Transfer" food → select occupied walk-in → confirm | Transfer flow still works; `handleTransfer` untouched by CR-163 |

---

## BLOCK 3 — Cross-Item Regression (pos_pms_1 interaction)

Both CR-163 and BUG-388 touch the PMS check-in flow. Run after Blocks 1 and 2 pass.

| CI | Flow | Tests interaction between |
|---|---|---|
| **CI-01** | Walk-in check-in (BUG-388 path): fill room ₹7,500 + advance ₹100 → confirm check-in → room is now checked in → open that room's order in OrderEntry → click "Move Items" (CR-163) → select destination → confirm split | BUG-388 (GST on check-in) + CR-163 (split after check-in) — no interference |
| **CI-02** | Room check-in → add food item to room order → record mid-stay partial payment (CR-162) → then "Move Items" (CR-163 Path A) | CR-162 (`recordPartialPayment`) + CR-163 (`splitRoomOrder`) both on same room — `roomService.js` handles both independently |

---

## Coverage Summary

| Item | Files | Test cases | Regressions | Cross-flow | Total |
|---|---|---|---|---|---|
| BUG-388 | 2/2 ✅ | TC-388-01..05 (5) | R-388-01..03 (3) | SMOKE-388 (1) | **9** |
| CR-163 | 3/3 ✅ | V-01..V-12 (12) | R-1..R-3 (3) | XF-163-01..02 (2) | **17** |
| Cross-item | — | — | — | CI-01..CI-02 (2) | **2** |
| **TOTAL** | | | | | **28 checks** |

---

## Registry Spot-Check (run at end of all tests)

```python
python3 -c "
import json
d = json.load(open('/app/memory/control/registry.json'))
items = {i['id']: i for i in d.get('items', d.get('change_requests', []))}
for cid in ['CR-163', 'BUG-388']:
    item = items.get(cid, {})
    print(cid, '| gate:', item.get('gate'), '| sprint:', item.get('sprint_key'), '| status prefix:', item.get('status','?')[:50])
"
```

Expected: both at gate=5, sprint=pos_pms_1. If mismatch → REGISTRY DRIFT finding (BLOCKER).

---

## Credentials

```
POS URL:       https://preprod.mygenie.online
Login:         owner@thegoankitchen.com / Qplazm@10
restaurant_id: 69 (The Goan Kitchen)
Preview:       https://pos-frontend-deploy-31.preview.emergentagent.com

Room for CR-163: r2 (table_id: 8526) — needs food item added first
Free dine-in:   Table 1 (8529), Table 2 (8530), Table 3 (8531)
"Room r2" table: already exists (id: 8553) — do NOT create again
```

---

## Report Path

When executing, write to:
`/app/memory/test_reports/QA_REPORT_CR163_BUG388_<DATE>.md`

Format per finding:
```
| # | Test | PASS/FAIL | Severity | Evidence |
```
