# CR-385 · Phase 0.5 — Bug-fix plan note (Planning-lite, LOW risk) — 2026-09-21

```
Items:     BUG-434 · BUG-435 · BUG-436 · BUG-437 · BUG-438 (all P0.5, LOW risk, no hotspots, RELATED CR-385) · sprint_key pos_pms_2
Owner GO:  "Phase 0.5 GO. Severities confirmed as listed." (2026-09-21) · decisions D70 (437 → a) · D71 (435 → coalesce + 5 s)
Role:      BUG FIX (Role 5) then QA (Role 4). Files WILL change: pages/pms/FrontDeskWorkstationPage.jsx · components/pms/frontdesk/{RoomsPanel,ArrivalsPanel,DeparturesPanel}.jsx · tests under components/pms/frontdesk/__tests__/ + api/transforms/__tests__/frontDeskTransform.cr385.test.js
Will NOT touch: every other file (hotspots, legacy PMS pages, transforms/services, .env, mockup HTML, App.js, Sidebar.jsx). Any need beyond this list → STOP, ask owner.
Entry Verification (§4.2, run 2026-09-21): M0.5 markers 0 · anchors found by content; numeric drift only: hook L27–47 (handover ~L26–46), <main> L112 (~L106), error card L118–125 / fd-retry-btn L123 (~L118–124); RoomsPanel onRetry L12 / retry btn L44; CHIP_ORDER L7; GuestTable onKeyDown L27; searchSnapshot L17 (endsWith L15); hotspots-clean; mockup sha 12fd0f4a…1168b86 unchanged.
Repro method: failing RTL/jest test written BEFORE each fix (Step 0), then fix, then green (Step 3).
```

| Bug | File · line (pre-fix) | Change | Test (jest) | QA row |
|---|---|---|---|---|
| **434** | `FrontDeskWorkstationPage.jsx` L123 `fd-retry-btn`; L104 `<RoomsPanel …>`; `RoomsPanel.jsx` L12 props, L44 `fd-rooms-retry-btn` | `disabled={refreshing}` + `<Loader2 animate-spin/>` + "Retrying…" while refreshing; page passes `retrying={refreshing}` to RoomsPanel, same treatment on `fd-rooms-retry-btn` | `phase05.cr385.test.jsx`: RoomsPanel boardError + retrying → button disabled, text "Retrying…"; retrying=false → "Retry" enabled | P0.5-1 |
| **435** | `FrontDeskWorkstationPage.jsx` L27–47 `useFrontDeskSnapshot` (refresh L33, focus L45) | `inFlightRef` — second `refresh()` while in flight returns the in-flight promise (coalesced, never dropped); `lastFetchRef` set on success; focus handler skips when `Date.now() − lastFetch < 5000` (D71). Hook exported (named) for RTL | `phase05.cr385.test.jsx` (renderHook, service mocked): mount → 1 call; 2× focus < 5 s → still 1; ↻ twice during in-flight → 1 extra call; focus after 6 s → new call | P0.5-2 |
| **436** | `FrontDeskWorkstationPage.jsx` L112 `<main>` | add `data-testid="fd-workstation-body"` (keep `fd-page`); QA brief + "Channel Manager" DOM assertion scoped to that id | DOM probe in QA (testing_agent): `fd-workstation-body` innerText has no "Channel Manager"; `fd-page` still present | P0.5-3 |
| **437** | `FrontDeskWorkstationPage.jsx` L24 `DEFAULT_CHIP`; `ArrivalsPanel.jsx` L7 `CHIP_ORDER` + L35–36/L51/L53; `DeparturesPanel.jsx` L25–26/L31/L33 | `DEFAULT_CHIP.arrivals/departures = null` (= auto). New `firstNonEmptyChip(counts, order)` (→ first key with count > 0, all-zero → `'today'`) exported from ArrivalsPanel; panels use `active = chip ?? firstNonEmptyChip(...)` so it re-evaluates on every refresh until the user clicks a chip (click sets a value → pinned for the session). In-House / Rooms stay `all` | unit `firstNonEmptyChip`; RTL ArrivalsPanel on the real fixture (Today 0 / Late 10) → "Late" `aria-selected`, rows visible; rows [] → Today; explicit `chip='today'` honoured; DeparturesPanel overdue row → Overdue | P0.5-4 |
| **438** | `components/pms/frontdesk/__tests__/GuestTable.cr385.test.jsx` (new), `GlobalSearch.cr385.test.jsx` (new), `api/transforms/__tests__/frontDeskTransform.cr385.test.js` (+1 test) | keyboard ↑↓ focus / Enter toggles / Esc closes; `searchSnapshot` phone-suffix (In-house `••••5723` → 155), room `r4` → Rooms group, guest name, empty query; `/` focuses input, Esc clears; `isTurn` on a live-shaped snapshot (fixture rooms + fromReservation rows) and 0 turns on the real fixture | the new files themselves; A4/A5/A6 + keyboard added to the P0.5 QA brief | P0.5-5 |

Exit: `CI=true yarn test --watchAll=false --testPathPattern=cr385` green (20 + new) · grep guards empty · `yarn build` exit 0 · testing_agent 1920×800 + 1366×768 (5 fixes + P0 matrix §1–§3 + A4/A5/A6/keyboard), zero console errors · EXIT GATE 5/5 · registry `GATE_5B_QA_PASSED (P0+P0.5)`, BUG-434…438 → QA-VERIFIED · checklist M0-01/02/03/10, X-10/X-12 re-evidenced · `handover/SESSION_HANDOVER_2026_09_21_CR385_P0_5.md` · combined owner smoke (phased plan §0.5 + master §4.4) → wait for "Phase 0 smoke OK".
