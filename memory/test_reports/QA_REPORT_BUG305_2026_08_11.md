# QA Report — BUG-305 orderTransform discountRatio Fix
**Date:** 2026-08-11
**QA Agent:** Role 4
**Handover read:** `/app/memory/handover/QA_HANDOVER_BUG305_2026_08_11.md`
**Test iterations:** iteration_12 (code verification) + iteration_13 (browser regression)

---

## Precondition Check
- Registry synced: **YES** ✅
- EXIT GATE: **5/5 PASS** ✅
- Status at QA start: `IMPLEMENTED — AWAITING QA` → proceeding

---

## Test Results

| # | Test Case | Type | Result | Severity if FAIL |
|---|---|---|---|---|
| T1 | `buildCartItem` line 748 has `_giveDiscount` marker | Code | **PASS** | — |
| T2 | `calcOrderTotals` line 787 declares discountable buckets | Code | **PASS** | — |
| T3 | `calcOrderTotals` forEach uses `item._giveDiscount !== false` | Code | **PASS** | — |
| T4 | `calcOrderTotals` uses `discountableRatio` (not `discountRatio`) | Code | **PASS** | — |
| T5 | `itemGstPostDiscount` uses split formula | Code | **PASS** | — |
| T6 | `vatTaxPostDiscount` uses split formula | Code | **PASS** | — |
| T7 | `buildBillPrintPayload` line 1859 declares buckets | Code | **PASS** | — |
| T8 | `buildBillPrintPayload` forEach reads `food_details.give_discount` | Code | **PASS** | — |
| T9 | `buildBillPrintPayload` split formulas at lines 1923-1929 | Code | **PASS** | — |
| V11 | App loads, OrderEntry opens, no JS errors | Browser | **PASS** | — |
| V12 | All-discountable cart + 10% discount: CGST=₹9, SGST=₹9, Total=₹378 | Browser | **PASS** | — |
| V13 | No discount: CGST=₹10, SGST=₹10, Total=₹420 | Browser | **PASS** | — |
| R1 | Place order → Collect Bill: correct subtotal, GST, grand total | Cross-flow | **PASS** | — |
| R2 | Collect Bill zero discount: GST non-zero, `discountableRatio=0` safe | Cross-flow | **PASS** | — |
| REGISTRY | BUG-305 + BUG-304 status = `IMPLEMENTED — QA PASS` | Registry | **PASS** | — |

**Total: 15/15 PASS. 0 FAIL. 0 BLOCKER. 0 MAJOR.**

---

## Math Verification (Live Browser)

| Cart | Discount | Expected GST | Actual GST | Match? |
|---|---|---|---|---|
| ₹400 (2 items, 5% GST) | None | CGST=₹10, SGST=₹10 | CGST=₹10, SGST=₹10 | ✅ |
| ₹400 (2 items, 5% GST) | 10% (= ₹40) | GST on ₹360 = ₹18 → CGST=₹9, SGST=₹9 | CGST=₹9, SGST=₹9 | ✅ |

**Math identity confirmed:** When all items are discountable, `discountableSubtotal = subtotal` → `discountableRatio = discountAmount/subtotal` (same as old `discountRatio`) → formula reduces to original. Zero regression.

---

## Coverage Sufficiency Check

| Changed file | Tests exercising changes | Covered? |
|---|---|---|
| `orderTransform.js` — `buildCartItem` (E1) | T1 (code), R1 (cart items built for place-order call) | ✅ |
| `orderTransform.js` — `calcOrderTotals` (E2) | T2-T6 (code), V12/V13/R1/R2 (collect-bill calls calcOrderTotals) | ✅ |
| `orderTransform.js` — `buildBillPrintPayload` (E3) | T7-T9 (code), R1 (collect-bill triggers auto-print via buildBillPrintPayload) | ✅ |

**Coverage: 3/3 changed functions have ≥1 test. COMPLETE.**

---

## Registry Spot-Check

```
BUG-305 | IMPLEMENTED — QA PASS (Gate 5b) | pos_5_1  ✅
BUG-304 | IMPLEMENTED — QA PASS (Gate 5b) | pos_5_1  ✅
```
**REGISTRY: SYNCED**

---

## Limitations (not failures)

- Mixed-cart scenario (non-discountable items + discount): Cannot test in browser — cafe103 has no `give_discount=No` items configured. Fix verified by code inspection only (T1-T9 confirm the split logic is correctly implemented). Owner Gate-6 smoke on a restaurant with non-discountable items recommended.

---

## QA Verdict

```
QA complete. 15/15 PASS. 0 failures (0 BLOCKER, 0 MAJOR, 0 MINOR).
Coverage: 3/3 changed functions exercised.
Registry: SYNCED — BUG-305 + BUG-304 both QA PASS.
Ready for Gate 6 (Owner Smoke).
QA report: /app/memory/test_reports/QA_REPORT_BUG305_2026_08_11.md
```
