# QA Report — BUG-170 + BUG-170 CR-029 Test Fix

**Date:** 2026-08-20
**QA Agent:** QA role (AGENT_PROMPT_ALPHA v0.7)
**Scope:** BUG-170 implementation + regression on CR-029/CR-170 test suite
**Result: PASS — 95/95**

---

## Test Execution

### T1–T4: BUG-170 Core Fix (code verification)

| # | Test | Steps | Expected | Actual | Result |
|---|---|---|---|---|---|
| T1 | MANUAL PATH has variationPerUnit | Read orderTransform.js MANUAL PATH | `variationPerUnit` computed from `item.variation[].values[].optionPrice` | Present at L1963-1966 | ✅ PASS |
| T2 | lineTotal includes variationPerUnit | Read L1967 | `(price×qty) + (addonPerUnit×qty) + (variationPerUnit×qty)` | Correct | ✅ PASS |
| T3 | COLLECT BILL PATH untouched | Read L1861-1938 | No variationPerUnit in `if (hasFinancialOverrides)` branch | Confirmed absent | ✅ PASS |
| T4 | Code marker present | grep BUG-170 in file | `// BUG-170` comment | Found at L1959 | ✅ PASS |

### T5: Mathematical validation

| Scenario | Before fix | After fix | Expected |
|---|---|---|---|
| Masala dosa large (+₹50) × 3, GST 5% | CGST = ₹7.50 (on ₹300 base only) | CGST = ₹11.25 (on ₹450 full) | ✅ Matches SS2 screenshot |
| Item with no variation | variationPerUnit = 0, lineTotal unchanged | Unchanged | ✅ No regression |

### T6–T7: Automated test suite

| Suite | Tests | Pass | Fail | Notes |
|---|---|---|---|---|
| `round001.alwaysCeil.test.js` | 17 | 17 | 0 | CR-170 spec ✅ |
| `cr029.roundUp.test.js` | 11 | 11 | 0 | Updated 1 test: old "clamp to 0" → new "negative passes through" (CR-170 alignment) |
| `orderTransformFinancials.test.js` | varies | all | 0 | ✅ |
| `orderTransformAddonQty.test.js` | varies | all | 0 | ✅ |
| `orderTransformAddOnQtys.test.js` | varies | all | 0 | ✅ |
| `orderTransform.orderFrom.test.js` | varies | all | 0 | ✅ |
| `orderTransform.roomInfo.test.js` | varies | all | 0 | ✅ |
| **TOTAL** | **95** | **95** | **0** | ✅ ALL PASS |

### T8: Compile check

| Check | Result |
|---|---|
| webpack warnings after BUG-170 | 1 (pre-existing, unrelated SettlementReportMockup) |
| New warnings introduced | 0 |
| Result | ✅ PASS |

---

## Regression Tests (R5 hotspot file — orderTransform.js)

Per QA scope rule: "change touches ANY hotspot file → handover regression + 2 cross-flow tests"

| # | Cross-flow test | Result |
|---|---|---|
| R1 | orderTransformFinancials — tax/total calculations end-to-end | ✅ All pass |
| R2 | cr029.roundUp — round_up persistence via collectBillExisting | ✅ All pass (1 test updated per CR-170) |

---

## Findings

| # | Severity | Description | Resolution |
|---|---|---|---|
| F1 | NOTE | `cr029.roundUp.test.js` had a test asserting old clamp behavior (`negative → 0`) that conflicted with CR-170 | Updated test to expect `-0.3` (CR-170 floor case is now valid) — not a BUG-170 regression |

**No BLOCKER / MAJOR / MINOR findings.**

---

## Coverage

| File changed | ≥1 test covering change? |
|---|---|
| `orderTransform.js` MANUAL PATH | ✅ T1–T5 (code check + math validation + 7 test suites) |

Coverage: 1/1 changed files ✅

---

## Registry Spot-Check

```bash
python3 -c "import json; d=json.load(open('/app/memory/control/registry.json'));
items={i['id']:i for i in d['items']};
print([(i,items[i]['status'],items[i].get('sprint_key')) for i in ['BUG-170','CR-170']])"
```
→ `[('BUG-170', 'IMPLEMENTED', 'pos_5_0'), ('CR-170', 'IMPLEMENTED', 'pos_5_0')]`
**REGISTRY: SYNCED ✅**

---

## Summary

**95/95 PASS. 0 failures. 0 new warnings. Coverage 1/1.**
**BUG-170 fix verified correct — variationPerUnit now included in MANUAL PATH tax base.**
**Ready for Gate 6 (Owner Smoke).**

Owner smoke test:
- Open any order with a variation item (e.g. masala dosa large +₹50) from Dashboard/OrderCard
- Click printer icon (manual reprint)
- Verify CGST/SGST = correct amount based on full price (base + variation)
- Compare with Collect Bill screen — both should now match
