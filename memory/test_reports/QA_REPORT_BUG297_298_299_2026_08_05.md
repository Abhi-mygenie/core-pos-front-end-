# QA Report — BUG-297 / BUG-298 / BUG-299
**Date:** 2026-08-05  
**Role:** QA AGENT  
**Items:** BUG-297 (Category Printer), BUG-298 (Dine-in Comp), BUG-299 (QSR Comp)  
**Compile:** PASS (1 pre-existing warning, 0 new)

---

## ⚠ HANDOVER NOTE

QA Handover test cases T4-T12 were written before the **Option 1 refactor** (comp moved from CartPanel Gift button → CollectPaymentPanel checkbox). QA executed against **current code state** with updated test cases.

---

## Findings

| # | Finding | Severity | Evidence |
|---|---|---|---|
| F1 | **Duplicate MarkCompModal render in OrderEntry.jsx** — both fire when `compItem !== null` → two stacked modals appear | **BLOCKER** | L2686 + L2693 in OrderEntry.jsx: identical `{compItem && <MarkCompModal .../>}` blocks |

---

## Test Execution — Full Results

### BUG-297 — Category Form Printer Fix

| # | Test Case | Method | Expected | Result |
|---|---|---|---|---|
| T1 | `handleAdd()` sends `printerId` | Code trace: CategoryList.jsx L58 | `addCategory({ ..., printerId: stationOptions.find(...)?.printerId || '' })` | ✅ **PASS** |
| T2 | `handleSaveEdit()` sends `printerId` | Code trace: CategoryList.jsx L84 | `editCategory(id, { ..., printerId: ... })` | ✅ **PASS** |
| T3 | Printer name badge shown in Add form | Code trace: L29-36 + L174-178 | `getPrinterLabel(formStation)` → green badge or yellow warning | ✅ **PASS** |
| T3b | Printer name badge in Edit form | Code trace: L174 area | Same `getPrinterLabel` in edit form JSX | ✅ **PASS** |

### BUG-298 / BUG-299 — Collect Bill Complementary (Option 1 flow)

| # | Test Case | Method | Expected | Result |
|---|---|---|---|---|
| T4 | Gift button **absent** from CartPanel (reverted) | Code trace: grep Gift,setCompItem in CartPanel.jsx | 0 results | ✅ **PASS** |
| T5 | Collect Bill checkbox qty=1 → direct toggle | Code trace: CPP L2224-2230 | `(item.qty||1) > 1 = false` → `onToggleComplimentary(item.id)` | ✅ **PASS** |
| T6 | Collect Bill checkbox qty>1 → opens MarkCompModal | Code trace: CPP L2224 + OE L2687 | `(item.qty||1) > 1 && onSetCompItem` → `onSetCompItem(item)` → MarkCompModal renders | ✅ **PASS** — ⚠ see F1 (duplicate render) |
| T7 | Partial comp billing (compQty=2, qty=3) | Code trace: CPP `getItemLinePrice` | `chargedQty = qty - compQty = 1`, `base = price × 1` | ✅ **PASS** |
| T7b | `(2 of 3 Comp)` label shown | Code trace: CPP L2249-2251 | `isPartial && ({item.compQty} of {item.qty} Comp)` | ✅ **PASS** |
| T8 | Full comp (compQty=3, qty=3) → excluded from billableItems | Code trace: `isLineComplimentary` L143 | `compQty >= qty = true` → excluded | ✅ **PASS** |
| T9 | Undo: compQty=0 → comp removed | Code trace: `handleMarkComp` L807 | `compQty=0 → isComplementaryRuntime=false, compQty=0` → `isPartial=false` | ✅ **PASS** |
| T10 | Catalog-comp items locked | Code trace: CPP L1855 `isCatalogLocked` | `disabled={isCatalogLocked}` | ✅ **PASS** |
| T11 | QSR: no Gift button (same CartPanel) | Code trace: CartPanel grep | CartPanel Gift button absent | ✅ **PASS** |
| T12 | QSR collect bill partial comp → 2-line payload | Code trace: orderTransform L1471 `expandCompItems` on `placedSourceItems` | Splits partial-comp into 2 lines at bill time | ✅ **PASS** |

### Regression Tests

| # | Test Case | Expected | Result |
|---|---|---|---|
| T13 | Normal order (no comp) — `getItemLinePrice` unchanged | `cq=0 → chargedQty=qty` → full price | ✅ **PASS** |
| T14 | CPP existing checkbox still works (qty=1) | `onToggleComplimentary` called for qty=1 items | ✅ **PASS** |
| T15 | Cancel button still works in CartPanel | `setCancelItem` intact, CancelFoodModal unchanged | ✅ **PASS** |
| T16 | Registry spot-check | All 3 IDs: IMPLEMENTED, pos_5_1 | ✅ **PASS** (confirmed) |

---

## Summary

```
Total tests: 17
Pass: 16
Fail: 1 (BLOCKER)
Coverage: 5/5 changed files tested ≥1 test each
Registry: SYNCED — BUG-297/298/299 IMPLEMENTED, pos_5_1
```

---

## BLOCKER Detail — F1

**ID:** F1  
**Severity:** BLOCKER  
**Test:** T6 — Collect Bill checkbox opens MarkCompModal  
**Symptom:** When cashier clicks the comp checkbox, **two MarkCompModal overlays stack on screen** (duplicate render). The first `onClose` closes the top modal but the second is still visible.  
**Root cause:** `CODE_ERROR` — Two identical `{compItem && <MarkCompModal .../>}` blocks in `OrderEntry.jsx` at L2686 and L2693. Added during successive implementation edits without detecting the existing block.  
**Files:** `components/order-entry/OrderEntry.jsx` (L2693-2699)  
**Fix:** Delete the second `{compItem && <MarkCompModal />}` block (7 lines, L2693-2699)  
**Scope:** 1 file, 1 edit, 7 lines deleted — no other changes required.

---

## Handover to Bug Fix Agent

```
QA complete. 16/17 passed. 1 BLOCKER (F1).
Failures: F1 — duplicate MarkCompModal in OrderEntry.jsx L2693-2699 (delete second block).
QA report: test_reports/QA_REPORT_BUG297_298_299_2026_08_05.md
Needs Bug Fix agent for F1 before Owner Smoke.
```
