# QA REPORT — CR-077 / INV-SOCKET-001 — B2 Final Gate — 2026-07-20

**Role:** QA (validate-only; zero app code changed) | **Scope:** backend B2 deployment (all socket emit paths room-scoped) + regression of the full fix chain
**Verified by:** testing_agent (authoritative, /app/test_reports/iteration_7.json) + main-agent boot probe (b2_qa_probe_result.txt)
**Environments:** socket server presocket.mygenie.online (external), fixed FE branch https://core-pos-preview-10.preview.emergentagent.com, login API preprod.mygenie.online

## Precondition Check
- CR-077 FE code implemented on EXTERNAL branch (core-pos-preview-10), not this repo — registry here still shows "PLANNED — GATE 3" for the FE edits. Noted as REGISTRY DRIFT (NOTE severity — implementation happened outside this environment; reconcile when branch merges).
- Backend B1 + B2 are external deployments, tracked via INV-SOCKET-001/backend brief. Environment responsive (login 200, socket 200).

## Test Results — 5/5 PASS, 0 failures
| # | Test | Expected | Actual | Verdict | Evidence |
|---|---|---|---|---|---|
| QA-1 | Outsider (no join) during order trigger + login (fires login_disabled_644, pre-B2 this LEAKED) | 0 events | 0 | PASS | b2_qa_probe_result.txt, iteration_7 |
| QA-2 | Joined rest_644 gets scoped delivery | new_order_644 + login_disabled_644 | both received | PASS | same |
| QA-3 | Cross-tenant: joined rest_777001 during rid-644 triggers | 0 | 0 | PASS | same |
| QA-4 | FE E2E wiretap (rid 644 on fixed branch), single trigger | exactly 1 new_order_644, 0 foreign | 1 / 0; envelope ['new-order',777123,644,0,{...}] = MSG_INDEX | PASS | QA4_QA5_wiretap_result.json |
| QA-5 | Passive soak 236s during live preprod traffic | 0 foreign events (pre-fix baseline: 6 per 4 min) | 0 | PASS | same |

Prior gates already PASSED (context): /order-update room emit (iteration_5 era probes), B1 join handler 100% (iteration_6), FE branch E2E incl. reconnect re-join (FE_FIX_E2E_VERIFICATION_2026-07-20.md).

## Findings
- BLOCKER: none. MAJOR: none. MINOR: none.
- NOTE-1: double `join_restaurant` emit on reconnect (connect handler + effect re-run) — idempotent, harmless; optional polish on FE branch.
- NOTE-2: registry drift (see precondition) — reconcile CR-077 status on merge.
- NOTE-3: join remains unauthenticated (any client can join any room) — known, deferred to Phase-2/B3 token validation (owner decision D2).

## Coverage
All changed surfaces exercised: /order-update path (QA-2/4), non-order emit paths via login_disabled (QA-1/2), room membership (QA-2/3), FE join+delivery+isolation (QA-4/5). Reconnect covered in prior session (check #5 of FE E2E).

## Verdict
**PASS — the original P0 (global broadcast → peak-hour outage cause) and the tenant privacy leak are BOTH closed, verified end-to-end.**
Baseline comparison: foreign events per 4-min window: 6 → 0. Outsider reception: everything → nothing. Own-event delivery: 0 (during regression) → exactly 1.

## Next
1. Owner smoke on core-pos-preview-10 (place a real order, watch it appear on a second logged-in device).
2. Merge FE branch → reconcile registry (CR-077 → IMPLEMENTED/QA PASS) → owner verified → CLOSED.
3. Register Phase-2/B3 (token-validated join) + parked hardening (FE-1 connect race, FE-2 jitter/backoff, FE-3 fetch dedupe).
4. Monitor next peak window; if hangs persist, remaining suspects are the non-socket findings (F2 GET-back herds, F3 poll sync, F5 origin flaps) — separate items.
