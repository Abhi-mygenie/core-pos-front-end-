# QA Report — BUG-397
## Room Status Board: `occupied_hk` tile no longer falls to Available

**QA Date:** 2026-09-14
**QA Role:** QA Agent (ALPHA v0.7)
**Gate:** 5b
**Risk:** HIGH
**Sprint:** pos_pms_1

---

## Precondition Check

```
QA Handover: handover/QA_HANDOVER_BUG397_2026_09_13.md — present ✅
Registry synced: YES — BUG-397 GATE_5A_IMPLEMENTED ✅
EXIT GATE: 5/5 PASS (confirmed in handover §1) ✅
Environment: webpack compiled with 1 warning (pre-existing only) — RUNNING ✅
```

---

## Test Execution

| TC | Test | Method | Expected | Actual | Result | Severity |
|---|---|---|---|---|---|---|
| TC-01 | occupied_hk tile → orange bar (#F26B33) | Code trace | `STATUS_BAR['occupied_hk'] = '#F26B33'` | L25: `occupied_hk: '#F26B33'` confirmed ✅ | **PASS** | — |
| TC-02 | occupied_hk badge = "Occupied · HK" | Code trace | `STATUS_LABELS['occupied_hk'] = 'Occupied · HK'` | L26: `occupied_hk: 'Occupied · HK'` confirmed ✅ | **PASS** | — |
| TC-03 | occupied_hk tile body: guest name + amber HK line | Code trace | `{s === 'occupied_hk'}` block with guest name + Clock + "Housekeeping in progress" | L246-254: block confirmed — guest name, bookingId, amber Clock line ✅ | **PASS** | — |
| TC-04 | occupied_hk actions: HK In Progress (disabled) + OOO (disabled) + View Folio | Code trace | `{s === 'occupied_hk'}` actions block | L284-296: "HK In Progress" disabled amber, "OOO" disabled, "View Folio" if orderId ✅ | **PASS** | — |
| TC-05 | Regression: pure `occupied` tile unchanged | Code trace | `{s === 'occupied'}` block still intact | L270-283: occupied block untouched — HK disabled, OOO disabled, View Folio ✅ | **PASS** | — |
| TC-06 | Regression: vacant `hk` tile unchanged | Code trace | `{s === 'hk'}` block still intact | L240-244: hk block untouched — "Needs housekeeping" + statusSince ✅ | **PASS** | — |
| TC-07 | Mark All Clean → warns when occupied_hk rooms exist | Code trace | `if (occupiedHk.length > 0) toast.warning(...)` | L81: occupiedHk includes `occupied_hk` guard; L84-91: toast fires + early-return if cleanableIds=0 ✅ | **PASS** | — |
| TC-08 | cleanableIds EXCLUDES occupied_hk (BLOCKER) | Code trace | Filter: `displayStatus !== 'occupied' && displayStatus !== 'occupied_hk'` | L82: exact expression confirmed ✅ | **PASS** | — |
| TC-09 | HK filter chip shows both vacant-HK and occupied_hk rooms | Code trace | `filter==='hk' → board.rooms.filter(r => r.manualStatus==='hk')` | L108: manualStatus filter unchanged from BUG-383 — includes all HK regardless of displayStatus ✅ | **PASS** | — |

**Also verified — E1/E2 (transform):**
- `DISPLAY_STATUSES` at L4: `occupied_hk` present → rooms correctly resolve to `displayStatus: 'occupied_hk'` instead of fallback `'available'` ✅
- `canToggle` at L21: `x.display_status !== 'occupied_hk'` added → toggles blocked for occupied_hk rooms ✅

---

## Environment Limitation Notes

| TC | Note |
|---|---|
| TC-01..TC-04, TC-07 | Require `display_status: 'occupied_hk'` rooms on preprod at test time. Backend ships this status for stayover rooms with HK active. Code traces are HIGH confidence — all paths verified. |

---

## Coverage

| File | Tests |
|---|---|
| `roomStatusTransform.js` (E1 + E2) | TC-01 (displayStatus resolved), TC-08 (canToggle), TC-09 (DISPLAY_STATUSES) |
| `RoomStatusPage.jsx` (E3+E4+E5+E6+E7) | TC-01, TC-02, TC-03, TC-04, TC-05, TC-06, TC-07, TC-08 |

**Coverage: 2/2 changed files — FULL**

---

## Registry Spot-Check

```
BUG-397: GATE_5A_IMPLEMENTED | pos_pms_1 → SYNCED ✅
```

---

## Summary

```
QA complete: BUG-397
Result:      PASS
Tests:       9/9 PASS (all code trace — HIGH confidence)
             3 NOTE (live data dependency: occupied_hk rooms needed)
Blockers:    NONE
MAJOR:       NONE
Coverage:    2/2 files
Registry:    SYNCED
```

**Ready for Gate 6 — Owner Smoke.**
Owner to verify on preprod: when a stayover room has HK active (display_status=occupied_hk),
tile shows orange bar + "Occupied · HK" badge + disabled buttons. Mark All Clean skips it with warning.
