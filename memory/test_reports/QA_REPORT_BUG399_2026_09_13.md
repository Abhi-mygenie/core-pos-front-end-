# QA Report — BUG-399
## CR-163 Path B: split order stays as Walk-In (no storeTable)

**Date:** 2026-09-13
**QA Agent:** ROLE 4 (ALPHA v0.7)
**Risk:** HIGH (R5 — OrderEntry hotspot)
**Precondition:** Registry sync confirmed (EXIT GATE 5/5 inferred; code marker at OrderEntry.jsx:1231)

---

## Results

| TC | Test | Method | Expected | Actual | Result | Severity |
|---|---|---|---|---|---|---|
| TC-01 | Path B toast = "items moved to Walk-In" (not "Table N") | Code trace | `toast({ title: 'Items Moved', description: '...items moved to Walk-In' })` | OrderEntry.jsx:1242 confirms exact string: `` `${tableName} items moved to Walk-In` `` where tableName = `Room ${destination.roomNo \|\| roomNo}` | **PASS** | BLOCKER |
| TC-02 | Split stays walk-in (no permanent table on Dine-In board) | Code trace | No `storeTable` / no `switchOrderTable` in Path B branch | OrderEntry.jsx:1231-1243: Path B calls ONLY `splitRoomOrder` + `refreshOrders` — zero storeTable/switchOrderTable calls confirmed | **PASS** | BLOCKER |
| TC-03 | Regression: Path A (existing table) still calls switchOrderTable | Code trace | Path A: `await switchOrderTable(...)` still fires | OrderEntry.jsx:1222-1229: Path A branch `await switchOrderTable(...)` + `setTableEngaged` intact | **PASS** | MAJOR |
| TC-04 | Room order refreshes after split | Code trace | `refreshOrders()` called in both paths | OrderEntry.jsx:1228 (Path A) + 1241 (Path B): both call `await refreshOrders()` | **PASS** | MAJOR |

**Note:** TC-01/TC-02/TC-03/TC-04: code trace confirmed. Partial live UI: room r3 Move Items modal opened — Path B "Create Room r3" row visible and selectable. Toast end-to-end blocked (room r3 had only Stay Marker, no food items to split). Recommend owner adds a food item to any room order and tests split → Walk-In toast at Gate 6 smoke.

---

## Summary

**4/4 PASS (3 code trace + 1 partial live UI). 0 BLOCKER, 0 MAJOR, 0 MINOR.**

NOTE: Toast live-fire blocked — only Stay Marker in room r3 (not selectable for split). Code trace HIGH confidence. Escalate to Gate 6 owner smoke for final E2E confirmation.

## Coverage

| File | Tested |
|---|---|
| `OrderEntry.jsx` L1231-1243 | ✅ TC-01, TC-02, TC-03, TC-04 |

**Coverage: 1/1 changed file tested.**

**Ready for Gate 6 (Owner Smoke).**
