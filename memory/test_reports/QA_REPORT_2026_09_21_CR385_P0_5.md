# QA REPORT — CR-385 Phase 0.5 (bug-fix re-test) — 2026-09-21

```
Role:       QA (Role 4) after Bug Fix (Role 5) · ALPHA v0.7 · owner "Phase 0.5 GO" (severities confirmed as listed)
Scope:      BUG-434/435/436/437/438 fixes + full Phase 0 matrix (QA handover §1–§3) + newly automated cases (keyboard, phone-suffix, Turns, focus-refresh)
Sandbox:    OWNER_TGK · RID 69 sandbox-pms · preprod (DEGRADED during the run: login 11 s, LR 3–8 s, one 60 s axios timeout at boot)
Runs:       Run A test_reports/iteration_7.json (5 fixes, 1920×800) · Run B iteration_8.json (P0 matrix R1–R15, 1366×768 + 1920×800 screenshots) · main-agent network probe (screenshot_tool, timings from page.on('response'))
Result:     PASS — 0 BLOCKER · 0 MAJOR · **1 MINOR (BUG-439, round 2, owner decides)** · 3 NOTE. Console errors (non-ignored): 0 in all three runs.
Round 2:    /app/test_reports/iteration_9.json (owner-requested independent re-run of the round-1 FAIL/BLOCKED cases, preprod healthy: batch 1.2 s) — RT-1/RT-2/RT-3/RT-4 4/4 PASS with request timelines; regression subset 5/6.
Unit:       CI=true yarn test --testPathPattern=cr385 → 38/38 (was 20/20) · grep guards X-01/X-06 empty · "Channel Manager" in frontdesk/ + page = 0 · yarn build exit 0 · hotspots untouched (git status)
Sandbox restore: r4/8525 Mark Clean → Available → Request HK → HK verified (it.8 final_restore_check). 8524/8526 never touched. No bookings/payments.
```

## 1 · Fix verification (Run A + probe)

| Bug | Case | Result | Evidence |
|---|---|---|---|
| BUG-437 | Arrivals initial chip = first non-empty (Late 10 / Today 0 / Tomorrow 0 / Upcoming 4) | PASS | it.7 Late aria-selected, no `fd-table-arrivals-empty`, rows visible |
| BUG-437 | Departures → Today 2 · In-House `all` · Rooms `all` | PASS | it.7 |
| BUG-437 | Click Today 0 → "No today arrivals"; ↻ → still Today (pin); reload `?tab=arrivals` → Late | PASS | it.7 |
| BUG-434 | `fd-retry-btn` disabled + "Retrying…" ≤200 ms; exactly 1 LR request per click; re-enables | PASS | it.7 |
| BUG-434 | Recovery after unroute | PASS (probe) | it.7 FAIL was a preprod 60 s timeout ("Request timed out") — probe: recovery in **1.8 s**, 1 LR request; `repro_435C_434.jpeg` |
| BUG-434 | `fd-rooms-retry-btn` disabled + "Retrying…" then Retry; tiles back after unroute | PASS | it.8 R8 (BLOCKED in it.7 only by the timeout above) |
| BUG-435 | A: 2 focus events < 5 s after last fetch → 0 requests | PASS | it.7 |
| BUG-435 | B: two ↻ clicks 50 ms apart → 1 batch (coalesced) | PASS | it.7 |
| BUG-435 | C: focus after idle → 1 batch | PASS (probe) | it.7 measured idle from the click while LR took ~7 s → still in flight → coalesced (D71 by design). Probe measured from the **last response** ≥7 s → **1 request**; then focus <5 s → 0 |
| BUG-435 | D: manual ↻ right after → 1 request (never debounced) | PASS | it.7 |
| BUG-436 | `fd-workstation-body` is `<main>` inside `fd-page`; innerText has no "Channel Manager" on 4 tabs | PASS | it.7 |
| BUG-438 | Live keyboard ↑↓ / Enter (1 `.fd-expansion`) / Esc | PASS | it.7 (fd-row-15 ↔ fd-row-17) |
| BUG-438 | `/` focus → type masked-phone suffix → `fd-search-result-inhouse-0` → In-House row | PASS | it.7 (••••1345) |
| BUG-438 | Turns chip 0 == tiles with TURN TODAY 0 → `fd-rooms-empty`; All restores | PASS | it.7 |

## 2 · Phase 0 regression matrix (Run B, 1366×768) — 15/15 PASS

R1 route/nav · R2 tiles === network (0/2/2, 0 free) · R3 tabs + `?tab=rooms` + localStorage · R4 one expansion · R5 sticky th (Δ1 px, padding-top 0) · R6 group-by Area "First Floor" persisted · R7 RoomDetail 8525 Mark Clean → Available → Request HK (restored) · R8 Rooms retry · R9 alert bar 3 + "+9 more" popover · R10 header greeting/date/sync pill/New Booking disabled · R11 money en-IN · R12 zero duplicate testids (4 tabs + detail + popover) · R13 scrollWidth == 1366 on all tabs · R14 legacy pages render, 0 errors · R15 screenshots `evidence/CR-385/qa_2026_09_21_p0_5/runB_{1366,1920}_*.jpg`.

## 3 · Automated coverage added (BUG-438)

| File | Tests |
|---|---|
| `components/pms/frontdesk/__tests__/GuestTable.cr385.test.jsx` | ↑↓ move + clamp · Enter toggles · Esc closes (onToggle(null)) · Esc no-op · one expansion rendered |
| `components/pms/frontdesk/__tests__/GlobalSearch.cr385.test.jsx` | phone suffix 5723 → in-house 155 (Room r3) · `r4` → rooms 8525 · name → arriving chip · empty/whitespace/null → [] · `/` focus, click navigates + clears, Esc clears |
| `components/pms/frontdesk/__tests__/phase05.cr385.test.jsx` | RoomsPanel retry state · `firstNonEmptyChip` ×6 · Arrivals fixture → Late · rows [] → Today · explicit chip honoured · Departures overdue/today · hook: focus debounce, coalescing (`p1 === p2`), error → clear |
| `api/transforms/__tests__/frontDeskTransform.cr385.test.js` | `isTurn` on live-shaped snapshot (r4 leaving + arriving on BD → turn; fixture → 0 turns) |

## 3b · Re-test round 2 (ALPHA Role 4 re-test protocol) — `/app/test_reports/iteration_9.json`

"Re-test round 2: 4/4 previously FAIL/BLOCKED cases pass (was 1/4 in round 1)."

| Case | Round 1 | Round 2 | Evidence (timeline) |
|---|---|---|---|
| RT-1 BUG-435 case C — focus after ≥7 s idle measured from the last batch response | FAIL (idle measured from click, batch ~7 s) | **PASS** | click 15.21 s → batch complete 16.42 s → idle 7.0 s → focus 23.42 s → +1 LR/+1 board/+1 kpis; second focus <5 s → +0 |
| RT-2 BUG-434 recovery after unroute | FAIL (60 s preprod timeout) | **PASS** | in-flight: disabled + aria-busy + "Retrying…"; recovery click 30.58 s → LR 200 at 31.73 s → strip back in 3.56 s, exactly 1 LR request |
| RT-3 BUG-434 Rooms retry path | BLOCKED | **PASS** | error + retry in-flight state; tiles back 2.04 s after unroute |
| RT-4 BUG-435 case B coalescing | PASS | **PASS** | two clicks 50 ms apart → 1 LR request |
| REG-1 chips / tiles / one expansion / no CM wording / console | PASS | **PASS** | Late 10 active; 0/2/2; 1 `.fd-expansion`; 0 errors |
| REG-1 no duplicate testids **with a row expanded** | (not exercised in P0/round 1) | **FAIL — MINOR → BUG-439** | `fd-row-17-checkin-btn`, `fd-row-17-kebab` ×2 (row cell + expansion drawer); Rooms + detail 0 |

Registry spot-check (BUG-434, BUG-435, CR-385): `QA-VERIFIED (P0.5)` / `GATE_5B_QA_PASSED (P0+P0.5)` / sprint `pos_pms_2` → PASS, no drift. Coverage: 4/4 changed source files exercised (page, RoomsPanel, ArrivalsPanel, DeparturesPanel).

## 4 · NOTEs (no bug intake needed)

- **N-1 (QA protocol)** "Idle" for the D71 focus debounce must be measured from the **last snapshot response**, not from the ↻ click — on slow preprod the batch itself can exceed 5 s. Added to the brief + handover.
- **N-2 (environment)** Preprod degraded during the run (login 11.6 s, LR 6.9 s, one 60 s `ECONNABORTED` at boot). Retry recovery re-verified at 1.8 s once stable. Not a code path issue.
- **N-3 (brief drift)** Brief referenced `fd-tile-*`, `fd-greeting`, `fd-business-date`; actual testids `fd-tab-<id>-count`, `fd-header-greeting`, `fd-header-date`. Code unchanged; brief corrected in the handover.

## 5 · Exit

EXIT GATE 5/5 · registry `GATE_5B_QA_PASSED (P0+P0.5)` · BUG-434…438 `QA-VERIFIED (P0.5)` (independently re-confirmed round 2) · **BUG-439 MINOR INTAKE — owner to decide fix-in-P0.5 vs ship** · Gate 6 owner smoke after that decision (`handover/SESSION_HANDOVER_2026_09_21_CR385_P0_5.md` §6).
