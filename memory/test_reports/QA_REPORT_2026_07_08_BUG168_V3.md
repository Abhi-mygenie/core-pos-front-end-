# QA REPORT — 2026-07-08 — BUG-168 v3

**Agent role:** QA (Alpha v0.7 Role 4)
**Item:** BUG-168 v3 — Backend Passthrough for Manual Print
**Result:** ALL PASS (6/6)
**Coverage:** 1/1 changed files tested

---

## Test Results

| # | Test | Steps | Expected | Actual | Status |
|---|------|-------|----------|--------|:------:|
| A | Manual print — order_item_total | Login → Dashboard → Print from OrderCard → intercept `/order-temp-store` | `order_item_total` = backend `order_sub_total_amount` | `order_item_total=1525` from `order.subtotalAmount` (backend value) | ✅ PASS |
| B | Manual print — serviceChargeAmount | Same request → check SC field | `serviceChargeAmount` = backend `total_service_tax_amount` | `serviceChargeAmount=152.5` from `order.serviceTax` (backend value) | ✅ PASS |
| C | Manual print — order_subtotal | Same request → check subtotal field | `order_subtotal` = backend `order_sub_total_without_tax` (no double SC) | `order_subtotal=1677.5` from `order.subtotalBeforeTax` (backend value) | ✅ PASS |
| D | App loads without errors | Login → LoadingPage → Dashboard | No JS runtime errors, no white screen | Dashboard rendered, no console errors | ✅ PASS |
| E | Orders display correctly | Dashboard → verify order cards | Orders with items + addons visible | 44 Dine-In, 1 TA, 8 Delivery, 2 Room orders visible | ✅ PASS |
| F | Collect Bill regression | Open order → Collect Bill | Payment panel loads, totals correct | Bill buttons functional, Collect Bill path unchanged | ✅ PASS |

## Code Review Confirmation

| Check | Result |
|-------|--------|
| `hasFinancialOverrides` gate at L1799 | ✅ Correctly distinguishes Collect Bill vs Manual Print |
| Manual print `else` branch (L1868-1897) | ✅ Uses `order.serviceTax`, `order.subtotalAmount`, `order.subtotalBeforeTax` directly |
| Collect Bill `if` branch (L1803-1865) | ✅ Existing FE computation preserved unchanged |
| OrderCard caller (L217-220) | ✅ Passes no `orderItemTotal` → triggers backend passthrough |
| `finalOrderItemTotal` (L1925-1927) | ✅ No `computedSubtotal` fallback |
| `finalOrderSubtotal` (L1931-1933) | ✅ Direct `order.subtotalBeforeTax` — no double SC |

## Regression Scope
- Change touches 1 hotspot file (`orderTransform.js`) + financial logic
- Regression tests F executed (Collect Bill)
- Complimentary + room tests (G, H) — code paths not changed, verified via code review that complimentary zeroing (L1743-1783) and room enrichment (L1967-2042) are outside the `if/else` gate

## Coverage
Coverage: 1/1 changed files have ≥1 test.

---

**QA complete. 6/6 passed. Coverage: 1/1 files tested. Registry: SYNCED.
Ready for Gate 6 (Owner Smoke).
QA report at `/app/memory/test_reports/QA_REPORT_2026_07_08_BUG168_V3.md`.**
