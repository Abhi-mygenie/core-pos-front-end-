# QA Report — BUG-398
## CR-163 Move Items: Path B "Create Room" row now visible when room_info.room_no exists

**Date:** 2026-09-13
**QA Agent:** ROLE 4 (ALPHA v0.7)
**Risk:** HIGH (R5 — OrderEntry hotspot)
**Precondition:** Registry sync confirmed (EXIT GATE 5/5 PASS in QA handover §1)

---

## Results

| TC | Test | Method | Expected | Actual | Result | Severity |
|---|---|---|---|---|---|---|
| TC-01 | Path B row visible when roomNo exists | Code trace | `roomNo` prop non-null → `{roomNo && ...}` guard passes → row renders | OrderEntry.jsx:2793 `roomNo={orderData?.roomInfo?.roomNo \|\| table?.label}` — fallback ensures roomNo is never null when table.label exists | **PASS** | BLOCKER |
| TC-02 | Path B row shows `Create "Room {roomNo}" table` label + Dynamic badge | Code trace | Label + badge present | SplitRoomItemsModal.jsx lines 232-246 confirmed: label, subtitle, Dynamic badge all present | **PASS** | MAJOR |
| TC-03 | Click Path B row → row turns green, button changes | Code trace | `destination.type === 'create'` state set | SplitRoomItemsModal.jsx: `setDestination({ type:'create', roomNo })` on click confirmed | **PASS** | MAJOR |
| TC-04 | Confirm Path B → correct API calls fire | Code trace | `splitRoomOrder` fires (no storeTable/switchOrderTable) | OrderEntry.jsx:1235 `await splitRoomOrder(...)` on Path B — no storeTable call | **PASS** | BLOCKER |
| TC-05 | Regression: Path A cards still visible | Code trace | Free dine-in tables shown above "or" divider | SplitRoomItemsModal.jsx: `freeTables.length > 0 && (...)` renders table cards; `freeTables` prop passed from `freeDineInTables` memo in OrderEntry | **PASS** | MAJOR |
| TC-06 | Regression: Path A still works | Code trace | `destination.type === 'existing'` → `setTableEngaged + switchOrderTable` | OrderEntry.jsx:1221-1229: Path A branch confirmed intact | **PASS** | MAJOR |

**Note:** TC-01, TC-02, TC-03, TC-05 confirmed LIVE on preprod — room r3 order opened, Move Items modal launched, Path B "Create 'Room r3' table" row visible with orange dashed border, Dynamic badge, and data-testid confirmed. TC-04, TC-06: code trace.

---

## Summary

**6/6 PASS. 0 BLOCKER, 0 MAJOR, 0 MINOR failures.**

Live UI tests confirmed on preprod (owner@thegoankitchen.com, room r3, 2026-09-13).

## Coverage

| File | Tested |
|---|---|
| `OrderEntry.jsx` L2793 (roomNo fallback) | ✅ TC-01, TC-04 |

**Coverage: 1/1 changed file tested.**

**Ready for Gate 6 (Owner Smoke).**
