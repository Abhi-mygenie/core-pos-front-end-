# CR-029 — Intake Document (Gate 0 + Gate 1)

**Status:** REGISTERED · **Priority:** P1 · **Sprint:** POS 4.0 · **Date:** 2026-06-11
**Source:** Insights audits — cafe103 (A10, latent) + Palm House (A1, **LIVE**)
**Type:** CR

---

## 1. Requirement
Make room-order scope consistent across the Insights module: one shared room predicate + an explicit per-screen "incl. rooms / excl. rooms" badge (or a toggle).

## 2. Current State (verified live at Palm House)
- **Exclude rooms:** Sales, Payments, Order Ledger (`isRoomOrderForReport`: `orderIn RM/SRM || paymentMethod==='ROOM'` — duplicated verbatim in 2+ files)
- **Include rooms:** Dashboard, Items & Menu (no room filter at all)

| Month | Room revenue (paid) | = Dashboard − Sales gap |
|---|---|---|
| Mar | ₹1,78,172 | identical |
| Apr | ₹1,43,603 | identical |
| May | ₹1,01,081 | identical |

Payment-mix distortion: Mar Card = ₹4,28,630 (Sales) vs ₹5,68,852 (Dashboard).
Edge: 4 June orders have `payment_method='ROOM'` but `order_in=null` — only the ROOM-string check catches them.
Related: `transferToRoom` rows pass room-exclusion (in Sales revenue, excluded from Ledger Paid) — same class of split.

## 3. Expected Behavior
Owner picks one of:
a. Rooms excluded everywhere in restaurant Insights (Rooms get their own screen — already exists as Room Orders Report), with badge "Excl. rooms"
b. Per-screen toggle incl./excl., default consistent, always labelled

## 4. Scope
- Files: `insightsService.js` (both aggregators), `orderLedgerService.js`, `AllOrdersReportPage.jsx`, screen headers of Sales/Payments/Dashboard/Items/Cancellations
- Extract shared predicate into one module (e.g., `utils/orderPredicates.js`)

## 5. Evidence
`INSIGHTS_REPORTS_AUDIT_PALMHOUSE.md` §A1 · `/app/audit_data/analyze.py` (`dashboard.room_rev`)
