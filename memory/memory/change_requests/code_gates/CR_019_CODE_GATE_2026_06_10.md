# CR-019 — Code Gate (Gate 4) — MANDATORY SCOPE LOCK

**Status:** AWAITING OWNER GO
**Date:** 2026-06-10
**CR:** CR-019 — Restaurant Settings Self-Onboarding Wizard
**Prerequisite:** Gate 3 (Implementation Plan) — PASSED

---

## Scope Lock

### ✅ FILES TO CREATE (3 new files)

---

#### 1. `frontend/src/api/services/restaurantSettingsService.js` (NEW — ~50 lines)

```js
// Restaurant Settings Service — CR-019
// Self-onboarding wizard API calls

import api from '../axios';
import { API_ENDPOINTS } from '../constants';
import { fromAPI, toAPI } from '../transforms/restaurantSettingsTransform';

/**
 * Fetch current restaurant settings
 * @returns {Promise<Object>} - Transformed per-step form data
 */
export const getSettings = async () => {
  const response = await api.get(API_ENDPOINTS.RESTAURANT_SETTINGS_LIST);
  return fromAPI.settingsResponse(response.data.data);
};

/**
 * Update restaurant settings
 * @param {Object} formState - All steps merged { step1, step2, ..., step6 }
 * @param {File|null} logoFile - Logo image file
 * @param {File|null} pdfFile - PDF menu file
 * @returns {Promise<Object>} - API response
 */
export const updateSettings = async (formState, logoFile = null, pdfFile = null) => {
  const payload = toAPI.settingsPayload(formState);
  const formData = new FormData();
  formData.append('data', JSON.stringify(payload));
  if (logoFile) formData.append('logo', logoFile);
  if (pdfFile) formData.append('pdf', pdfFile);

  const response = await api.post(API_ENDPOINTS.RESTAURANT_SETTINGS_UPDATE, formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
  });
  return response.data;
};
```

---

#### 2. `frontend/src/api/transforms/restaurantSettingsTransform.js` (NEW — ~200 lines)

Full transform spec as documented in Gate 3 Implementation Plan §Phase 1:
- `toBool(val)` — `"Yes"` → true, `"No"` → false, bool passthrough, `1/0` → bool
- `toYesNo(bool)` — `true` → `"Yes"`, `false` → `"No"`
- `fromAPI.settingsResponse(data)` → `{ step1, step2, step3, step4, step5, step6 }`
- `toAPI.settingsPayload(formState)` → `{ basic, advanced, vendor }`

No diff preview (new file) — full code written per Implementation Plan §Phase 1 transform spec.

---

#### 3. `frontend/src/pages/RestaurantSettingsPage.jsx` (NEW — ~800 lines)

6-step wizard page. No diff preview (new file) — built per Implementation Plan §Phase 2-5:
- Wizard shell: left-rail stepper + main content + bottom action bar
- Steps 1-6 content sections
- Form state management, validation, save, navigation
- File upload for logo + PDF
- Loading/error/saving states

---

### ✅ FILES TO MODIFY (3 existing files — exact diffs below)

---

#### 4. `frontend/src/api/constants.js` — ADD 2 endpoint constants

**Insert at line 84** (after `CANCELLATION_REASONS`, before `// Orders`):

```diff
  // Settings
  CANCELLATION_REASONS: '/api/v1/vendoremployee/cancellation-reasons',
+
+ // CR-019: Restaurant Settings (Self-Onboarding Wizard)
+ RESTAURANT_SETTINGS_LIST: '/api/v2/vendoremployee/restaurant-settings/settings-list',
+ RESTAURANT_SETTINGS_UPDATE: '/api/v2/vendoremployee/restaurant-settings/update-settings',
  
  // Orders (Phase 1 Part B)
  RUNNING_ORDERS: '/api/v1/vendoremployee/pos/employee-orders-list',
```

**Lines changed:** +3 (2 endpoints + 1 comment). **Zero existing lines modified.**

---

#### 5. `frontend/src/App.js` — ADD import + route

**Insert import at line 21** (after `SettlementMockup`, before `Toaster`):

```diff
 import SettlementMockup from "./pages/SettlementMockup";
+import RestaurantSettingsPage from "./pages/RestaurantSettingsPage";
 import { Toaster } from "./components/ui/toaster";
```

**Insert route at line 89** (after `settlement/preview`, before `</Routes>`):

```diff
              {/* CR-015: Settlement Module (panel on dashboard, preview for mockup) */}
              <Route path="/settlement/preview" element={<SettlementMockup />} />
+             {/* CR-019: Restaurant Settings Self-Onboarding Wizard */}
+             <Route path="/restaurant-settings" element={<ProtectedRoute><RestaurantSettingsPage /></ProtectedRoute>} />
            </Routes>
```

**Lines changed:** +3 (1 import + 1 comment + 1 route). **Zero existing lines modified.**

---

#### 6. `frontend/src/components/layout/Sidebar.jsx` — ADD sidebar item + visibility + permission

**6a. Import — line 7** (add `Building2` icon):

```diff
-  LayoutGrid, List, Columns, Rows, LineChart, Banknote
+  LayoutGrid, List, Columns, Rows, LineChart, Banknote, Building2
```

**6b. SIDEBAR_PERMISSIONS — insert at line 25** (after `settings`):

```diff
   settings: 'restaurant_settings',
+  'restaurant-setup': 'restaurant_setup',
 };
```

**6c. sidebarMenuItems — insert at line 100** (after `settlement` entry, before `menu-management`):

```diff
    path: "/settlement",
  },
+ {
+   id: "restaurant-setup",
+   label: "Restaurant Setup",
+   icon: Building2,
+   path: "/restaurant-settings",
+ },
  {
    id: "menu-management",
```

**6d. VISIBLE_SECTIONS — line 221** (add `'restaurant-setup'`):

```diff
- const VISIBLE_SECTIONS = new Set(['dashboard', 'reports', 'insights', 'credit', 'settlement', 'menu-management', 'visibility-settings']);
+ const VISIBLE_SECTIONS = new Set(['dashboard', 'reports', 'insights', 'credit', 'settlement', 'restaurant-setup', 'menu-management', 'visibility-settings']);
```

**Lines changed:** 4 edits across 4 locations. **Zero existing logic modified — all additive.**

---

### 🚫 FILES NOT TO CHANGE (no-touch list)

| File | Reason |
|---|---|
| `components/panels/SettingsPanel.jsx` | Existing slide-over — stays independent |
| `components/panels/settings/ViewEditViews.jsx` | Existing settings views — untouched |
| `components/panels/settings/ListFormViews.jsx` | Existing settings views — untouched |
| `components/panels/settings/TableManagementView.jsx` | Existing settings views — untouched |
| `components/panels/settings/shared.jsx` | Existing settings primitives — untouched |
| `contexts/SettingsContext.jsx` | No state overlap — wizard uses local state |
| `api/services/settingsService.js` | Different API (cancellation reasons) |
| `api/transforms/settingsTransform.js` | Different data shape |
| `contexts/RestaurantContext.jsx` | Read-only consumer |
| `pages/DashboardPage.jsx` | Not related |
| `components/order-entry/OrderEntry.jsx` | Not related |
| `components/order-entry/CartPanel.jsx` | Not related |
| `components/cards/OrderCard.jsx` | Not related |
| Any report page (`*Mockup.jsx`) | Not related |
| `api/axios.js` | No changes needed — existing interceptors handle auth |
| `contexts/index.js` | No new context — wizard uses local state |

---

## Change Summary

| Metric | Count |
|---|---|
| **New files** | 3 |
| **Modified files** | 3 |
| **New lines (est.)** | ~1,050 |
| **Modified lines in existing files** | ~13 (all additive) |
| **Deleted lines** | 0 |
| **Existing logic changed** | 0 |
| **Regression risk** | ZERO |

---

## ⛩️ OWNER DECISION REQUIRED

**This is the Code Gate. No code may be written until owner gives GO.**

> **Please confirm:**
> 1. The 3 new files are approved for creation
> 2. The 3 existing file modifications (exact diffs above) are approved
> 3. The no-touch list is acknowledged
> 4. Implementation may begin with Phase 1

**Reply "GO" to unlock implementation.**

---

*CR-019 Code Gate — 2026-06-10. 3 new + 3 modified files. 0 existing logic changed. Awaiting owner GO.*
