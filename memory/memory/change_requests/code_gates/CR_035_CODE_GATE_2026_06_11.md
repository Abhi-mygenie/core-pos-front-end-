# CR-035 — Report Definitions Help Page · Gate 4 Code Gate
**Date:** 2026-06-11 · **Owner GO:** "yes start and follow gates" (verbatim, after explainer)
**Origin:** follow-up proposed in INSIGHTS_BATCH_HANDOVER §"Report Definitions help page" — now approved.

## Scope (LOCKED)
1. NEW static screen `/reports-module/definitions` (`ReportDefinitionsMockup.jsx`) — no API calls, display-only:
   - Date-basis table per screen (collection / punch / cancel date + settlement "sold" note)
   - The three TAB numbers (Added to Credit · Credit Settled · Credit Outstanding)
   - Business-day rule (06:00 → 03:00, owner-worded)
   - "Why screens differ" worked example + cancellation valuation one-liners (H18/H22/OPS-CANCEL)
2. "ⓘ Definitions" link appended inside the existing basis-label line of 5 screens:
   Dashboard · Sales (S7) · Payments (S8) · Items & Menu (S5) · Cancellations (S9)
   — text-only button, navigate() only, NO logic/number changes anywhere.
3. Route added in App.js (protected, same pattern as sibling reports).

## Explicitly out of scope
No metric/aggregation changes · no API calls · no sidebar entry (reachable via ⓘ links) · no export.

## Freeze-log impact
S5/S7/S8/S9 headers gain one inline link — amendment appended (visual-only, zero data change).

## QA
Screenshot: definitions page renders; ⓘ link from Sales navigates correctly. No console errors.

## Rollback
Delete route + page + 5 one-line link spans.
