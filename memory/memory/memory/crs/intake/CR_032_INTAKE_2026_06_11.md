# CR-032 — Intake Document (Gate 0 + Gate 1)

**Status:** REGISTERED · **Priority:** P2 · **Sprint:** POS 4.0 · **Date:** 2026-06-11
**Source:** Insights audits — cafe103 (A5) + Palm House (A9)
**Type:** CR

---

## 1. Requirement
One shared payment-method classifier module used by Sales, Payments, Dashboard (and Ledger), with explicit handling for every live enum.

## 2. Current State (3 different classifiers, verified)
- `SalesMockup` inline pmKey chain · `PaymentsMockup.classifyPaymentMethod` · `insightsService` Dashboard inline chain — all differ.

Live enums leaking through (Palm House Mar–Jun):
| Enum | Count | Today's handling |
|---|---|---|
| `pending` | 13 (fStatus 2/5, unpaid) | Unknown to ALL classifiers; would surface as raw/"Pending" bucket if paid |
| `partial` | 17 | Sales→"Partial", Dashboard→raw `partial`, Payments→"Partial"; backend splits into real cash/UPI legs — Mar: ₹2,268 (Sales) vs ₹9,858 (Dashboard) |
| `cash_on_delivery` | 6 | Classified "Cash" by substring luck |
| `zomato_gold` | 14 (cafe103) | Raw pass-through |
| Zero-amount paid orders | 6/7/**17**/3 per month | Counted in order counts → AOV skew |

## 3. Expected Behavior
Single `classifyPaymentMethod()` in one module; documented bucket list; unknown enums logged/flagged not silently bucketed; zero-amount paid orders excluded from AOV (owner to confirm).

## 4. Scope
- New: `frontend/src/utils/paymentClassifier.js` (or extend constants.js)
- Consumers: SalesMockup, PaymentsMockup, insightsService (dashboard), AllOrdersReportPage A0a mask
- Backend ask (info only): semantics of `payment_method='pending'`

## 5. Evidence
`INSIGHTS_REPORTS_AUDIT_PALMHOUSE.md` §A9 · `/app/audit_data/analyze.py` (`enums`, `sales.pm` vs `dashboard.pm`)
