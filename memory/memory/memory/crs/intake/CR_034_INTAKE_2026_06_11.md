# CR-034 — Intake Document (Gate 0 + Gate 1)

**Status:** PLANNED · **Priority:** P1 · **Sprint:** POS 4.0 · **Date:** 2026-06-11
**Source:** Owner Q4 ruling (Insights audit batch, decision queue Category H addendum)
**Type:** CR

---

## 1. Requirement (owner-ruled)
Items & Menu gets Order Ledger-style buckets so item analytics match the ledger's money truth:
**Sold (paid) / Added to Credit (TAB) / Cancelled / Pending Billing**

Owner rulings shaping it:
- TAB is cumulative, no order linkage — credit items NEVER move buckets on settlement (Q4 verbatim: "no connection of order id to tab, tab is cumulative")
- Items stays PUNCH-DATED; only the Cancelled bucket uses cancel_at (ITEMS-BASIS verbatim: "this will be punched date, only cancelled will be at cancel, we already filtering with status correct")
- Comp items valued via `complementary_price` (live-verified key) where loss display applies

## 2. Current State
`getItemSalesAggregated` buckets per item: Sold / Cancelled / Complementary / Pending — TAB items sit inside Sold (₹49,460 May Palm House), so Items "Sold" overstates vs Ledger Paid.

## 3. Expected
- Sold excludes TAB items → matches Ledger Paid scope (punch-dated)
- New "Added to Credit" bucket = TAB orders' items, permanent
- Cancelled bucket by cancel_at via shared valuation module (CR-031)
- Header label: "By punch date" (Q-B)

## 4. Scope
`insightsService.js` (bucket logic), `ItemSalesHybridMockup.jsx` (tab UI), shared `cancellationValuation.js` (CR-031)

## 5. Evidence
OWNER_DECISION_QUEUE.md Category H addendum (Q4, ITEMS-BASIS, H22-KEY) · `/app/audit_data/` replicas
