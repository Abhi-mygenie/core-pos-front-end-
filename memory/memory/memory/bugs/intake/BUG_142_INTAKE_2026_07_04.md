# BUG-142 — Intake Document (Gate 0 + Gate 1)

**Status:** REGISTERED · **Priority:** P0 · **Risk:** HIGH · **Sprint:** POS 5.0 · **Date:** 2026-07-04
**Source:** OWNER-REPORTED (batch intake 2026-07-04)
**Type:** Bug — Frontend (Order Entry → Cart / Qty input)
**Duplicate check:** DISTINCT
**Financial flag:** R6 — qty directly drives order total. Regression must include money verification.

---

## 1. Symptom
While typing item quantity in the POS Cart panel with NumLock ON on the keyboard, the entered qty ends up NEGATIVE (e.g., typing `2` yields `-2` or similar). Without NumLock the behavior is normal.

## 2. Suspected Area
- `frontend/src/components/order-entry/CartPanel.jsx` — qty input field / keydown handler.
- Possibly a step/decrement (−/+) button intercepting keypad `-` / `Numpad-` codes when NumLock is on.
- `originalQty` / `displayQty` / delta computation (see CartPanel.jsx L49-50 — BUG-237 comment).

## 3. Blast Radius
1–2 files (Cart input + qty utility). Blast: SMALL. Hotspots: touches `CartPanel` (order-entry hotspot).

## 4. Evidence
- Owner verbal report only. Confidence: REPORTED.
- Screen recording of NumLock ON vs OFF: NOT PROVIDED — high value for Planning; please share if possible.

## 5. Open Questions (for Planning)
1. Does it happen on the input `<input>` directly or only when using the keypad number keys?
2. Does it happen for edit-existing-qty or only new-item-add?
3. OS / browser combo — Windows keypad only, or also Mac Numpad?

## 6. Next
Planning Gate 2 — trace CartPanel keydown handler + qty state; include end-to-end money regression per R6.
