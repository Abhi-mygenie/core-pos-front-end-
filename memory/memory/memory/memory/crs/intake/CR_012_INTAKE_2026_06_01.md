# CR-012 — Intake — 2026_06_01

**Change Request:** Menu Management — migrate to new API contract (scope-limited)
**Sprint:** POS 4.0
**Status:** OPEN — INTAKE
**Priority:** P1
**Area:** Settings
**Created:** 2026_06_01 (via conversational intake)

---

## 1. Requirement (owner-reported)

The current Menu Management module (admin-side CRUD over categories, items, modifiers, variations, addons, pricing, availability — everything the admin edits today) is wired to one set of admin APIs. Going forward, POS Menu Management will use a different set of APIs: different endpoints and different request/response structure. Scope is strictly limited to the Menu Management module's API layer. The rest of POS (Order Entry, Cart, Collect Bill, Print, Reports, etc.) continues to use whatever menu data flow it uses today — no changes. The in-app menu shape that downstream POS consumers read must remain byte-compatible with today. Open question for Gate 2 Discovery: refactor in place (swap service layer, keep UI/state), rebuild from scratch, or hybrid — decided after the new API shape is on the table.

## 2. Expected Behavior

All admin menu-management capabilities preserved (no functional regression for admin user). All Menu Management network traffic moves to the new API contract. Downstream POS code is untouched — the menu data shape it reads is unchanged. Clean cutover (no dual-mode toggle) unless owner specifies otherwise. Cross-CR dependency: CR-010 (item_unit / item_unit_price) requires admin to edit those fields; new Menu Management API must support them end-to-end — backend/owner reconciles both contracts together in Discovery.

## Affected Modules

- Settings (auto-detected from description — agent to refine during impact analysis)

## 7-Artifact Status

| # | Artifact | Status |
|---|---|---|
| 0 | Session Start | PENDING |
| 1 | **Intake** | **PRESENT (this doc)** |
| 2 | Impact Analysis | PENDING |
| 3 | Implementation Plan | PENDING |
| 4 | Code Gate | PENDING (mandatory for POS 4.0) |
| 5 | Impl Summary + QA | PENDING |
| 6 | Owner Smoke Sign-off | PENDING |

## Next Steps

1. Agent performs impact analysis (artifact #2)
2. Agent creates implementation plan (artifact #3)
3. Agent creates code gate / scope lock (artifact #4)
4. Owner gives GO → implementation begins
5. Impl summary + QA (artifact #5)
6. Owner smoke sign-off (artifact #6)

---

*Generated by `create_intake.py` — 2026_06_01*
