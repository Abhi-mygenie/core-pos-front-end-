# BUG-122 — Code Gate / Scope Lock (Gate 4)

**Date:** 2026-06-10
**Sprint:** POS 4.0
**Status:** LOCKED
**Prerequisites:** Gates 0–3 (Intake + Impact Analysis + Implementation Plan) ✅

---

## 1. Scope Lock — What Is IN

### Modified Files (2)

| File | Change | Lines (est.) |
|---|---|---|
| `src/components/dashboard/ScanOrderPopOut.jsx` | Add `&& order.isWebOrder` to `isUnconfirmedScanOrder` predicate (L58-59) | 1 |
| `src/components/cards/OrderCard.jsx` | +2 derived flags (`isWebYetToConfirm`, `isPosYetToConfirm`) after L277. Gate snooze to `isWebYetToConfirm` (L470). Split footer: web → Accept/Reject (unchanged), POS → tick (✓) button only. | ~20 |

**Total: 2 files, ~21 lines, 0 new files.**

---

## 2. Scope Lock — What Is OUT

| Item | Reason |
|---|---|
| `DashboardPage.jsx` | No change — `handleConfirmOrder` already handles the confirm API call for both web and POS YTC. The tick button calls the same `onAccept` prop. |
| `ScanOrderPopOut.jsx` — any UI changes | Popup itself unchanged. Only predicate tightened. |
| `orderTransform.js` | No change — `isWebOrder` already parsed in `fromAPI.order` (L243). |
| `socketHandlers.js` | No change — both `handleNewOrder` and `handleScanNewOrder` already set `isWebOrder` correctly. |
| Merge/Shift/FoodTransfer guards | No change — these use `isYetToConfirm` (applies to ALL YTC). Correct behavior: hidden for both web and POS YTC. |
| New API endpoints | None — tick uses existing `confirmOrder` API. |

---

## 3. Baseline Rules Affected

| Rule | Impact |
|---|---|
| ScanOrderPopOut predicate | Tightened: `fOrderStatus === 7` → `fOrderStatus === 7 && isWebOrder`. More restrictive — no false negatives for web orders. |
| OrderCard footer for fOrderStatus 7 | Split into web (Accept/Reject — unchanged) vs POS (tick only — new). Same underlying API call (`confirmOrder`). |

**No financial baseline rules affected.**

---

## 4. Frozen Sprint Cross-Check

| Frozen Sprint | Files We Touch | Conflict? |
|---|---|---|
| POS 2.0 (POS2-002 Phase 4) | `ScanOrderPopOut.jsx` | **NO** — additive predicate condition |
| POS 3.0 (BUG-087, BUG-097, BUG-102) | `OrderCard.jsx` | **NO** — new derived flags, new ternary branch. Existing web Accept/Reject code untouched. |

**Zero frozen-sprint conflicts.**

---

## 5. GO / NO-GO Checklist

| # | Item | Status |
|---|---|---|
| 1 | Root cause identified (fOrderStatus 7 ≠ web-exclusive) | ✅ |
| 2 | `isWebOrder` field verified in `fromAPI.order` + both socket paths | ✅ |
| 3 | Scope locked — IN/OUT defined | ✅ |
| 4 | No frozen-sprint conflicts | ✅ |
| 5 | No financial baseline changes | ✅ |
| 6 | Same API for tick as Accept (`confirmOrder`) | ✅ |
| 7 | Backward compatible (web flow completely unchanged) | ✅ |
| 8 | **Owner GO** | ✅ APPROVED (2026-06-10) |

---

*BUG-122 Code Gate — 2026-06-10. Gate 4 LOCKED → CLOSED — OWNER VERIFIED (2026-06-10). 2 files, ~21 lines.*
