# BUG-141 — Intake Document (Gate 0 + Gate 1)

**Status:** REGISTERED · **Priority:** P1 · **Risk:** MEDIUM · **Sprint:** POS 5.0 · **Date:** 2026-07-04
**Source:** OWNER-REPORTED (batch intake 2026-07-04)
**Type:** Bug — Frontend (Menu Management → Import)
**Duplicate check:** DISTINCT

---

## 1. Symptom
When updating the menu by importing an Excel file, the "Type" column value is not captured / not applied to imported rows. Other columns from the sheet appear to import correctly.

## 2. Suspected Area
- Menu Excel import path (file location TBD — grep did not surface an import util under `pages/MenuManagementPage.jsx` immediate scope). Planning must locate the import handler (likely in `components/panels/menu/` or a `utils/` importer).
- Column mapping between sheet header "Type" and backend field name (`food_type` or similar).

## 3. Blast Radius
~2–5 files including the import handler and any transform. Blast: MEDIUM. Hotspots: NO.

## 4. Evidence
- Owner verbal report only. Confidence: REPORTED.
- Sample Excel file / imported vs expected diff: NOT PROVIDED — request during Planning.

## 5. Open Questions (for Planning)
1. What header exactly appears in the Excel template — "Type", "Veg/Non-Veg", or another label?
2. Is the import a full replace or an update-in-place path (endpoint differs)?
3. Related to BUG-140? (Same field, different entry point — possibly the same transform bug on the write side.)

## 6. Next
Planning Gate 2 — locate importer, curl-probe endpoint, confirm header-to-field mapping.
