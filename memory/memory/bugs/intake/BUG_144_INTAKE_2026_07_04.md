# BUG-144 — Intake Document (Gate 0 + Gate 1)

**Status:** REGISTERED · **Priority:** P1 · **Risk:** MEDIUM · **Sprint:** POS 5.0 · **Date:** 2026-07-04
**Source:** OWNER-REPORTED (batch intake 2026-07-04)
**Type:** Bug — Frontend (Order Ticket UI + Print/KOT)
**Duplicate check:** DISTINCT

---

## 1. Symptom
Token Number is not reflecting on order tickets (in-app card / preview) and is not being passed to the print output (bill / KOT). Field appears to be absent end-to-end from the display and print layers.

## 2. Code Reality
- `grep -rn "token_number|tokenNumber|token_no"` in `frontend/src/` returned ZERO matches. Field is not consumed anywhere in the FE codebase (as of 2026-07-04).
- Whether the backend `/order-logs-report` (and related order payload) already carries token info is unknown — Planning must curl-probe (R11).

## 3. Blast Radius
3–5 files if the field must be added end-to-end: transform → order-card renderer(s) → print template. Blast: MEDIUM. Hotspots: OrderCard renderer.

## 4. Evidence
- Owner verbal report only. Confidence: REPORTED.
- Backend payload sample for a token-issued order: NOT PROVIDED — critical for Planning.

## 5. Open Questions (for Planning)
1. Is Token Number a backend-generated per-order value, or an operator-entered field?
2. Where should it appear — order card, KOT header, bill header, or all three?
3. Are all channels affected (walk-in / dine-in / delivery / scan) or channel-specific?

## 6. Next
Planning Gate 2 — curl-probe order payload for token field; map insertion points on card + print template.
