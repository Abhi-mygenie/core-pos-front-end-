# QA Handover — CR-114 + CR-115: Stock Update Opt-In + Search/Sort

**Document:** QA_HANDOVER_CR114_115_2026_07_31.md
**Items:** CR-114 · CR-115
**Implementation Date:** 2026-07-27

---

## 1. Registry Sync Confirmation

```
Items: CR-114, CR-115 — IMPLEMENTED gate 5a
Code markers:
  ✅ CR-114: SmartPurchasePanel.jsx L21 (selectedForPurchase Set), L109, L122
             AutoShoppingList.jsx L103 (split sections), L128 (Purchase List), L228 (All Ingredients)
  ✅ CR-115: SmartPurchasePanel.jsx L22 (purchaseSearchQuery), L23 (purchaseCategoryFilter), L115
             AutoShoppingList.jsx L107 (search filter), L112 (category filter), L252 (filter bar)
  ✅ purchasePlanner.js L181+188 — category lookup for headers + dropdown
```

---

## 2. Code Checks

| Check | Command | Expected |
|-------|---------|---------|
| C1-CR114 | `grep -c 'selectedForPurchase' /app/frontend/src/components/inventory/SmartPurchasePanel.jsx` | ≥2 |
| C2-CR114 | `grep -c 'CR-114' /app/frontend/src/components/inventory/smart/AutoShoppingList.jsx` | ≥2 |
| C3-CR115 | `grep -c 'purchaseSearchQuery\|purchaseCategoryFilter' /app/frontend/src/components/inventory/SmartPurchasePanel.jsx` | ≥2 |
| C4-CR115 | `grep -c 'CR-115' /app/frontend/src/components/inventory/smart/AutoShoppingList.jsx` | ≥3 |

---

## 3. Test Cases

### CR-114 — Default All Items Unselected (Opt-In)

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-1 | Inventory → Stock Update → wait for data to load | No items pre-selected; "Update Stock" button hidden (0 active rows) |
| TC-2 | Tick checkbox on 1 item → enter rate | "Update Stock (1 vendor)" floating button appears |
| TC-3 | Untick that item | Floating button disappears again |
| TC-4 | Two sections visible | Page shows "Purchase List" (ticked items) section + "All Ingredients" section |

### CR-115 — Search Filter + Category Sort

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-5 | Stock Update → type in search box | Item list filters to matching ingredient names in real time |
| TC-6 | Clear search | All items visible again |
| TC-7 | Open category dropdown filter | Shows category options derived from ingredient data |
| TC-8 | Select a category | Items filtered to only show that category |
| TC-9 | Items grouped by category | "All Ingredients" section has category header rows separating groups |

---

## 4. Regression Tests

| R# | What | Why |
|----|------|-----|
| R1 | CR-123 sticky button still shows when items ticked | CR-114 changes selectedForPurchase state — must not break CR-123's `activeRows.length > 0` gate |
| R2 | Existing vendor/rate selection preserved | CR-114 opt-in layer is above existing form logic |

---

## 5. Credentials + Environment

| Field | Value |
|---|---|
| Login | `owner@18march.com` / `Qplazm@10` |
| Route | Inventory → Stock Update |
| Note | Stock Update API may hang in QA env (pre-existing). Code verification checks C1–C4 are conclusive if data doesn't load. |
