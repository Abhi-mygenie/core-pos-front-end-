# SESSION HANDOVER — 2026-09-26 — ENTRY POINT for the next agent: CR-389 (Registry Excel Export) then CR-390 (Screen Reference PDFs, all modules)

**Sprint:** `modules_pdf` (both CRs) · **Your role:** PLANNING (Role 2) for CR-389 first, then CR-390 · **Code changed this session:** NONE (`frontend/src` untouched; memory docs only)
**Supersedes as entry point:** `handover/SESSION_HANDOVER_2026_09_26_CR390_MODULES_PDF_INTAKE.md` (keep for CR-390 detail)

Owner instruction (verbatim): *"write a handover for agent who will work on pdf as well as this CR for planning — present him specific details of these 2 CRs; first he will start 389 and then pdf module one."*

---

## 0. Read first (5 min)
1. `control/AGENT_PROMPT_ALPHA.md` — v0.7 rules, PLANNING role §ROLE 2, approval matrix, R0/R3/R19/R20.
2. This file.
3. `change_requests/CR-389_REGISTRY_EXCEL_EXPORT_ALL_CRS_BUGS_INTAKE.md` (pulled from remote, sha-verified).
4. `change_requests/CR-390_MODULES_SCREEN_REFERENCE_PDF_ALL_MODULES_INTAKE.md` (§ "Owner direction 2026-09-26 (session 2)" + § "Brainstorm — option analysis").
5. `control/SPRINT_STATUS.md` → `## modules_pdf` section + Owner Decision Log rows dated 2026-09-26.

---

## 1. WORKSPACE STATE — read before touching any control doc

| Fact | Detail |
|---|---|
| Local `/app` | Fresh clone of `21implement` at commit `0aa17ee9` (2026-09-25) + this session's memory edits. **Not tracking remote.** |
| Remote `21implement` HEAD | `0b4d3a6` (2026-09-25 late) — **ahead of local by 13 memory files, 0 `frontend/src` diffs** |
| Only-on-remote files | `change_requests/BUG-462_*`, `BUG-463_*`, `BUG-464_*`, `CR-389_REGISTRY_EXCEL_EXPORT_*` (this one already pulled locally), `backend_briefs/BACKEND_BRIEF_BUG-463_2026_09_25.md`, `handover/QA_HANDOVER_CR376_FU_A_2026_09_25.md`, `handover/SESSION_HANDOVER_2026_09_25_CR376_FU_B_GATE2_COMPLETE.md`, `impact/CR-376-FU-A_IMPACT_ANALYSIS.md`, `impact/CR-376-FU-B_IMPACT_ANALYSIS.md`, `plans/CR-376-FU-A_IMPLEMENTATION_PLAN.md`, `test_reports/QA_REPORT_CR376_GATE5B_RETEST_ROUND2/3_2026_09_25.md`, `.gitconfig` + **updated** `control/registry.json` (729 items), `CR_REGISTRY.md`, `BUG_TRACKER.md`, `SPRINT_STATUS.md`, `CONTROL_DASHBOARD.md` |
| Local-only work (this session) | CR-390 intake + `evidence/CR-390/` + CR-390 handover; CR-390 entry in `registry.json` (726 items); `## modules_pdf` section + decision-log rows in `SPRINT_STATUS.md`; Last-Updated lines in `CR_REGISTRY.md` / `CONTROL_DASHBOARD.md` |
| ID collision (resolved) | Modules-PDF CR was first registered as CR-389, renumbered to **CR-390** because remote already held CR-389 = Excel Export. Remote max IDs: CR-389, BUG-464 → **next free: CR-391 / BUG-465** |
| **HARD BLOCKER HB-389-01** (owner-set) | **Registry sync before CR-389 build starts.** Pull remote `memory/control/*` + the 12 docs above, then re-apply local CR-390 entry/sections on top (target: registry = remote 729 + CR-390 = 730). Preserve `frontend/.env`, `backend/.env`, `.emergent/`, `node_modules/`. Verify with `python3 -c "import json;d=json.load(open('memory/control/registry.json'));print(len(d['items']))"` → 730 and both `CR-389`/`CR-390` present. Owner wording: "we will do registry sync before starting — put as hard blocker". Do this **with owner approval**, as a DEPLOYMENT-lite step, before Gate 4 of CR-389. |

Tip: a shallow clone of the remote is reproducible with `git clone --depth 1 --branch 21implement --filter=blob:none --no-checkout <repo> /tmp/remote_chk && git -C /tmp/remote_chk show FETCH_HEAD:<path>` (repo URL in `PRD.md` / previous handovers; never print credentials).

---

## 2. CR-389 — Registry Excel Export (START HERE)

### 2.1 What it is
Owner (2026-09-25): *"list down all CRs and bugs with status and 3–5 lines of desc … we will create excel for it."* Tooling/doc CR — **zero app code**. Output: `memory/reports/REGISTRY_EXPORT_<YYYY_MM_DD>.xlsx` (tabs **All Items · Open Only · Summary**) generated by re-runnable `memory/reports/registry_export.py` (`openpyxl`, local tooling only; **not** in frontend `package.json` / app `requirements.txt`). `openpyxl` is NOT installed in the pod → `pip install openpyxl` at implementation.

### 2.2 Source-data facts (from intake audit + re-checked on remote registry 2026-09-26)
729 items on remote (BUG 469 · CR 249 · INV 5 · GAP 1 · misc POS2-*/PROD-*/PROD-HOTFIX-*/DEV-*/UX-*). Coverage: `area` 233/729 (**32 %** → infer), `priority|severity` 579, `risk` 475, registered date ~50 %, implemented date few, intake docs ≈190. `CR_REGISTRY.md` (211 rows) / `BUG_TRACKER.md` (354 rows) are fallback sources for descriptions.

### 2.3 Owner decisions given 2026-09-26 — **STATED BUT NOT YET WRITTEN INTO THE INTAKE DOC** (I asked for approval before updating; owner redirected to this handover). **Your step 1: apply them to the intake OD table, registry, SPRINT_STATUS decision log.**

| OD | Owner verbatim | Locked value |
|---|---|---|
| OD-389-01 Area value set | "01 - all possible modules" | **(c) full module taxonomy** (beyond POS/PMS/INV/CRM/SHARED). Proposed value list — Order Entry · Menu Management · Expenses · Inventory · Recipes · Day Closure & Settlement · Credit Management · Daily Report · Insights · Printing · Settings · Employees · Aggregator · PMS · CRM · Auth/Boot · Control/Tooling · Shared; unknown → `UNCLASSIFIED` for owner review. Finalise list in the plan. |
| OD-389-02 Columns | "02 all 21" | **(a) all 21 columns** as listed in intake §Columns |
| OD-389-03 Non-CR/BUG IDs | "03 yes add" | **(a) include all items** (INV, GAP, POS2-*, PROD-*, PROD-HOTFIX-*, DEV-*) with Type column |
| OD-389-04 Description depth | "04 a" + **2026-09-26 addendum: "description in CR-389 should be in plain English, not technical, good enough to describe in 3–5 lines what that bug contains; it's not a hard rule 3–5 lines, but desc should be self-explanatory for anyone to look at it and understand the CR/bug"** | **(a) every row gets a description, written in PLAIN ENGLISH for a non-technical reader** — what the user saw / what was asked, why it matters, what was (or will be) done, current state. **3–5 lines is a guideline, not a hard rule**; the test is "anyone can read it and understand the CR/bug without opening another doc". No file names, function names, endpoint paths, gate codes or agent jargon in the Description column (those belong in Files / Status Detail / Docs). Rows without an intake doc: still plain-English from title + tracker row + status; flag `DESC-THIN` in Notes only if genuinely too little is known. **Add to the plan a description style guide + 3 worked examples (a BUG, a CR, a blocked item) for owner sign-off before bulk generation; QA spot-check must include a "plain-English readability" pass on 10 random rows.** |
| Conflict pre-check | "we will do registry sync before starting put as hard blocker" | **HB-389-01** (see §1) — Gate 4 precondition |
| Route | "Fast Lane: planning route" | Fast Lane **rejected** → normal Planning route (Gate 2 → Gate 3 → owner "Gate 4 GO" → Implementation → QA spot-check → owner smoke) |
| Sprint key | "key - modules_pdf" | CR-389 `sprint_key` = **`modules_pdf`** (intake proposed `sep_bug_closure` → change) |
| Gate 2/3 in one go or separately | not answered | **Ask owner first** ("both" vs "IA first"). Recommendation: combined IA + Plan (small tooling item). |

### 2.4 Planning notes already established (reuse, don't redo)
- **Code reality: NONE** — `memory/reports/registry_export.py` absent locally and on remote.
- **Conflict pre-check:** only HB-389-01 (registry drift). No `src/` files, no hotspots, no FILE_OWNERSHIP entries.
- **Local registry does not yet contain CR-389** (it's on remote only). After HB-389-01 sync it will. If you must register anything before the sync, add CR-389 locally as a mirror with note "reconcile at sync".
- Verification matrix seed for the plan: (1) row count == registry item count; (2) 10-row spot-check vs `CR_REGISTRY.md`/`BUG_TRACKER.md` (ID, status, priority, sprint); (3) every `area` non-empty; (4) description present for every row, **plain-English readability pass on 10 random rows (no file/function/endpoint names, no gate codes; a non-technical reader understands the item)**, `DESC-THIN` flag only where genuinely unknown; (5) Summary pivots sum to total; (6) `Open Only` = status ∉ {CLOSED, DUPLICATE, WONT-FIX}; (7) re-run idempotent (same input → same sheet); (8) no secrets/credentials copied into cells (R20).
- **Description generation approach (plan must state it):** descriptions are *written*, not copied — the script should assemble candidate facts (intake Requirement/Symptom, tracker row, status) and the agent (or an LLM step if owner approves) rewrites them into plain English per the style guide; raw technical status strings stay in `Status Detail`. Style guide + 3 worked examples (BUG · CR · blocked item) go to the owner for sign-off **before** bulk generation of 729 rows.
- Artifact paths: `impact/CR-389_IMPACT_ANALYSIS.md`, `plans/CR-389_IMPLEMENTATION_PLAN.md` (or one combined doc if owner says "both").

### 2.5 CR-389 sequence for you
1. Apply §2.3 decisions to intake + registry + SPRINT_STATUS (+ Last-Updated lines). Get owner "ok".
2. Ask: "Gate 2 + Gate 3 together, or IA first?" → write accordingly. Present. Owner "Gate 3 closed" → "Gate 4 GO".
3. **Before build:** execute HB-389-01 registry sync (owner-approved). Confirm 730 items.
4. Implement script → generate xlsx → run verification matrix → QA spot-check → hand file path to owner.

---

## 3. CR-390 — Screen Reference PDFs for ALL modules (AFTER CR-389)

### 3.1 What it is
Repeat the PMS exercise (real React routes + realistic dummy data → screenshots → branded PDF) for every module, through a **committed, re-runnable pipeline** (the original PMS scripts `/app/pms_dummy_data.py`, `/app/generate_pms_pdf_v2.py` were never committed → lost). Existing model deliverable: `frontend/public/MyGenie_PMS_Screen_Reference.pdf` (v2.0, 16 Sep 2026, 38 pp, 36 screens, owner-approved). Evidence pointer: `evidence/CR-390/EVIDENCE_POINTER.md`.
P2 · Risk LOW (→ MEDIUM only if OD-390-01 = in-app demo flag) · Gate 1 INTAKE complete · **Gate 2 blocked until the OPEN ODs below are locked.**

### 3.2 Module list — LOCKED (OD-390-09/10) — 9 PDFs, one approval cycle each
| Module | Screen set | Notes |
|---|---|---|
| Menu Management | `/menu` (categories, items, variations/add-ons, availability) | |
| Expenses Management | `/expense-setup`, `/expenses` | smallest → recommended pilot |
| Inventory Management | `/inventory-setup`, `/inventory-smart-purchase`, `/inventory-receive`, `/inventory-purchase`, `/inventory-current-stock`, `/inventory-sub-recipe-stock`, `/inventory-audit`, `/inventory-physical`, `/inventory-dashboard`, `/recipes` | |
| Day Closure & Settlement | `/settlement/preview`, `/reports-module/cashier-settlement`, `/day-closure`, `/reports-module/settlement` | screen SET locked; order NOT |
| Credit Management | `/credit` | owner: "credit management different module" |
| Daily Report | P&L, Consumption, Sales Summary, Order Report, Settlement Report, Expense Report, Purchase Report | Beta / coming-soon excluded (OD-11 locked) |
| Insights — Basic | 13 daily-use reports (allocation in intake OD-390-12) | locked |
| Insights — Advanced | 23 finance/analyst reports | locked; 4 Beta reports excluded |
| PMS (redo) | all `/pms/*` incl. Front Desk v2 (CR-385) | **scope DEFERRED (OD-13)** — PMS work ongoing → sequence LAST |
Excluded unless owner adds: Order Entry/Dashboard, Delivery Mgmt, Settings, Employees, Aggregator Setup, dev routes.

### 3.3 Decisions LOCKED / DEFERRED (details + verbatim in intake and SPRINT_STATUS decision log)
- **OD-390-04 LOCKED:** page order = perfect user journey; **each module's journey is drafted by the agent, then discussed and approved LIVE with the owner at module start** ("I will discuss each journey when we start"). Never pre-lock a journey.
- **Audience LOCKED: for clients** → scrub rules (OD-14 LOCKED): no Beta/coming-soon, fictional business, realistic ₹/GST, no internal CR/BUG IDs or test-ids.
- **OD-08 template DEFERRED** ("later") — PMS v2.0 template is interim default. **OD-13 PMS redo scope DEFERRED.**

### 3.4 Still OPEN — must be locked before "Gate 2 GO" (recommendations already written in intake §Brainstorm)
| OD | Topic | Recommendation |
|---|---|---|
| OD-390-01 | Data strategy | (b) Playwright intercepts `/api/*` → fixture JSON; real logic/charts; 0 app code |
| OD-390-02 | Persona | one fictional business ("Sharma Hotel & Restaurant"); report fixtures generated from one synthetic order set |
| OD-390-03 | Granularity | primary view + 1–2 key states, ≤3 pages/route |
| OD-390-05 | Packaging | master + per-module PDFs + PNG packs from one run |
| OD-390-06 | Output location | `memory/design_briefs/downloads/screen_reference/` (CR-372 `PUBLIC_ROUTES.md` treats `public/` PDFs as temporary carve-outs; move PMS PDFs out in the redo) |
| OD-390-07 | Sequencing | pilot Expenses → Menu → Inventory → Day Closure & Settlement → Credit → Daily Report → Insights Basic → Advanced → PMS redo |
| OD-390-14b | Disclaimer placement/wording | cover line + short per-page footer ("Sample data shown for illustration · © MyGenie") — owner: "in footer or somewhere we need to define" |
Owner may answer "all recommended".

### 3.5 CR-390 sequence for you (after CR-389 reaches Gate 4/implementation or as owner directs)
1. Lock OD-390-01/02/03/05/06/07/14b with owner → record in intake + decision log.
2. Owner verbatim "Gate 2 GO" → PLANNING Gate 2: pipeline design (Playwright + fixtures + manifest + PDF assembler in `frontend/scripts/screen-reference/`), **read-only endpoint inventory per route** (grep `src/api/services/*`, no live calls), fixture persona schema, and a **journey DRAFT for the pilot module (Expenses)** to discuss live.
3. Gate 3 plan per module (manifest + fixtures + verification: page count, scrub check, totals reconcile) → Gate 4 GO → generate pilot → owner reviews PDF → lock edition v1.0 → next module.
4. No pipeline code or screenshots before "Gate 2 GO" + per-module journey approval.

---

## 4. Files touched this session (all under `memory/`)
`change_requests/CR-390_*_INTAKE.md` (NEW) · `evidence/CR-390/EVIDENCE_POINTER.md` (NEW) · `handover/SESSION_HANDOVER_2026_09_26_CR390_MODULES_PDF_INTAKE.md` (NEW) · this file (NEW) · `change_requests/CR-389_*_INTAKE.md` (pulled from remote, unmodified) · `control/registry.json` (CR-390 added; meta) · `control/CR_REGISTRY.md`, `control/SPRINT_STATUS.md`, `control/CONTROL_DASHBOARD.md` (Last-Updated lines, `## modules_pdf` section, decision-log rows).

## 5. Rules to keep
R0 registered IDs only (CR-389, CR-390) · R3 no invented policy — every open point is an OD · R19 session scope · R20 no secrets in docs/xlsx · zero `frontend/src` change in either CR until an approved plan says otherwise · owner approval before every gate move and before the registry sync.
