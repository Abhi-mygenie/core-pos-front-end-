# SESSION HANDOVER — 2026-09-22 — CR-385 · Phase 2 ENTRY (M1 New Booking · M3 Check-In)
**For the next agent.** Boot per `control/AGENT_PROMPT_ALPHA.md` → take the **PLANNING role first**, not Implementation. Your first message to the owner must be: (1) the Phase 2 plan in plain English, (2) the **top five things** the owner must know/decide, (3) the **conflict list** in §3 below (things the phased plan §2 says that are no longer true after Phase 0/1). Then **wait for "Phase 2 GO"**. Do not write Phase 2 code before it.

---
## 0. Where we are (one paragraph)
Front Desk (Beta) `/pms/front-desk-v2` — Phase 0 (shell: 4 tiles, 4 tabs, chips, expansions, Rooms, alerts, search) and Phase 1 (Channel Manager › Front Desk Rules tab; Arrivals Modify with server preview; Cancel/No-Show inline) are **coded, unit-tested (66/66), agent-QA'd (iteration_11–16) and owner-smoked. Gate 6 for P0+P1 is CLOSED (D80).** Bug-fix sub-phases 0.5, 1.5, 1.5b, 1.5c are closed (BUG-434…442, 445 QA-VERIFIED; 443/444 deferred to legacy retirement FU-385-C). Phase 2 is next: **+ New Booking** comes alive in the Front Desk header, and **Check In** comes alive on Arrivals rows / RoomDetail.

## 1. Read in this order (30 min)
1. `control/AGENT_PROMPT_ALPHA.md` (roles, gates, R14 scope rule, EXIT GATE 5/5)
2. `plans/CR-385_IMPLEMENTATION_PLAN_PHASED.md` — §0 (frozen rules, §0-bis Phase N.5 rule), **§2 Phase 2** (M1/M3), §4 testids, §1.7 (what P1.5 changed)
3. `plans/CR-385_DESIGN_DECISIONS.md` — D17, D23, D30, D32, D34–36, D39, D47-h, D48-c, D50–D54, **D70–D80**, OD-385-16/17/18 (+ OD-385-19 pending)
4. `backend_briefs/BACKEND_BRIEF_CR-385_MASTER.md` v2.2 — contracts for `direct-reservation`, `room-availability`, check-in FormData, `charge{}`; open asks BQ-385-22…25
5. Intakes: `change_requests/BUG-431_*`, `BUG-432_*` (P2 entry conditions), `BUG-446_*`, and OD-385-19 (in DESIGN_DECISIONS)
6. Latest reports: `test_reports/QA_REPORT_2026_09_22_CR385_P1_5_ROLE4.md`, `handover/CR-385_P1_5C_BUG_FIX_REPORT_2026_09_22.md`
7. Code you will touch/extend: `pages/pms/FrontDeskWorkstationPage.jsx`, `components/pms/frontdesk/ArrivalsPanel.jsx`, `ModifyBookingForm.jsx` (pattern to mirror), `GuestTable.jsx` (`PhaseButton`, `renderExpansion`), `RoomsPanel.jsx`/RoomDetail, `api/services/frontDeskService.js`, `restaurantSettingsService.js` (`getFrontDeskRules`), `api/transforms/frontDeskTransform.js`, legacy `pages/pms/CheckInPage.jsx` + `NewBookingPage.jsx` (copy source, never edit)

## 2. Top five for the owner (present these)
1. **Money in, first time.** M1 takes an optional advance (Cash/Card/UPI + reference) and M3 collects at check-in (+ paid upgrades). Frozen rules: the FE **never sends a price** (`rate_per_night`, `room_price`, `amount_after_tax` are forbidden); every rupee shown comes from the server's `charge{}`. BUG-431/432 (legacy pages priced on the client) are fixed *by construction* here and must be re-verified in P2 QA.
2. **Early check-in rule goes live.** The switch you saved in Phase 1 (Channel Manager › Front Desk Rules) now gates the **Check In** button for future-dated bookings (tooltip "Arrives {date} — modify the booking dates…"); server 422 text shown verbatim regardless.
3. **Rooms at check-in.** Booked-type rooms from the board: `available` and `hk` (needs-cleaning, amber note, allowed — N9/D52); "Show higher categories" = upgrade (paid/complimentary + reason, D23). Sandbox: only **r4/r5/r1 (Suite)** may be checked in; **r2/r3 (8526/8524) never**; every P2 test stay must be **settled/checked out** afterwards (old Departures page, Cash) so the room is free again.
4. **Two owner decisions are open and affect Phase 2 layout/scope:** OD-385-19 (Modify as flat button vs kebab — Phase 2 adds Check In as a third live button per row) and OD-385-18 (Split payments: **no Split tile in P2**, option a already chosen). Also decide BUG-446 routing (legacy browser date) after the note.
5. **Gates:** Plan note → "Phase 2 GO" → Implementation (new files + the existing-file edits listed in §3) → unit + build → testing_agent (live sandbox, both viewports, settle everything) → Bug Fix 2.5 if needed → QA → owner smoke §2.5 ("Phase 2 smoke OK") → Phase 3. Backend latency BQ-385-22 still makes the LR call 1–10 s.

## 3. CONFLICTS — phased plan §2 vs. what actually shipped in P0/P1/P1.5 (highlight to the owner; update the plan note before GO)
| # | Plan §2 says | Reality after P0/P1 | What to do in the plan note |
|---|---|---|---|
| C1 | §2.1 "Existing-file edits — **none**" | Wrong. Phase 2 must edit: `ArrivalsPanel.jsx` (Check In → live, `onOpen(r.id,'checkin')`, `renderExpansion` kind `'checkin'`), `FrontDeskWorkstationPage.jsx` (`expanded.kind` += `'checkin'`; **+ New Booking** live → a top-of-tab expansion state; one-open-at-a-time F1 still applies), `GuestTable.jsx` `PhaseButton` phase 2 → live, RoomDetail "Check in" live, `frontDeskService.js` (+ `createBooking`, `getAvailability`, `checkIn`), possibly `KpiTabStrip`/header for the New Booking button. List them with line anchors (Entry Verification). |
| C2 | Early-check-in guard reads `allowEarlyCheckin` "from profile" | Phase 1 introduced `restaurantSettingsService.getFrontDeskRules()` → `{ allowEarlyCheckin, extendRateMode }` from `restaurant-settings/list` `basic.*` (+ `pms.*` aliases). Profile does not carry it. | Read via `getFrontDeskRules()` once per snapshot (or add it to `useFrontDeskSnapshot`). Same for `auto_print_checkin_receipt` (D32) — extend `getFrontDeskRules` to return it (read-only there; the M7 tab keeps writing only the two rules). |
| C3 | Modify "(kebab)" · row shows 3 buttons after P2 | P1 shipped **flat buttons** (Check In · Modify · Cancel/No-Show). | OD-385-19 — owner picks flat (recommended) or kebab before GO. |
| C4 | `cancelled_by` / staff name "from profile" pattern (CR-362 copy rule) | BUG-442: `restaurant.profile.fullName` never existed; the correct source is `useAuth().user.fullName || 'staff'`. | Any `checked_in_by` / `booked_by` field in M1/M3 uses `useAuth().user.fullName`. Never `restaurant.profile.*`. |
| C5 | Test file location: §2.3 unspecified | P1 tests live in `components/pms/frontdesk/__tests__/*.cr385.test.jsx`; owner-mandated P1.5/1.5c tests live in `tests/cr385/*.cr385.test.jsx`. | Put Phase 2 tests in `tests/cr385/phase2.cr385.test.jsx` (owner's later convention); keep the `cr385` name so `--testPathPattern=cr385` runs all. CRA `resetMocks:true` → re-arm mock implementations in `beforeEach`. |
| C6 | Expansions are "detail" only in P0 plan text | P1 added `expanded.kind` ∈ detail·modify·cancel·noshow with `openExpansion`/`afterAction` (toast → close → refetch). | Reuse: `kind:'checkin'`; success path = `afterAction('Checked in — Room rX')`. Inline forms must `stopPropagation` on click/keydown (GuestTable arrow-key handler `preventDefault`s inside inputs otherwise). |
| C7 | Modify preview "(debounced)" unspecified | Owner rule from P1: **≥500 ms debounce, never two previews in flight, persistence check in QA**. `ModifyBookingForm.jsx` is the reference implementation. | If M1's bill preview calls the server (rates/availability), apply the same debounce/in-flight pattern and the same QA persistence check (no side effects on reads). |
| C8 | Sandbox: "use today's date", "pick a suite for the upgrade" | Business date is server `meta.business_date` (rolls at IST midnight — it moved 21 → 22 Sep mid-session and broke a QA run). Only r4/r5/r1 (Suite) may be occupied; r2 (executive) is forbidden, so the "executive booking → upgrade to suite r4" path is the safe upgrade scenario. | Write dates relative to `meta.business_date`; brief the testing agent with chip ids `fd-chip-arrivals-<late|today|tomorrow|upcoming>` and the room rule. |
| C9 | Cancel-dialog reasons come "from settings" | BUG-440 fixed the shape `{ reasons:[{reasonId, reasonText}] }`. Any other list from `settingsService` (e.g. payment methods, ID types) must be checked for the transformed shape, not the raw API. | Probe shapes in Entry Verification. |
| C10 | `updateSettings`-style multipart writes | D75: shared axios default is JSON → FormData needs an explicit `multipart/form-data` header (check-in FormData builder copied from `pmsCheckIn` already does this — verify on the wire in QA). | Wire-shape assertion in the QA brief. |
| C11 | Legacy pages as fall-back during P2 | BUG-443/444 deferred: old Modify sends a harmless dirty payload; old tape chart hides new Direct bookings. Old Arrivals/Reservations Cancel now works (BUG-441). | Smoke instructions must not rely on the old Reservations page to find new bookings. |
| C12 | Row-action testids | BUG-439 convention: row cell `fd-row-<id>-<action>-btn`, drawer copy `fd-row-<id>-exp-<action>-btn`; X-10 duplicate audit is a standing QA case. | Keep the convention for `checkin`; run X-10 with the Check-In and New Booking forms open. |

## 4. Sandbox + credentials
OWNER_TGK in `memory/test_credentials.md` (never paste values). Preprod RID 69 `sandbox-pms`. Rooms: r4/r5/r1 = Suite (allowed), r2 executive 8526 / r3 suite 8524 = **forbidden** (owner's real test data, currently in-house/overdue). Test bookings: Suite type; **check-ins only into r4/r5/r1 and always settled/checked out afterwards**; settings end at defaults (`allow_early_checkin=false`, `extend_rate_mode=calendar`, `auto_print_checkin_receipt=false`); never confirm a No-Show. Single-session token: a browser login invalidates a curl token.

## 5. Open items ledger
- Entry conditions P2: BUG-431, BUG-432 (fixed by construction; re-verify in P2 QA).
- Review after the P2 note (owner): BUG-446, OD-385-19, BQ-385-25.
- Deferred to FU-385-C: BUG-443, BUG-444. Deferred to P3: BUG-433 (₹1 rounding, AC-02).
- Backend asks open (not blocking): BQ-385-22 (LR latency), 23 (modify reason → special_requests), 24 (500 on non-int id), 25 (occupancy %).
- Push: workspace has no git remote → owner uses the platform "Save to GitHub".

## 6. Definition of done for your first turn
Owner has seen: plan in plain English · top five (§2) · conflict table (§3, with your proposed resolutions) · the Entry Verification results (grep anchors, `getFrontDeskRules` shape, `direct-reservation`/`room-availability` probes on the sandbox — read-only) · the sandbox rules. You have written nothing under `frontend/src`. You stop and wait for **"Phase 2 GO"**.
