# QA Handover — CR-146 + BUG-325 + BUG-326 — 2026-08-17

**Date:** 2026-08-17
**Items:** CR-146, BUG-325, BUG-326
**Test account:** `owner@thegoankitchen.com` / `Qplazm@10` (restaurant 69 — has Aggregator menu + sub-brand "mallu goan" id=109)
**URL:** https://react-pos-frontend-12.preview.emergentagent.com

---

## 1. Registry Sync Confirmation

| Item | Status | Sprint |
|---|---|---|
| CR-146 | IMPLEMENTED | pos_5_1 |
| BUG-325 | IMPLEMENTED | pos_5_0 |
| BUG-326 | IMPLEMENTED | pos_5_0 |

Registry synced: YES ✅
EXIT GATE: 5/5 PASS ✅

---

## 2. Test Cases

### BUG-325 — Variation Stock tab: `available` badge

| # | Test | Steps | Expected |
|---|---|---|---|
| T1 | Badge renders for Inactive values | Login → Aggregator Setup → Variation Stock tab → expand "echhi spcl" | Each variation value chip shows red "Inactive" badge between label and En button |
| T2 | Active badge is green | After clicking "En" on a value → wait for reload | That value's badge turns green "Active" |
| T3 | En button still present | Same view | Green "En" button visible after badge |
| T4 | Dis button still present | Same view | Red "Dis" button visible after En |
| T5 | Layout intact | Same view | Chips wrap normally, no overflow |

---

### BUG-326 — `is_packaged_good` + `swiggy_packing_chrg`

| # | Test | Steps | Expected |
|---|---|---|---|
| T6 | Transform reads `is_packaged_good` | Open aggregator food in ProductForm (or BulkEditor) → check "Packaged Item" toggle | Toggle reflects actual API value (not always false) |
| T7 | `swiggyPackingChrg` toggle visible | Edit any Aggregator food → Platform Sync section | New "Swiggy Packing Charge" toggle present |
| T8 | Toggle ON → payload | Edit aggregator food → toggle "Swiggy Packing Charge" ON → Save → check Network | Request body contains `swiggy_packing_chrg: "YES"` + `is_packaged_good: 1` (if Packaged also ON) |
| T9 | Toggle OFF → payload | Same with both OFF | `swiggy_packing_chrg: "NO"` + `is_packaged_good: 0` |
| T10 | Normal food unaffected | Edit Normal food → Save → Network | No `is_packaged_good` or `swiggy_packing_chrg` in payload; `packed_food` present |
| T11 | BulkEditor Aggregator — column visible | Menu Mgmt → Aggregator → Bulk Edit | "Swiggy Pack Chrg" column visible in Editing bar |
| T12 | BulkEditor dirty detection | Toggle Swiggy Pack Chrg in BulkEditor → | Row turns highlighted/dirty |
| T13 | QuickEdit aggregator food | ProductCard quick-edit on aggregator food | "Swiggy Pack Chrg" select shown |
| T14 | QuickEdit normal food | ProductCard quick-edit on normal food | No "Swiggy Pack Chrg" select |

---

### CR-146 — Client/Branch Selector Dropdown

| # | Test | Steps | Expected |
|---|---|---|---|
| T15 | Dropdown appears | Menu Mgmt → Aggregator mode | Second `<select>` appears after menu type selector showing "All / Main Branch / mallu goan" |
| T16 | Not visible in Normal mode | Menu Mgmt → Normal mode | No client dropdown |
| T17 | All → all foods | Select "All" | All 7 aggregator foods shown |
| T18 | Main Branch filter | Select "Main Branch" | Only 5 foods with `clientId=0` shown (poi, poi_poi_A, poison, 69 special, _GOAN_TEST_MAIN_) |
| T19 | Brand filter | Select "mallu goan" | Only 2 foods with `clientId=109` shown (_GOAN_TEST_CLIENT_, test_A) |
| T20 | Category counts match | Select "Main Branch" | Category sidebar item counts reflect filtered foods only |
| T21 | BulkEditor filtered | Select "Main Branch" → Bulk Edit | BulkEditor shows only 5 Main Branch foods |
| T22 | Reset on type change | Aggregator → select Main Branch → switch to Normal → back to Aggregator | Shows "All" again |

---

## 3. Regression Tests

| # | Test | Reason |
|---|---|---|
| R1 | Normal food add/edit still saves `packed_food` | Verify BUG-326 didn't break normal food path |
| R2 | Aggregator BulkEditor dirty count shows 0 on load | BUG-323 regression check |
| R3 | Aggregator food swiggy/zomato/clientId dirty tracking | BUG-324 regression check |
| R4 | Variation Stock tab En/Dis toggles still work | BUG-325 additive — no toggle regression |

---

## 5. Files Changed

| File | Item |
|---|---|
| `VariationStockTab.jsx` | BUG-325 |
| `menuManagementTransform.js` | BUG-326 |
| `BulkEditor.jsx` | BUG-326 |
| `ProductForm.jsx` | BUG-326 |
| `ProductCard.jsx` | BUG-326 |
| `MenuManagementPanel.jsx` | CR-146 |
