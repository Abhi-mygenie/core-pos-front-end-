# INVESTIGATION REPORT — BE Reply Cross-Reference (CR-363, CR-364, CR-366, CR-357)
**Date:** 2026-09-14 · **Role:** INVESTIGATION · **Steps used:** 10/10 · **Confidence:** HIGH

---

## 1. Summary

Two BE replies received 2026-09-14:
- `sep_14_364_be_reply.md` → CR-364 data path enrichment on `get-single-order-new`
- `sep_14_be_reply.md` → CR-363 Night Audit + CR-366 Revenue Dashboard (both endpoints shipped)

**Net result: 3 items unblocked, 1 OD answered, 1 print path still blocked.**

---

## 2. CR-363 Night Audit — FULLY UNBLOCKED ✅

**Endpoint shipped:** `GET aiosell/night-audit` — `AiosellController@nightAudit` + `PmsNightAuditService`

All Q-363-01..08 answered. Key answers:

| Q | Answer | Impact |
|---|---|---|
| Q-363-01 | Same `PmsRevenueMetricsService` as revenue-summary | R6 single source of truth ✅ |
| Q-363-02 | `reconciliation.settlement_room_share` from room payment ledger | Day Closure reconciliation ✅ |
| Q-363-03 | Combined checkout: proportional split `room_price / (room_price + folio_fnb)` | Tender correctness ✅ |
| Q-363-04 | Night-audit **splits** `room_balance` vs `fnb_balance` even if `balance_payment` blends | Resolves CR-357 OD-7 for folio view ✅ |
| Q-363-05 | No artificial history floor | Unlimited replay ✅ |
| Q-363-06 | No historical OOO snapshots — current board only | **LIMITATION** — FE must label "as of now" |
| Q-363-07 | `audit_trail` from `order_lifecycle_logs` — available ops only | Section I partial ⚠️ |
| Q-363-08 | Keys as in curl response shape | Key mapping confirmed ✅ |

**Gate status: Gate 1 → Gate 2 UNBLOCKED.** Next: FE curl-probe on preprod (R11) → joint Gate 2 Impact Analysis with CR-366.

---

## 3. CR-366 Revenue Dashboard — FULLY UNBLOCKED ✅

**Endpoint shipped:** `GET aiosell/revenue-summary` — `AiosellController@revenueSummary` + `PmsRevenueMetricsService`

All Q-366-01..13 answered. Key answers:

| Q | Answer | Impact |
|---|---|---|
| Q-366-01 | Per-night `sell_rate` when present; else even spread | ADR accuracy ✅ |
| Q-366-02 | Room collected from `restaurant_room_payments`; combined: proportional split | Collected correctness ✅ |
| Q-366-03 | Day-use = 1 night | Occupancy denominator ✅ |
| Q-366-04 | Both `occupancy_percent` (excl comp) + `occupancy_percent_physical` | Owner can show either ✅ |
| Q-366-05 | OTA: gross ex-tax on `booked_on`; no commission | No OTA net split ⚠️ |
| Q-366-06 | **Room balance = room ONLY (excl F&B)** | **Answers CR-357 OD-7** ✅ |
| Q-366-07 | No historical OOO — current only | **LIMITATION** ⚠️ |
| Q-366-08 | IST calendar midnight→midnight | Day boundary ✅ |
| Q-366-09 | BUG-385 Option A | no_show_count ✅ |
| Q-366-10 | No hard ceiling — live compute | No range ceiling ✅ |
| Q-366-11 | `room_revenue_collected_by_tender`; booking key = `booking_payment_type` | Key names confirmed ✅ |
| Q-366-12 | `reconciliation.settlement_room_share` | Day Closure reconciliation ✅ |
| Q-366-13 | Same `PmsRevenueMetricsService` — R6 confirmed | Single source of truth ✅ |

**Limitation:** Live compute (no cache). Long `group_by=day` spans cost CPU — FE should auto-switch to `week`/`month` for large ranges.

**Gate status: Gate 1 → Gate 2 UNBLOCKED.** Joint Gate 2 with CR-363.

---

## 4. CR-364 Guest Folio — DATA PATH UNBLOCKED ✅ / PRINT STILL BLOCKED ❌

**Endpoint enriched:** `POST get-single-order-new` — `PmsRoomStayPayloadService` (new)

| Q | Answer |
|---|---|
| Q-364P-02 | `room_info` enriched: full stay doc + `gst_tax` + `room_type` + optional `reservation` (channel, rateplan, actual CI/CO, guest email/address) |
| Q-364P-03 | FE derives `room_price ÷ nights` for v1 |
| Q-364P-04 | `associated_order_list` + `order_type_label`, `waiter_name`, `item_count`, `item_names[]` |
| Q-364P-05 | Use `room_info.gst_tax` (NOT `room_payment_summary.gst_tax`) |
| Q-364P-06 | No round-off on room folios |
| Q-364P-07 | `reservation.checked_in_at` / `checked_out_at` for actual times |
| Q-364P-14 | Payment types = `advance | interim | checkout | refund` |

**Fields NOT on schema (omit):** `discount_amount`/`discount_reason` on lodging, `guest_id_proof_no`, structured `booking_details.*`, `room_payment_summary.gst_tax`

**Print deferred (still blocked):** Q-364P-01, 08, 09, 10, 13, 15 — `order-temp-store` / `rtype=RM` template not in this ship.

**Gate status: Gate 2 UNBLOCKED for data path.** Print path blocked until next BE ship.

---

## 5. CR-357 OD-7 — ANSWERED ✅

Q-366-06 explicitly states: **"Outstanding room balance = room component only (excl F&B)"**

This resolves CR-357 OD-7: `remaining_room_balance` = room-only. FE must compute food balance separately. Combined outstanding at checkout = `room_balance + fnb_balance` (both returned by night-audit).

**For the `+ Pay` button interim fix:** OD-7 is now answered. However, the implementation path (how FE handles food payments mid-stay) still requires Gate 2 Impact Analysis and Gate 4 GO before the button can be re-enabled.

---

## 6. Limitations to Carry Into Gate 2 Planning

| Limitation | Affects | Handling |
|---|---|---|
| `room_status_close` = current board only | CR-363 Section G, CR-366 `rooms_ooo` history | Label "Status as of now" in UI; historical OOO = 0 or hidden |
| Live compute, no cache | CR-366 large ranges | FE auto-switch: ≤92d → `day`, ≤366d → `week`, >366d → `month` |
| OTA commission not stored | CR-366 `room_revenue_collected` for OTA | Collected = gross ex-tax on `booked_on`; document in UI |
| `associated_order_list` settled F&B only | CR-364 Section C | Open room tabs not shown mid-stay |
| `audit_trail` — available ops only | CR-363 Section I | Not all event types guaranteed |
| No per-night rates for some bookings | CR-363/366 ADR | FE shows "evenly spread" note |

---

## 7. Next Steps

| Action | Owner | Priority |
|---|---|---|
| FE curl-probe: `aiosell/revenue-summary` + `aiosell/night-audit` on preprod (R11) | PLANNING agent | P1 — before Gate 2 |
| FE curl-probe: `get-single-order-new` with enriched `room_info` on a live room order | PLANNING agent | P1 — before Gate 2 |
| Joint Gate 2 Impact Analysis: CR-363 + CR-366 | PLANNING role | P1 |
| Gate 2 Impact Analysis: CR-364 data path | PLANNING role | P1 |
| CR-357 Gate 2: now that OD-7 is answered, proceed to Impact Analysis | PLANNING role | P1 |
| CR-364 print: await next BE ship (Q-364P-10 `rtype=RM` template) | — | Deferred |

---

```
Root cause: All backend questions answered. Confidence: HIGH. Steps: 10/10.
FE fix: YES — CR-363 / CR-366 / CR-364 data path all ready for Gate 2 Impact Analysis.
          CR-357 OD-7 answered — Gate 2 now unblocked but needs owner Gate 4 GO before re-enabling + Pay button.
Backend ask: CR-364 print still deferred (Q-364P-01/08/09/10/13/15).
Planning skip eligible: NO — all items need full gate cycle (financial/report logic, R6).
Retroactive candidates: NONE.
Investigation report: memory/handover/INV_BE_REPLY_CR363_364_366_2026_09_14.md
```
