# SESSION HANDOVER — 2026-09-22 — CR-385 · Phase 2 IMPLEMENTED + QA partial (M1 New Booking · M3 Check-In)
**For the next agent.** Boot per `control/AGENT_PROMPT_ALPHA.md`. State: CR-385 **`GATE_5B_CLOSED (P2 QA)`** — owner D83 2026-09-22 closed the QA gate with acknowledged gaps OG-PMS-038…044 (`control/OPEN_GAPS_REGISTER.md`). Gate 6 owner smoke open (tracker `#m2s`). Owner decisions this session: D81 (OD-385-19 = flat, BUG-446 → FU-385-C, New Booking top-of-tab F1), D82 (Stay summary pre-save; bill from `charge{}` post-201; advance max = soft hint, never blocks Save). Owner rule: **tell the owner before every curl/QA login** (single-session account).

## Where we are
- Code: plan §2.1 amended E11–E18 and implemented; new `NewBookingForm.jsx`, `CheckInForm.jsx`, `tests/cr385/phase2.cr385.test.jsx` (+6 fixtures). jest cr385 **81/81**, `yarn build` exit 0, guards empty, hotspots byte-identical. EXIT GATE 5/5 done (registry, CR_REGISTRY, FILE_OWNERSHIP, markers, compile).
- QA: `test_reports/QA_REPORT_2026_09_22_CR385_P2_ROLE4.md` (round 1 smoke 13/13 · round 2 T1/T4/T6 PASS live, wire shapes verified, 0 defects). `iteration_17.json`, `iteration_18.json`. Evidence `evidence/CR-385/phase2_qa/`.
- Sandbox: QA stay (res 236, r1) **cashed out by the main agent** via legacy Departures; no settings toggled in round 2 (defaults as restored by owner). **API end-state read-back PENDING** — preprod `common-login` returned Laravel 404 from ~16:30 IST (backend-side). First task next session (after telling the owner): rerun `evidence/CR-385/probes_2026_09_22_p2_entry/run_before_after.py`-style read-back → LR in-house = r2/r3 only, r4/r5/r1 available, 3 settings at defaults; save `phase2_qa/t9_end_state.json`.
- Owed QA: T3 live early-check-in toggle cycle, T7 F1/C7 live counts (both unit-covered). T2/upgrade path **BLOCKED-BY-SANDBOX** (Executive sold out — r2/r3 are the owner's real stays). BQ-385-26 filed (MASTER v2.3).
- Next gate: owner Phase 2 smoke (§2.5 below, adapted) → "Phase 2 smoke OK" → Phase 2.5 only if findings → Phase 3 plan note (M4 Extend · M5 balances; entry condition BUG-433).

## Phase 2 owner smoke (adapted §2.5) — canonical copy in the HTML tracker `#m2s` (M2-S01…S13)
1. Front Desk (Beta) → **+ New Booking** (under the tiles; any open row collapses) → Suite, today → tomorrow, 1 adult, pick a rate cell (price shown = rate-table price, you cannot type one) → RIGHT shows a *Stay summary* (no total/GST until save) → advance ₹1,000 UPI + UTR → **Save booking** → confirmation shows total / SGST / CGST / advance / balance from the server → **Done** → Arrivals Today row with a **₹1,000 advance chip**.
2. Row → **Check In** → bill on the right = the same figures → choose **r4/r5/r1** (a Needs-cleaning room is allowed, amber note) → Collect now ₹500 Card + ref → **Confirm** → toast "Checked in — Room rX" → In-House row **Paid so far ₹1,500**; Rooms tile occupied.
3. **Upgrade (needs an Executive room free):** if you can free r2 or book Executive, create an Executive booking → Check In → tick **Show higher categories** → pick a Suite (r4/r5/r1) → Paid ₹1,500 + reason → Confirm → old Guest Folio shows "Room upgrade: <reason>" and the recomputed GST. Otherwise skip and note "upgrade untested on sandbox".
4. **Early check-in:** create a Suite booking for **tomorrow** → its Check In is greyed ("Arrives … — modify the booking dates…") → Channel Manager › Front Desk Rules → Allow early **ON** → Save → back → button live → turn it **OFF** → Save → cancel that booking.
5. Settle every stay from the old Departures page (Cash); cancel leftover test bookings; settings end at defaults. Say **"Phase 2 smoke OK"** (or list findings → Phase 2.5).
