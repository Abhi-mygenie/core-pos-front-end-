# CR-027 HANDOVER — 2026-06-12 (for next agent)

**CR:** CR-027 Unified Toast & Error Surfacing — `/app/memory/change_requests/CR_027_UNIFIED_TOAST_ERROR_SURFACING_CR.md`
**Audit/gap report:** `/app/memory/CR027_REVALIDATION_REPORT.md` (amendments A1–A10 in CR §4.8 are BINDING)
**Test reports:** `/app/test_reports/iteration_10.json` (Phase 1 smoke), `/app/test_reports/iteration_11.json` (Phase 2A mutative E2E)
**Credentials:** `/app/memory/test_credentials.md` — owner@cafe103.com (READ-ONLY) · manager@palmhouse.com (MUTATIVE ALLOWED, clean up after)

---

## 1. DONE (verified, do not redo)

### Phase 1 — interceptors (COMPLETE + E2E verified)
- `api/axios.js`: readableMessage chain is now: `validationLine (Laravel 422 errors-object) → errors[0].message → data.message → data.error (string) → friendlyTimeout (ECONNABORTED) → friendlyNetwork (ERR_NETWORK) → error.message → 'Something went wrong'`.
- `api/crmAxios.js`: same additive branches, keeps `data.detail` + `'CRM request failed'` terminal.
- **Gap fix (post-plan, preprod-verified):** preprod returns `{ error: "msg" }` on some 4xx (curl-verified: POST `/api/v1/vendoremployee/product/add-categories` duplicate name → 400 `{"error":"Category with this name already exists in this restaurant"}`). `data.error` branch added to BOTH interceptors.
- Tests: `src/__tests__/api/axios.interceptor.cr027.test.js` — **23/23 pass**. Run: `cd /app/frontend && CI=true yarn test --testPathPattern="api/axios" --watchAll=false`.
- E2E proof: wrong-password login → "Login Failed / Unauthorized."; duplicate category → backend verbatim message in red toast (screenshot-verified on palmhouse).

### Phase 2A — Menu slice (COMPLETE + E2E verified)
- 17 conversions in 5 files: `MenuManagementPanel.jsx` (fetchFoods converted + 3 silent bootstrap catches now toast), `menu/CategoryList.jsx` (4), `menu/ProductForm.jsx` (2), `menu/ProductList.jsx` (4), `menu/BulkEditor.jsx` (3 — **processOne L373 deliberately untouched, reserved for Phase 3**).
- Pattern everywhere: `toast({ title: <preserved>, description: err.readableMessage, variant: "destructive" })`, side-effects byte-identical.
- Mutative E2E passed: category add/edit/delete happy paths, bootstrap clean (zero toasts), destructive styling, cleanup verified.

### Owner rulings (LOCKED in CR §6.1 — implement as ruled)
- **OD-027-A5 = (c):** background auto-print failures get destructive toast, title `"Auto-print failed — Order #<id>"`, description `err.readableMessage`.
- **OD-027-A7a = (a):** `lookupAddresses` keeps its swallow; **OrderEntry L249 catch gets NO toast** (dead code for API errors — leave as-is).

---

## 2. NOT STARTED — Phase 2B (Cards/Modals/OrderEntry) + Phase 2C (Panels/Pages)

I completed file recon (read all catch blocks) but **wrote NO code** for 2B/2C. Line numbers below verified 2026-06-12.

### Phase 2B work list

**`components/order-entry/OrderEntry.jsx`** (the big one — 31 catch sites; line numbers approximate ±5):
- CONVERT (replace manual `err?.response?.data?.error || ...?.message || err?.message || 'X'` extraction with `err.readableMessage`; titles preserved): ~L956 "Order Update Failed", ~L1026 "Order Failed" (**also missing variant — add destructive**), ~L1054 "Order Failed" (**add variant**), ~L1079 "Transfer Failed", ~L1105 "Merge Failed", ~L1129 "Shift Failed", ~L1157 + ~L1183 "Cancel Failed", ~L1266/~L1372/~L1417/~L1919/~L1997 + ~L2085 "Payment Failed" (~L2085 **add variant**), ~L1699 "Failed to print bill".
- A5 RULING — ADD toasts to silent auto-print catches: ~L1308/1310 (QSR PlaceAndPay auto-print `.catch`), ~L1809 (`[AutoPrintBill] THREW`), ~L1942-51 (Prepaid auto-print), ~L2062 (`[AutoPrintCollectBill] THREW`). Title `"Auto-print failed — Order #<id>"` (order id is in scope at each site: `newOrderId` / `collectOrderId`), description `err.readableMessage`, destructive.
- LEAVE AS-IS: ~L231 loyalty `.catch` (commented intentional), **~L249 fetchDeliveryAddresses (A7a ruling)**, ~L282 handleAddAddress catch → CHECK: `addAddress` in customerService may re-throw (unlike lookupAddresses) — if it re-throws, Decision C applies, add toast; verify before editing. ~L2637 "Bill Split" catch (**A10 — keep success-style toast**). localStorage/parse catches.
- CAUTION: many catches live inside `.catch(err => {...})` chains attached to `placePromise` — convert only the message string; `apiFailed` flags, `setIsPlacingOrder(false)`, early-return flow MUST stay byte-identical.

**`components/cards/OrderCard.jsx`:**
- L194 KOT + L222 Bill print toasts: have title+variant but NO description → add `description: err.readableMessage` (printOrder rides main axios — readableMessage exists).
- L245 settle: add description `err.readableMessage` (currently title+variant only — recheck, audit said readable used; verify).
- L264 dispatch: `err?.readableMessage || err?.message || fallback` → tighten to `err.readableMessage` (optional, §4.1 says no fallback).
- L117/L131/L145 (Ready/Serve/Accept) silent catches: these wrap PARENT callbacks (`onMarkReady?.(order)` etc. from DashboardPage/ChannelColumn). **VERIFY whether the parent handler toasts before adding here** — if parent toasts, leave silent (avoid double); if not, Decision C → add toast. Same for TableCard L99.

**`components/cards/TableCard.jsx`:** L197 KOT / L226 Bill / L247 settle — same as OrderCard (add description). L130 dispatch — tighten. L99 silent (see above). L564 is a localStorage IIFE catch — leave.

**`components/modals/SplitBillModal.jsx`** L231-233: `msg = err?.response?.data?.message || err?.message || "Failed to split bill"` → `err.readableMessage`.

**`components/modals/RoomCheckInModal.jsx`** ~L632: 409 branch shows custom FE copy ("Room unavailable / just taken by another operator") — **action-specific UX copy for a race condition; recommend preserving the 409 branch as-is** (flag to owner if strict backend-verbatim is wanted); the non-409 else branch (read it — below L640) should use `err.readableMessage`. L249 image-processing catch is not API — leave.

**Already compliant (touch lightly or not at all):** `WhatsAppPaymentModal.jsx` L61-64 (readableMessage||message||fallback — optionally tighten), `AssignRiderModal.jsx` L42 (inline setError — valid inline pattern) + L70 (tighten optional), `credit/CreditClearanceModal.jsx` L90 (readableMessage||fallback — tighten optional), `order-entry/CustomerModal.jsx` (fully compliant), `order-entry/RePrintButton.jsx` L55-57 + L118-120 → add `description: err.readableMessage`.

**A3 — `components/reports/CollectBillPanelDrawer.jsx`:** L144 `setLoadError(err?.message || ...)` → `err.readableMessage` (inline surface, no toast). L192 delegates to `onCollectError?.(order, err)` — find the parent report pages that pass `onCollectError` and verify they toast `err.readableMessage`.

### Phase 2C work list
- `panels/CreditManagementPanel.jsx`: L69 silent customer-list fetch → ADD toast (Decision C); L205/L252/L312 ("Statement failed"/"Download failed"/"Export failed") → description `err.readableMessage`.
- `panels/SettlementPanel.jsx`: L61 hardcoded "Failed to load settlement report." → readableMessage; L73 waiter-list silent → ADD toast.
- `pages/LoadingPage.jsx`: L262 kitchen-stations hardcoded description → readableMessage (other 7 bootstrap catches already compliant).
- `pages/AllOrdersReportPage.jsx`: L365 `setError(err.message)` → `err.readableMessage` (error UI, NO toast); L232 `.catch(()=>[])` intentional — leave; L609/L680/L825 verify descriptions use readableMessage.
- `pages/RoomOrdersReportPage.jsx`: L429 verify description; L466 silent catch → read context, ADD toast if API failure.
- `pages/StatusConfigPage.jsx`: ONLY L552 (station-data refresh after save, silent) → ADD toast. The 10 localStorage catches L214-L322 are OUT of scope (A6). L330/L338 already destructive — verify description source (note: `stationService.fetchStationData` swallows and returns `{error: err.message}` — component reads `.error`, not readableMessage; acceptable, or update service per A7(b)).
- `pages/SettlementPage.jsx`, `pages/LoginPage.jsx`: already canonical — leave (LoginPage's `|| fallback` is acceptable defensive).
- A7(b) optional 2-liner: foodCourtService L86 + prepServeService L139 chunk workers → `_error: err.readableMessage || err.message`.
- **REMOVED from scope (A2):** Sidebar.jsx, TableManagementView.jsx, shared.jsx (zero catches; shared.jsx optimistic-success-toast issue is a SEPARATE task — not CR-027).

### Per-slice gating (CR §4.7.3)
`yarn build` clean → `CI=true yarn test --watchAll=false` (expect 26 PRE-EXISTING failures in ScanOrderPopOut / placeOrderPayload / rawField / barrelExports — ignore those, fail on NEW ones) → preprod smoke (mutative OK on palmhouse only) → owner signoff per slice.

---

## 3. Phase 3 (after 2B/2C) — BulkEditor row trail
Feasibility CONFIRMED: extend `_saveStatus` pattern with `_saveError: err.readableMessage` at BulkEditor L376; tooltip on the L625 AlertCircle (ui/tooltip.jsx or native title); drawer via existing ui/dialog|sheet|drawer; `[View errors]` button via `ToastAction` (exported from ui/toast.jsx, currently unused app-wide). Spec in CR §4.3 + OD-025-3 hybrid (tooltip ≤3 failures, drawer >3).

---

## 4. Environment / gotchas
- Frontend-only app vs EXTERNAL preprod API (`REACT_APP_API_BASE_URL=https://preprod.mygenie.online/`). Local backend is a stub; don't test against it.
- Hot reload on; only restart supervisor for .env/dep changes.
- Login can take 5–60s on preprod; poll for `/dashboard`.
- Login inputs: `input[placeholder="Email"]` / `input[placeholder="Password"]` (email input is NOT type=email).
- Menu sidebar testid: `sidebar-menu-management`. Category form: `add-category-btn`, `new-category-input`. Product status toggle testid `status-toggle-{productId}` EXISTS but is hover-revealed (`group-hover:opacity-100`).
- `TOAST_LIMIT = 1` (OD-025-2 parked) — concurrent failures show only the last toast; do not "fix" without owner.
- The bottom-right "Web N · POS N" chip is a connectivity badge, NOT a toast.
- Duplicate-category POST returns 400 WITHOUT creating data — safe repeatable error trigger for smoke tests.

---
## UPDATE 2026-06-12 (later session): Phases 2B + 2C SHIPPED
Section 2 of this handover is now DONE (see CR doc artifact #8 + /app/test_reports/iteration_12.json — 9/9 regression PASS).
Only remaining work: **Phase 3** (section 3 below). Also logged follow-up: DashboardPage Ready/Serve/Accept handlers swallow errors (cards catches are dead code) — out of CR-027 scope, needs owner decision as separate task.

---
## UPDATE 2026-06-12 (final): Phase 3 SHIPPED — CR-027 COMPLETE
Section 3 of this handover is DONE (CR doc artifact #9 + BulkEditor.cr027p3.test.jsx 4/4). Nothing remains on CR-027 except owner signoff. Open follow-ups outside CR-027: DashboardPage Ready/Serve/Accept silent handlers; shared.jsx optimistic success toasts; OD-025-2 TOAST_LIMIT.
