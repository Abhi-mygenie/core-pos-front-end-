# QA Handover — BUG-323 + BUG-324
**Date:** 2026-08-15
**For:** QA agent spot-check
**Environment:** `https://core-pos-deploy-9.preview.emergentagent.com`
**Credentials:** owner@thegoankitchen.com / *** (RID 69 — has Aggregator with client_id=109)

---

## 1. Files Changed
| File | Change |
|------|--------|
| `components/panels/menu/BulkEditor.jsx` | L324 + L372 — isDirty categoryId fix + isRowDirty deps fix |

---

## 2. Test Cases

### TC-1: BUG-323 — Aggregator foods no longer false-dirty on load
**Steps:**
1. Login → Menu Management
2. Switch to Aggregator menu type
3. Open Bulk Edit
4. Observe the "Save N Changes" button / dirty row count **before making any edits**

**Expected:** 0 dirty rows on load. "Save Changes" button disabled or shows 0.
**Previously:** 37+ rows immediately dirty before any edit.

---

### TC-2: BUG-324 — Aggregator column dirty check works after fix
**Steps:**
1. In Bulk Edit (Aggregator mode), edit the Swiggy or Zomato value on one food
2. Observe that the row is marked dirty (amber/indicator)
3. Check that "Save N Changes" count increases by 1

**Expected:** Row is dirty after Swiggy/Zomato/clientId edit.
**Previously:** Aggregator-specific column edits were silently not detected as dirty.

---

### TC-3: Regression — Normal mode unaffected
**Steps:**
1. Switch to Normal menu type → Open Bulk Edit
2. Observe 0 dirty rows on load (no regressions from BUG-323 fix)
3. Edit a price field → row becomes dirty ✅
4. Edit category → row becomes dirty ✅ (also validates BUG-323 fix for normal mode)

**Expected:** Normal mode behaves exactly as before.

---

### TC-4: Regression — isDirty categoryId still works for category edit
**Steps:**
1. In Bulk Edit (any mode), change the Category dropdown on one food
2. Row should become dirty

**Expected:** Category change → row dirty.
**Risk of regression:** BUG-323 fix uses `??` coercion instead of `||`. Must confirm non-null categoryId still dirty-detects.

---

## 3. Regression Scope
Both fixes are in BulkEditor.jsx only. BulkEditor.jsx is a HIGH history file but not in R5 hotspot list.
Regression tests required: TC-3 + TC-4 (normal mode + category dirty detection).

---

## 4. Registry Sync Confirmation
- Registry synced: YES
- Items: BUG-323, BUG-324
- Sprint: pos_5_1
- EXIT GATE: ALL 5 PASSED

Fix report: `/app/memory/handover/BUG-323_324_BUG_FIX_REPORT_2026_08_15.md`
