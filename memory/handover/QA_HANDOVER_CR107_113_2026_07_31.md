# QA Handover — Aggregator Features: CR-107, CR-109, CR-110, CR-111, CR-112, CR-113

**Document:** QA_HANDOVER_CR107_113_2026_07_31.md
**Items:** CR-107 · CR-109 · CR-110 · CR-111 · CR-112 · CR-113
**Implementation Date:** 2026-07-23-27

---

## 1. Registry Sync Confirmation

```
Items: CR-107/109/110/111/112/113 — IMPLEMENTED gate 5a
Code markers:
  ✅ CR-109: profileTransform.js L359-367 — prepTimeBonusConfig + prepTimeCountMethod + auto-accept toggle
             aggregatorPrepTime.js L1 — dynamic prep time computation util
  ✅ CR-110: AggregatorOrderPopOut.jsx L4+103+378+381+393 — GENIE_LOGO_URL, isOwn badge
             OrderCard.jsx L5+335 — MyGenie badge for own delivery
  ✅ CR-111/112/113: AggregatorOrderPopOut.jsx L96 (currencySymbol) — item format + price + customer section
```

---

## 2. Code Checks

| Check | Command | Expected |
|-------|---------|---------|
| C1-CR109 | `grep -c 'prepTimeBonusConfig\|prepTimeCountMethod' /app/frontend/src/api/transforms/profileTransform.js` | 2 |
| C2-CR109 | `grep -c 'CR-109' /app/frontend/src/api/transforms/profileTransform.js` | ≥2 |
| C3-CR109 | `grep -c 'aggregatorPrepTime' /app/frontend/src/utils/aggregatorPrepTime.js` | ≥1 |
| C4-CR110 | `grep -c 'GENIE_LOGO_URL' /app/frontend/src/components/dashboard/AggregatorOrderPopOut.jsx` | 1 |
| C5-CR110 | `grep -c 'isOwn' /app/frontend/src/components/dashboard/AggregatorOrderPopOut.jsx` | ≥3 |
| C6-CR110 | `grep -c 'CR-110' /app/frontend/src/components/cards/OrderCard.jsx` | 1 |

---

## 3. Test Cases

### CR-109 — Dynamic Prep Time + Auto-Accept

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-1 | Restaurant Settings → Aggregator settings → Auto-accept toggle | Toggle visible; changes saved |
| TC-2 | Prep time bracket config in settings | Bracket config visible (e.g. 0-5 items = 15 mins, 6-10 = 20 mins) |
| TC-3 | When auto-accept enabled: new aggregator order arrives | Order auto-accepted with computed prep time; no manual popup required |
| TC-4 | Prep time sent with order acceptance | Network: accept API includes `prep_time_mins: <computed>` |

### CR-110 — MyGenie Badge for Own Delivery

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-5 | Dashboard: own delivery order card | Shows MyGenie logo/badge (not Swiggy S or Zomato Z) |
| TC-6 | Dashboard: Swiggy order | Shows orange "S" badge |
| TC-7 | Dashboard: Zomato order | Shows red "Z" badge |

### CR-111 — OrderCard Item Format: ● Qty× Name

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-8 | Click aggregator order in delivery column → expand popup/card | Items listed as "● 2× Paneer Tikka" pattern (bullet + qty× + name) |

### CR-112 — OrderCard Item Price Display

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-9 | Same aggregator order popup | Each item line shows price: "₹120" per item or per line |

### CR-113 — OrderCard Customer + Phone

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-10 | Aggregator order popup → customer section | Customer name + phone number visible in order popup |

---

## 4. Regression Tests

| R# | What | Why |
|----|------|-----|
| R1 | Manual accept still works when auto-accept is OFF | CR-109 auto-accept only fires when toggle is enabled |
| R2 | AggregatorOrderPopOut popup still opens for fOS=0/7 | CR-110/111/112/113 add elements; popup structure unchanged |

---

## 5. Credentials + Environment

| Field | Value |
|---|---|
| Login | `owner@18march.com` / `Qplazm@10` |
| Route | Dashboard → Delivery column; Settings → Aggregator/Platform |
| Note | CR-109 TC-3 requires a live new aggregator order (fOS=0/7). CR-110 requires own-delivery order. Code checks C1–C6 are sufficient for gate pass if live orders not available. |
