# QA Handover — BUG-270 + BUG-272: Order Customer Fields + Partial Payment

**Document:** QA_HANDOVER_BUG270_272_2026_07_31.md
**Items:** BUG-270 · BUG-272
**Implementation Date:** 2026-07-27

---

## 1. Registry Sync Confirmation

```
Items: BUG-270, BUG-272 — IMPLEMENTED gate 5a
Code markers:
  ✅ BUG-270: orderTransform.js L1132-1133 — cust_mobile + cust_membership_id in update-place-order
  ✅ BUG-272: reportTransform.js L842+1099 · AllOrdersReportPage.jsx L411 · OrderLedgerMockup.jsx L354+409 · orderLedgerService.js L88
```

---

## 2. Code Checks

| Check | Command | Expected |
|-------|---------|---------|
| C1-BUG270 | `grep -c 'BUG-270' /app/frontend/src/api/transforms/orderTransform.js` | 2 |
| C2-BUG270 | `grep -c 'cust_mobile.*BUG-270\|BUG-270.*cust_mobile' /app/frontend/src/api/transforms/orderTransform.js` | 1 |
| C3-BUG272 | `grep -c 'BUG-272' /app/frontend/src/api/transforms/reportTransform.js` | ≥2 |
| C4-BUG272 | `grep -c 'BUG-272' /app/frontend/src/pages/AllOrdersReportPage.jsx` | 1 |
| C5-BUG272 | `grep -c 'BUG-272' /app/frontend/src/pages/reports-module/OrderLedgerMockup.jsx` | ≥2 |

---

## 3. BUG-270 Test Cases — Customer Fields in Update Order

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-1 | Open an existing dine-in order → change CRM customer → Update Order | API payload includes `cust_mobile` and `cust_membership_id` (check Network tab) |
| TC-2 | Network: POST update-place-order body | `cust_mobile: "<phone>"` and `cust_membership_id: "<id>"` present (not empty/missing) |

---

## 4. BUG-272 Test Cases — Partial Payment Breakdown

| TC# | Steps | Expected |
|-----|-------|---------|
| TC-3 | Reports → All Orders → filter by payment method "Cash" | Shows orders where EITHER full payment is cash OR partial cash leg exists |
| TC-4 | Reports → Order Ledger → filter by "Cash" | Same — partial-cash orders included in Cash filter results |
| TC-5 | Order Ledger → expand order with partial payment (e.g. Cash + Card) | Payment breakdown shows two legs: cash amount + card amount separately |
| TC-6 | Payment badge color on Order Ledger row | "Cash" badge = green; "Card" badge = blue; "Partial" badge = distinct color |

---

## 5. Credentials + Environment

| Field | Value |
|---|---|
| Login | `owner@18march.com` / `Qplazm@10` |
| Routes | Reports → All Orders; Reports → Order Ledger |
| Note | BUG-270 requires Network tab inspection. BUG-272 requires at least one partial-payment order in the dataset. Code checks C1–C5 are sufficient if no partial orders available. |
