# CR-385 · Phase 4 QA HANDOVER (Gate 5B P4) — Role 4, live sandbox, MUTATING — 2026-09-22
Authority: owner D86 condition 1 + "Phase 4 GO" (Q7 a: sessions A/B/C). Plan note: `plans/CR-385_P4_PLAN_NOTE_2026_09_22.md` §4. Any finding → Phase 4.5 intake tagged with origin phase (P2/P3/P4). A main-agent run never closes the gate.

## 0. Environment / rules (hard)
- Preview app: `REACT_APP_BACKEND_URL` from `frontend/.env`. **LoginPage is mounted on `/`** (no `/login`); after login wait for the `/loading` redirect (≤15 s). Credentials: `memory/test_credentials.md` → **QA_TGK (QA-only staff login, owner decision 2026-09-22 — use ONLY this one for testing_agent sessions; OWNER_TGK is the owner's)** — never paste values into reports.
- QA_TGK is also single-session per account: run each session as ONE browser-automation call (login at the top, `finally` cleanup) — it.21/it.22 lesson (OG-PMS-045).
- Channel Manager tabs carry `data-testid="channel-manager-tab-<i>"` (`ChannelManagerPage.jsx` L198) → **Front Desk Rules = `channel-manager-tab-4`**; wait for `frontdesk-rules-card` then `toggle-allow-early-checkin`. Never click by text.
- Arrivals opens on the first non-empty chip (Late) by design (D70): after saving a booking click the chip matching its check-in date (`Today` / `Tomorrow`) before asserting the row (OG-PMS-046 declined — not a defect).
- Front Desk (Beta): `/pms/front-desk-v2`. Tabs `fd-tab-arrivals|departures|inhouse|rooms`; refresh `fd-refresh-btn`; header `+ New Booking` = `fd-new-booking-btn`.
- **"Today" = the server business date** in the header (`meta.business_date`, currently `2026-09-23`), never the machine clock.
- Rooms: QA may use **r4 (8525) / r5 (8527) / r1 (8528, currently HK — allowed with amber note)**. **NEVER touch r2 (8526, stay 174 "bkol") or r3 (8524, stay 155 "blpi")** — the owner's real stays. Never confirm a No-Show. Two late pending arrivals exist that are not ours — leave them.
- Settings must END at defaults: `allow_early_checkin=false` (`toggle-allow-early-checkin` OFF), `extend_rate_mode=calendar` (`radio-extend-rate-mode-calendar`), `auto_print_checkin_receipt=false`. Channel Manager › 5th tab "Front Desk Rules" (`frontdesk-rules-card`, `frontdesk-rules-save-btn`).
- Each session ends with its own cleanup: every stay QA created is settled (through the **new Bill panel** where that is the object under test, else legacy `/pms/departures` → `dep-tab-*` → `dep-checkout-btn-<orderId>` → `payment-cash-btn` → `complete-payment-btn`); leftover QA bookings cancelled from the Arrivals row (`fd-row-<id>-cancel-btn` → inline dialog Confirm); In-House = r2/r3 only.
- Both viewports where UI is asserted: **1920×800 and 1366×768**. Zero console errors (ignore pre-existing socket/firebase lines). X-10: no duplicate `data-testid` with the Bill expansion open.
- Entry baseline (read-back 18:40 UTC, `evidence/CR-385/phase4_qa/t0_entry_readback.json`): in-house = r3 (155) + r2 (174); r4 booked (late arrival, not ours), r5 available, r1 hk; settings at defaults. **Executive (r2) occupied → OG-PMS-038 upgrade leg NOT available unless the owner frees r2 → record "not available — OG-PMS-038 open, 5th deferral".**

## 1. Testids — Front Desk (Beta)
- **Chips (role="tab", NOT button):** `fd-chip-arrivals-late|today|tomorrow|upcoming` · `fd-chip-departures-overdue|today|tomorrow|upcoming` · `fd-chip-inhouse-all|arrived|leaving|stayover`. Always click by testid (it.24 lost the session using role=button/name).
- Rows: `fd-row-<id>` · actions `fd-row-<id>-checkin-btn | -modify-btn | -cancel-btn | -noshow-btn | -bill-btn | -hk-btn | -extend-btn` (expansion copies carry `-exp-`) · balance cell `fd-row-<id>-balance` (title = raw + round-off when paise) · badges `fd-row-<id>-badge`, `fd-row-<id>-badge-advance` (BUG-447: PAH + Advance both).
- New Booking: `booking-form`, `booking-guest-name`, `booking-guest-phone`, `booking-checkin`, `booking-checkout`, `booking-adults`, `booking-type-<type>`, `booking-cell-<type>-<plan>`, `booking-advance-toggle`, `booking-advance-amount`, `booking-pay-cash|card|upi`, `booking-pay-ref`, `booking-save-btn`, `booking-save-checkin-btn`, `booking-confirmation`, `booking-bill-total|sgst|cgst|advance|balance`, `booking-error`, `booking-close-btn`. Pre-save right pane = `booking-summary` (no client total, D82).
- Check-In: `checkin-form`, `checkin-room-select` (HK rooms carry `checkin-room-hk-badge` + `checkin-room-hk-warning`), `checkin-upgrade-toggle` ("Show higher categories"), `checkin-upgrade-paid|comp`, `checkin-upgrade-amount`, `checkin-upgrade-reason`, `checkin-collect-amount`, `checkin-pay-cash|card|upi`, `checkin-pay-ref`, `checkin-confirm-btn`, `checkin-early-tooltip` (title on the disabled Check In when the rule is OFF and checkin > today), `checkin-bill-total|sgst|cgst|paid|balance|upgrade`, `checkin-server-error`.
- Extend: `extend-form`, `extend-checkout`, `extend-reason`, `extend-collect-toggle`, `extend-collect-amount`, `extend-pay-cash|card|upi`, `extend-confirm-btn`, `extend-result`, `nights-lines-<date>-source` chips (inside `extend-result`), `extend-bill-total|sgst|cgst|paid|balance`, `extend-conflict`, `extend-move-room-<tableId>`, `extend-done-btn`, `extend-server-error`.
- **Bill (M6, new):** `bill-panel-<rowId>`, `bill-left`, `bill-guest`, `bill-room-toggle`, `bill-room-total`, `bill-nights-<date>-source`, `bill-room-booking`, `bill-room-upgrade` (only when upgrade_amount > 0), `bill-room-discount-btn` (disabled, title "needs BQ-385-07"), **`bill-room-sgst`, `bill-room-cgst`** (two separate lines), `bill-room-paid`, `bill-room-balance`, `bill-orders-heading`, `bill-orders-<i>` / `bill-orders-empty`, `bill-transferred-heading`, `bill-transferred-<i>` / `bill-transferred-empty`, `bill-right` (class `frontdesk-bill`, 440×560), `bill-print-folio-btn` (disabled), `bill-close-btn`, `bill-error` + `bill-retry-btn`, `bill-pay-error`. Inside the RIGHT panel (the unmodified POS panel): `payment-cash-btn`, `payment-card-btn`, `payment-upi-btn`, `payment-split-btn`, Credit/TAB tile, `complete-payment-btn`; the hidden rows are `checkout-room-booking-toggle`, `checkout-transferred-toggle`, `checkout-room-service-toggle` (must be `display:none` inside `.frontdesk-bill`, visible on `/dashboard` dine-in checkout).
- Rooms tab: tile → `fd-room-detail-<tableId>`, actions `fd-room-action-bill-<tableId>` / `-extend-` / `-checkin-` / `-clean-` / `-hk-`.
- Legacy Departures `/pms/departures`: `dep-tab-<key>`, `dep-checkout-btn-<orderId>`, drawer `pms-checkout-drawer` (must be unchanged).

## 2. Sessions (owner D86-1; report each as `test_reports/iteration_21|22|23.json` + one `QA_REPORT_<date>_CR385_P4_ROLE4.md`)
### Session A — Phase 2 legs (OG-PMS-038/039/040 + BUG-447) + settle through the NEW Bill panel
A1 **T7 / F1 + C7 (OG-PMS-040)**: Arrivals → expand any row → `fd-new-booking-btn` → form opens under the tiles and the row collapses; click a row → form closes (one thing open). With request logging on, change `booking-checkout` 3× rapidly → **≤ 1 rates + availability request pair after ≥500 ms** (not three); `local-reservations` is NOT re-requested by date toggling. Record the counts.
A2 **T1 booking**: guest "QA P4 A", today → tomorrow, 1 adult, Suite (any plan), advance **₹1,000 UPI** + ref → `booking-save-btn` → 201 → `booking-confirmation` figures; **wire shape**: body has `guest, checkin, checkout, adults, children, rooms[{room_code, rateplan_code, rooms_count}], advance{amount, method, reference}` and **no** `rate_per_night / room_price / amount_after_tax / order_amount`. Row appears with `fd-row-<id>-badge` PAH **and** `fd-row-<id>-badge-advance` "₹1,000" (**BUG-447 live**).
A3 **T3 early check-in toggle (OG-PMS-039)**: create a second booking for **tomorrow → day after** ("QA P4 A2"); its `fd-row-<id>-checkin-btn` is disabled with the "Arrives …" title. Channel Manager › Front Desk Rules → tick `toggle-allow-early-checkin` → Save → back on Front Desk (refresh) the button is enabled → **untick → Save → button disabled again. Rule ENDS OFF** (verify via settings-list or the tab after reload). Cancel "QA P4 A2".
A4 **T4 check-in**: "QA P4 A" → Check In → room **r5** (or r1 with HK badge/amber note) → collect-now **₹500 Card** + ref → `checkin-confirm-btn` → toast "Checked in — Room r5"; **wire shape**: multipart FormData with `room_price=0`, `order_amount=0`, `gst_tax=0`, `advance_payment=500`, `payment_method=card`, `aiosell_reservation_id`; no `rate_per_night`. **Upgrade leg**: only if an Executive room is free (it is NOT at entry) → otherwise record "not available — OG-PMS-038 open, 5th deferral". In-House row shows Paid so far ₹1,500.
A5 **Settle through the NEW Bill**: In-House row → `fd-row-<id>-bill-btn` → `bill-panel-<id>` → LEFT `bill-room-sgst` and `bill-room-cgst` present as two lines, `bill-room-paid` ₹1,500, `bill-room-balance` == row `fd-row-<id>-balance` == panel room balance → Credit/TAB → `complete-payment-btn` → toast "Checked out · Room r5" → row gone → Rooms tile r5 = Needs cleaning. **Wire**: `order-bill-payment` body is the POS body + `room_gst_tax`; no `booking_charge / balance_due / rate_per_night` keys.
A6 1366×768 + 1920×800: Bill RIGHT box 440×560, `complete-payment-btn` inside the viewport without page scroll after expand; X-10 with the Bill open; 0 console errors. Cleanup: rules OFF + Rate table; no QA bookings left.

### Session B — Phase 3 legs + M6 checkout shapes (BUG-433 paise, already_paid, Split) + **BUG-448 re-test (TAB)**
BUG-448 FIXED (P4.5): in the Bill panel select Credit/TAB → the `tab-customer-section` block is hidden (class `fd-bill-tab-prefilled` on `bill-right`) and `complete-payment-btn` is ENABLED at once → POST `order-bill-payment` with `payment_mode` TAB/credit → 200.
B1 Book + check in "QA P4 B" on **r1** (HK allowed) today → tomorrow, no advance. POS: place a **room-service order to r1 with an add-on that yields paise** (if no such item, record "integer case only"). In-House `fd-row-<id>-balance` == POS checkout drawer grand total **to the rupee**; title shows raw + round-off when paise (BUG-433).
B2 Extend +1 night, `extend-collect-toggle` **₹500 Cash** → `extend-result` per-night chips (`rate table`/`held rate`), `extend-bill-sgst` == `extend-bill-cgst`, balance == LR after refetch; `room-extend-stay` body has no `new_room_price / amount_after_tax / rate_per_night`. Shorten back one night (D15 figures, not an average).
B3 **Bill** on that row: LEFT shape (ii) — `bill-orders-<i>` lists the room-service line(s); (iii) TRANSFERRED if you can post a dine-in bill to r1 (else `bill-transferred-empty`, record). `bill-room-sgst`/`bill-room-cgst` two lines. Panel room balance == `bill-room-balance` == row balance.
B4 **TAB full** → 200 → LR line `departed`, `charge.balance_due 0`, ledger row = amount sent, room r1 → `hk`, row gone. **Second submit**: re-open the same order (legacy `/pms/departures` drawer if listed, or POST the same body once via API) → **200 `status:"already_paid"`**, no 500, no second ledger row; in the Bill panel this is toast "Already checked out".
B5 Second stay "QA P4 B2" on **r5** → Bill → `payment-split-btn` → Cash + Card rows (Card needs Txn ID) → Checkout disabled while rows < total → exact total → checkout → two ledger rows. Cleanup: all QA stays departed; rules at defaults.

### Session C — regression + exit read-back (OG-PMS-041)
C1 POS `/dashboard`: dine-in bill, split bill, TAB, coupon flow — unchanged (R-02); the three `checkout-*-toggle` rows are **visible** there.
C2 Front Desk Bill: the same three toggles are `display:none` inside `.frontdesk-bill` (computed style).
C3 Legacy `/pms/departures` drawer `pms-checkout-drawer` unchanged (screenshot vs before); legacy `/pms/new-booking`, `/pms/check-in` load.
C4 Both viewports: zero console errors on `/pms/front-desk-v2` all four tabs.
C5 **Exit read-back (API, one login)**: LR in-house = r2/r3 only; r4/r5/r1 not in-house (`room-status-board`); `settings-list basic.allow_early_checkin=false, extend_rate_mode=calendar, auto_print_checkin_receipt=false` → save `evidence/CR-385/phase4_qa/t9_readback.json`.

## 3. Report format (Role 4)
Per case: PASS/FAIL, evidence (request body keys, response status, screenshot path), viewport. Explicit lines for **OG-PMS-038 / 039 / 040 / 041**: CLOSED (evidence) or "still open — reason". **BUG-418**: two GST lines seen live (Beta) yes/no. **BUG-433**: paise case exercised yes / "integer case only". **BUG-447**: PAH + Advance both seen yes/no. Findings → Phase 4.5 with origin phase.

## 4. Registry Sync Confirmation (Precondition Check, Role 4)
- Registry synced: **YES** — `registry.json` CR-385 `status` = `GATE_5A_IMPLEMENTED (P4) 2026-09-22 …`, `files_p4` listed, `status_history` appended; `BUG_TRACKER.md` BUG-418 → FIXED-BY-CONSTRUCTION (Beta), QA pending; `OPEN_GAPS_REGISTER.md` 038–041 + 045/046 current; plan note §10 implementation record.
- EXIT GATE: **5/5 PASS** — (1) jest cr385 116/116 (97 → 116) · (2) `yarn build` exit 0, 0 warnings in P4 files · (3) X-01/X-06 grep guards: no new hits; hotspots byte-identical (`git status` clean on `CollectPaymentPanel.jsx`, `PmsCheckoutDrawer.jsx`, `pmsService.js`, `orderTransform.js`, `folioTransform.js`, `roundOffUtils.js`, legacy pages) · (4) `// CR-385 M6` markers on every new/edited line, `BUG-418` marker at the two GST lines · (5) registry/BUG_TRACKER/OPEN_GAPS/PRD synced (this section).
- Files changed (coverage check): `FolioCheckoutPanel.jsx` (NEW), `frontDeskService.js`, `DeparturesPanel.jsx`, `InHousePanel.jsx`, `RoomDetail.jsx`, `RoomsPanel.jsx`, `FrontDeskWorkstationPage.jsx`, `frontdesk.css` — every one is exercised by A5 (Bill via row + Rooms tile) and the CSS by A5/C2.
- Sessions so far: `iteration_21.json` (aborted, 0 mutations), `iteration_22.json` (partial: A0/A1 PASS → OG-PMS-040 CLOSED; A2 wire PASS; A3 tooling FAIL; A4–A6 not run; cleanup PASS). Next report: `iteration_23.json` (Session A re-run with QA_TGK), then 24 (B), 25 (C); merged `QA_REPORT_<date>_CR385_P4_ROLE4.md`.

- Sessions so far (cont.): `iteration_24.json` Session B BLOCKED at B1 by the QA tool's chip selector (role=button vs role=tab) — code testids exist (`fd-chip-arrivals-today`); 1 residual pending booking 'QA P4 B' (today) must be cancelled first in the re-run.
