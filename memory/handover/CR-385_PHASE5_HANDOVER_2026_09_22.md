# CR-385 — PHASE 5 HANDOVER FOR A NEW AGENT — 2026-09-22
Self-contained. Read this, then `control/AGENT_PROMPT_ALPHA.md`, then the files linked here. Do not ask the owner what is already written below. **Status at time of writing: Gate 6 combined owner smoke IN PROGRESS (owner runs it; facilitator records verdicts). Phase 5 has NOT started — it needs the owner's "Phase 5 GO" after the smoke closes.**

---
## 1. How to work
- **Roles / rules:** `control/AGENT_PROMPT_ALPHA.md` — boot sequence per role (Planner, Implementation, Bug Fix, QA Role 4, Smoke Facilitator Role 8, Intake). Every role ends with a SESSION_HANDOVER with the mandatory self-assessment (registry synced? scope drift?). Fast Lane only when the owner says so and the change is ≤10 lines / 1 file / no hotspot.
- **Branch:** `origin/21implement` **ONLY**. Push via the platform **"Save to GitHub"** button (the agent cannot push). The owner's tracker validates every push (code read, test counts, hotspot byte-identity, markers). Never push to any other branch; never force.
- **Gate words (owner, verbatim, never inferred):** "Phase N GO" (start coding), "Phase N.5 GO" (bug batch), "GATE 5B … closed" (QA gate), "Phase N smoke OK" / **"Phase 2 + 3 + 4 smoke OK"** (Gate 6 for the combined block), "Phase 5 GO", final "CR-385 closed". A summary sentence never replaces row-level smoke verdicts.
- **§0-bis N.5 rule** (`plans/CR-385_IMPLEMENTATION_PLAN_PHASED.md` L31): every bug found in phase N is fixed + re-QA'd in N.5 before N+1, unless the intake proves a hard dependency on a later phase → `DEFERRED-TO-P<k>` = entry condition of P<k>, owner-approved. Loop: plan note → owner GO → Bug Fix → QA re-test → one owner smoke.
- **Hotspots (must stay byte-identical; `git log -1 -- <file>` must still show `642ccb8` or older):** `components/order-entry/CollectPaymentPanel.jsx`, `api/transforms/orderTransform.js`, `api/services/pmsService.js`, `components/pms/PmsCheckoutDrawer.jsx`, legacy `pages/pms/NewBookingPage.jsx` / `CheckInPage.jsx` / `GuestFolioPage.jsx`, `OrderEntry.jsx`, `DashboardPage.jsx`, `LoadingPage.jsx`, shared round-off helper. Front Desk **copies** (`RoomTile.jsx`, `CheckInForm.jsx`) follow the mirror rule R9 until FU-385-C.
- **Frozen money rules:** **D50 / G-02** — the FE never computes or submits server-owned money (no `rate_per_night`, `room_price`, `amount_after_tax`, `order_amount`, `new_room_price`); every ₹ renders from `charge{}` / server response. **D85** — M5 row balance = FE sum of folio legs + shared round-off, explicit temporary exception until BQ-385-27. **D87** — M6 checkout body = unchanged POS `collectBillExisting` + `room_gst_tax` passthrough, host adds nothing (unit-pinned). **D88** — Split tile hidden in the Front Desk Bill by scoped CSS; Split → FU-385-D.
- **Sandbox (preprod, RID 69 `sandbox-pms`):** business date = header `meta.business_date`, never the PC clock. **r2 (8526, executive) and r3 (8524, suite) are the owner's live stays (174 bkol / 155 blpi) — never check in / pay / cancel / touch.** QA check-ins only into r4 / r5 / r1 (r1 may be HK — allowed, N9). **Never confirm No-Show.** Every QA stay settled after the test; leftover bookings cancelled; rules must end `allow_early_checkin=false`, `extend_rate_mode=calendar` ("Rate table"), `auto_print_checkin_receipt=false`. Read-back recipe: `test_reports/c6_readback.py` (reads credentials from memory).
- **Credentials:** `memory/test_credentials.md`. **QA_TGK** = the QA/testing_agent account (single-session: one login per browser call, cleanup in `finally`). **OWNER_TGK** = the owner's own login — never use it for QA. Never echo credential values in chat, reports, scripts or evidence files (read them from the memory file — see `evidence/CR-385/phase4_qa/bug450_selfcheck.py`). Owner will rotate the password after the smoke (§6b of `handover/SESSION_HANDOVER_2026_09_22_CR385_P4_QA_PASSED.md`).
- **Testing tool rules:** independent QA = testing_agent, single Playwright call, login at top; results → `test_reports/iteration_<n>.json`; main-agent runs are extra evidence only, never gate evidence. Login page is `/` (not `/login`); wait for the `/loading` redirect (bootstrap, up to ~15 s; a hard reload of any deep link passes through `/loading` and comes back). Use yarn only. Frontend lives in `/app/frontend` (supervisor); never move it.

---
## 2. Where we are
| Phase | Scope | Gate status | Evidence |
|---|---|---|---|
| P0 / P0.5 | M0 shell; BUG-434…438/439 | Gate 6 CLOSED (D80) | `test_reports/QA_REPORT_2026_09_21_CR385_P0_5.md`, it.7–10 (archived in handovers) |
| P1 / P1.5 / 1.5b / 1.5c | M7 Rules tab; M2 cancel / no-show / modify; BUG-440/441/442/445 | Gate 6 CLOSED (D80) | it.14–16 (referenced in PRD/handovers; only it.17+ still on disk), `QA_REPORT_*_P1*` |
| P2 | M1 New Booking, M3 Check-In | Gate 5B CLOSED with gaps (D83); owner smoke = Gate 6 combined (D84→D86) | it.17 (13/13), it.18 (wire), `test_reports/QA_REPORT_2026_09_22_CR385_P2_ROLE4.md` |
| P3 / 3.5 | M4 Extend, M5 balances, BUG-433, BUG-447 | Gate 5B PASSED; smoke = combined | it.19 (structural), it.20 (BUG-447), `QA_REPORT_2026_09_22_CR385_P3_ROLE4.md`, `evidence/CR-385/phase3_qa/` |
| P4 / 4.5 / 4.5b | M6 Bill/Checkout (Layout B), BUG-448 TAB prefill, D88 Split hidden | **Gate 5B PASSED (0 BLOCKER/MAJOR)**; smoke = combined | it.21 (aborted), it.22 (partial), **it.23 A 6/7**, it.24 (blocked), **it.25 B 5/5**, **it.26 C PASS + exit read-back**, it.27 (read-only regression + BUG-450), `test_reports/QA_REPORT_2026_09_22_CR385_P4_ROLE4.md`, `evidence/CR-385/phase4_qa/` |
| BUG-450 (legacy CR-358-P5, mini-gate) | RatesTab room types from rateplans | FIXED + QA-VERIFIED (it.27 + self-check); smoke row CM-S01 | `change_requests/BUG-450_RATESTAB_HARDCODED_ROOM_TYPES_INV_RESTRICTIONS_INTAKE.md` |
| **Gate 6** | combined owner smoke M2-S01…S13 → M3-S01…S10 → M4-S01…S09 → CM-S01 | **IN PROGRESS 2026-09-22** (owner: "GO combined smoke — logged in") | rows + verbatim verdicts: `control/POS_PMS_2_OWNER_SMOKE_BATCH_2026_09_21.md` (Phase 2/3/4 + CM sections; Result block) |
| P5 | closure | **NOT STARTED** — needs "Phase 5 GO" | this file |
Registry: `control/registry.json` item `CR-385` status `GATE_5B_QA_PASSED (P4 + P4.5)`, `current_gate` = 6 combined; `P5 handover written, awaiting owner GO`.

**Decisions D80–D88 (full text `plans/CR-385_DESIGN_DECISIONS.md`):**
- D80 Gate 6 closed for P0 + P1 (+1.5/1.5b/1.5c) by owner words.
- D81 Phase 2 plan-note round: OD-385-19 (a) flat; BUG-446 → FU-385-C; New Booking at top of Arrivals.
- D82 New Booking RIGHT pane pre-save shows type/plan/rate/nights/advance only (no totals) — deviation vs mockup v2.18 under D50.
- D83 Phase 2 Gate 5B closed with acknowledged gaps OG-PMS-038…044.
- D84 Phase 2 owner smoke deferred into the Phase 3 smoke.
- D85 M5 row balance = FE sum + shared round-off — explicit temporary D50 exception (BQ-385-27 retires it); Q2 a "…" placeholder while loading.
- D86 Combined P2+P3 smoke deferred again → ONE combined smoke after Phase 4 QA (M2+M3+M4 rows); conditions 1–5 (independent mutating P4 QA in 2–3 sessions; OG-PMS-038/039/040/041 must close in P4 QA; 041 read-back at entry + exit; P2/P3 defects → 4.5 tagged by origin; row-level verdicts only).
- D87 M6 checkout body = unchanged POS `collectBillExisting` + `room_gst_tax` — D50 exception, unit-pinned; BQ-385-29 retires it.
- OD-385-21 TAB block visible+prefilled accepted (deviation vs D47-c); hidden by scoped CSS only when the prefill satisfies the panel rule (`fd-bill-tab-prefilled`).
- D88 Split tile hidden in the Front Desk Bill by `.frontdesk-bill` CSS; room-stay Split → FU-385-D; M4-S07/S08 re-worded; OG-PMS-048.

---
## 3. Phase 5 scope (phased plan §5 + companion plan §6/§7) — checklist with entry conditions
**Entry conditions (all before any mutating run):**
- [ ] Gate 6 combined smoke CLOSED by the owner's words "Phase 2 + 3 + 4 smoke OK" with every M2/M3/M4/CM row carrying a verbatim verdict; any FAIL → intake → Phase 4.5x → re-smoke that row first.
- [ ] **OG-PMS-049** — full jest green **or each failure triaged with an owner decision**: 56 failures in 12 pre-existing suites — `src/__tests__/api/transforms/rawField.test.js`, `src/__tests__/structure/barrelExports.test.js`, `src/__tests__/components/menu/BulkEditor.cr027p3.test.jsx`, `src/__tests__/components/menu/BulkEditor.cr036.test.jsx`, `src/__tests__/api/transforms/placeOrderPayload.test.js`, `src/__tests__/api/transforms/updateOrderPayload.test.js`, `src/__tests__/api/transforms/qa_subtotal_delivery_validation.test.js`, `src/__tests__/api/transforms/profileTransform.test.js`, `src/api/transforms/__tests__/printerAgentConfigTransform.test.js`, `src/__tests__/integration/POS2_003_REOPEN_A_wire.test.js`, `src/__tests__/components/layout/PlatformDropdown.test.jsx`, `src/__tests__/components/dashboard/ScanOrderPopOut.test.jsx`; plus two CR-358-P4 script-style tests that call `process.exit` and kill a full run: `src/api/services/__tests__/pmsService.tapeChart.cr358p4.test.js`, `src/api/transforms/__tests__/roomStatusTransform.cr358p4.test.js`. Reference run: `CI=true yarn test --watchAll=false --testPathIgnorePatterns cr358p4` → 762 passed / 56 failed / 818. None import CR-385 files. No code without owner routing.
- [ ] QA password rotated by the owner (§6b security note) and `memory/test_credentials.md` updated; then scrub literals from the ~50 historical files on request.
- [ ] Owner "Phase 5 GO" on a Phase 5 plan note (`plans/CR-385_P5_PLAN_NOTE_<date>.md`: sessions, sandbox windows, rooms, cleanup).
**Phase 5 work items (plan §5):**
- [ ] 1. Full verification matrix re-run — companion plan §6 (34 rows: M0…M7 + X-rows) on the final build + probe pack → `evidence/CR-385/probes_<date>_release/` (§4 below).
- [ ] 2. Registry Step 5 checklist (companion §7) per module M0–M7 (§8 below); BUG-418 closed via M6 (Beta) with legacy → FU-385-C; BUG-412 re-verified (D17); BUG-431/432/433 states confirmed.
- [ ] 3. `control/FILE_OWNERSHIP.md`: every new CR-385 file + edited files (E-list) with "CR-385 <module> IMPL <date>"; BUG-450 files.
- [ ] 4. Owner decision **FU-385-C** (retire legacy pages, drop "(Beta)", mirror-rule diff) and **FU-385-D** (Split) — §5.
- [ ] 5. Owner sign-off → Gate 5 closed → sprint status line → CR-385 status `CLOSED` in registry / CR_REGISTRY / CONTROL_DASHBOARD / PRD.
- [ ] 6. POS F&B regression (unchanged behaviour) with a **PRICED** dine-in item incl. **Split** (see §4).

---
## 4. Mutating regression recipe (Phase 5, after the smoke, owner GO)
- **Matrix:** `plans/CR-385_IMPLEMENTATION_PLAN.md` §6 (rows 1…34, "How to verify" + "Automated?"). Automated rows = `CI=true yarn test --watchAll=false --testPathPattern "cr385|bug450"` (**128 tests**: cr385 suites + `RatesTab.bug450` 7). Manual/browser rows → testing_agent sessions.
- **Probe pack** (Python, reads credentials from memory — re-point the older scripts, they still contain a literal): `evidence/CR-385/probes_2026_09_20_gate4/run_gate4.py` (booking/check-in/extend wire + charge read-back), `probes_2026_09_20_n7n8/run_n7n8.py` (N7 early check-in guard 422 + N8 `extend_rate_mode`), `probes_2026_09_20_n11/run_n11.py` + `run_d14.py` (per-night GST slab, collect-now in response), `probes_2026_09_20_d1516/` (shorten re-price D15, move keeps price D16), `probes_2026_09_21_d17/` (BUG-412 re-verify), `probes_2026_09_21_held_fallback/` (temporary rate wipe → extend → `source: held_fallback` → restore). Output → `evidence/CR-385/probes_<date>_release/` with a `PROBE_REPORT.md` (endpoint, HTTP, key figures, forbidden-key grep).
- **Sandbox setup:** entry read-back (`c6_readback.py` pattern → `t0_entry_readback.json`); rooms r4/r5/r1 free, rules default; Executive availability decides the paid-upgrade leg (OG-PMS-038) — if r2 is still stay 174, write "not available" and ask the owner.
- **Session split (learned in P4):** A = booking → check-in (collect-now) → upgrade (if Executive free) → toggle cycle → Bill/Cash settle; B = extend +1 with collection → shorten → Bill shapes (SGST/CGST two lines, ROOM ORDERS, TRANSFERRED) → TAB settle via the M6 panel; C = POS `/dashboard` regression with a **priced** item (Split visible, Cash+Card split, toggles visible on POS, hidden under `.frontdesk-bill`), legacy pages load, 0 console errors at 1920×800 + 1366×768, exit read-back (`t9_readback.json`). One Playwright call per session, QA_TGK, cleanup in `finally`; chips via `fd-chip-<tab>-<key>` (`fd-chip-arrivals-today`, `fd-chip-inhouse-all`).
- **Read-only vs mutating:** read-only = tab clicks, Bill open (folio fetch), Channel Manager look (rates/inventory *fetch* endpoints are POSTs but reads — never click `rt-inv-push-btn` / `rt-rr-push-btn` / `rt-bulk-btn`), API GETs. Mutating = booking, check-in, extend/shorten, any payment, cancel, rule toggle, room status, Room Mapping save, OTA push, POS order placement. Mutating only after the smoke and with owner GO; never while the owner is on the sandbox.
- **BUG-433 paise case:** needs a room-service POS order with a paise-yielding item on the QA stay before Bill; otherwise record "integer case only".
- **Legacy drawer (OG-PMS-042):** testing_agent could not drive `PmsCheckoutDrawer`; settle QA stays through the Front Desk (Beta) Bill (Cash/TAB) or via API (`order-bill-payment` with the drawer's body — see `evidence/CR-385/phase4_qa/t5_cleanup_after_it25_timeout.json` for the cancel path). Legacy Departures TAB is disabled until name+phone typed (BUG-449) → use Cash.

---
## 5. Open items by owner
**OPEN OG-PMS (status · owner):** 020 View Folio route inconsistency (SC-385-01) · FE/owner ack — 021 IA note on payment panel scroll (planning) · FE/doc — 022 folio `remaining_room_balance` stale after TAB (backend residual) · backend — 026 (second entry, L424) closed via D14 — 028 doc precision `rooms[].order_payment_status` · FE/doc — 029 LR/KPIs need date window; board has no `sections[]` · contract note — 034 `updateSettings` FormData header latent · FE — 035 modify reason → `special_requests` (BQ-385-23) · backend — 036 500 on non-int id (BQ-385-24) · backend — 037 occupancy % mixed sources (BQ-385-25) · backend — **038 paid upgrade — 5th deferral → smoke M2-S08 only** · owner/sandbox — 042 legacy drawer QA recipe · process — 043 header "—" flash · cosmetic — 044 `direct-reservation` 200 skipped (BQ-385-26) · backend — 046 chip after save (D70 by design) · note — 047 balance "…" latency (D85) · note — **048 Split hidden → FU-385-D** · owner — **049 full jest not green → Phase 5 entry** · process/owner. CLOSED this cycle: 023/024/025/027/030/031/033/039/040/041/045; 032 confirmed contract. Register: `control/OPEN_GAPS_REGISTER.md`.
**Non-closed BUGs:** BUG-431 (CRITICAL, legacy CheckInPage GST re-apply) · BUG-432 (CRITICAL, legacy NewBookingPage rate override) · BUG-443 (legacy Modify `amount_after_tax`, harmless) · BUG-444 (tape chart pending-unassigned) · BUG-446 (legacy headers browser date, X-06) · BUG-449 (legacy drawer TAB prefill) → **all DEFERRED-TO-FU-385-C**, Front Desk (Beta) paths fixed by construction. BUG-433 legacy displays → FU-385-C (OD-385-20 a); Beta path FIXED (paise case still "integer case only"). BUG-418 FIXED + QA-VERIFIED (Beta), legacy → FU-385-C. BUG-412 re-verify in P5 (D17). Tracker: `control/BUG_TRACKER.md`.
**Backend brief (owner relays to backend):** `backend_briefs/BACKEND_BRIEF_CR-385_MASTER.md` v2.5 — BQ-385-22 LR performance/aggregation (also `BACKEND_BRIEF_CR-385_PERF_LR_2026_09_21.md`) · 23 modify reason leak · 24 non-int id 500 · 25 KPIs occupancy · 26 skipped:true → 422 · 27 folio-level `balance_due` (retires D85) · 28 `upgrade_reason` on `charge` · 29 charge-only checkout body (retires D87). BQ-385-07 room discount (M6 control greyed).
**FU-385-C (owner decision, separate CR):** retire 11 legacy files — `FrontDeskPage`, `ArrivalsPage`, `DeparturesPage`, `InHouseGuestsPage`, `RoomStatusPage`, `CheckInPage`, `NewBookingPage`, `GuestFolioPage`, `PmsCheckoutDrawer`, `ExtendStayDialog`, `ModifyBookingDialog`; re-point `/pms/front-desk` → `/pms/front-desk-v2`, drop the "(Beta)" sidebar label (OD-385-14), diff copies vs sources (mirror rule R9: `RoomTile.jsx`, `CheckInForm.jsx`), close BUG-431/432/443/444/446/449 + X-06 + legacy rounding. Needs: owner GO, intake, IA on routes/sidebar/permissions, regression on `/reports/rooms` (OG-PMS-020).
**FU-385-D (owner decision):** Split for room stays in the Front Desk Bill — choose panel Split inside the 440×560 host vs a Front Desk split form; re-enable = delete the D88 rule in `frontdesk.css` + the `payment-split-btn` line in `hideSectionRows` guard; smoke M4-S07 with Split. Row in `control/CR_REGISTRY.md`.

---
## 6. Code map (marker in every file header / edited line)
**New (marker `// CR-385 M<n>`):** `pages/pms/FrontDeskWorkstationPage.jsx` (M0 host, `?tab=`), `pages/pms/FrontDeskRulesTab.jsx` (M7), `components/pms/frontdesk/`: `WorkstationHeader.jsx`, `KpiTabStrip.jsx`, `AlertBar.jsx`, `GlobalSearch.jsx`, `GuestTable.jsx`, `ArrivalsPanel.jsx`, `DeparturesPanel.jsx`, `InHousePanel.jsx`, `RoomsPanel.jsx`, `RoomTile.jsx` (COPY), `RoomDetail.jsx`, `money.js` (M0) · `ModifyBookingForm.jsx` (M2) · `NewBookingForm.jsx` (M1), `CheckInForm.jsx` (COPY, M3) · `NightsLines.jsx`, `ExtendStayForm.jsx` (M4) · `FolioCheckoutPanel.jsx` (M6; `// CR-385 BUG-448` prefill) · `frontdesk.css` (M0 + M6 host rules + BUG-448 + D88) · `api/services/frontDeskService.js`, `api/transforms/frontDeskTransform.js`.
**Edited (additive, marked):** `App.js` (+route), `components/layout/Sidebar.jsx` (+item), `api/constants.js`, `api/services/restaurantSettingsService.js` (+`updateFrontDeskRules`), `api/transforms/restaurantSettingsTransform.js`, `api/transforms/roomStatusTransform.js` (additive fields), `components/pms/CancelBookingDialog.jsx` / `NoShowDialog.jsx` (+`inline` prop), `pages/pms/ChannelManagerPage.jsx` (Tab 4 Rules; BUG-450 +1 prop), `pages/pms/ArrivalsPage.jsx`, `pages/pms/ReservationsPage.jsx` (BUG-441/442 legacy id/cancelled_by), `pages/pms/RatesTab.jsx` (BUG-450, legacy CR-358-P5 file).
**Tests (128 = cr385 121 + bug450 7):** `api/transforms/__tests__/frontDeskTransform.cr385.test.js`, `api/services/__tests__/frontDeskService.cr385.test.js`, `components/pms/frontdesk/__tests__/{money,GuestTable,GlobalSearch,phase05,phase1,bug439}.cr385.test.*`, `tests/cr385/{phase1_5,phase1_5c,phase2,phase3,phase4,bug447,hideSectionRows}.cr385.test.*`, `pages/pms/__tests__/RatesTab.bug450.test.jsx`. Run: `CI=true yarn test --watchAll=false --testPathPattern "cr385|bug450"`.
**Fixtures:** `src/__fixtures__/cr385/` — `local_reservations_view_all.json`, `room_status_board.json`, `dashboard_kpis.json`, `settings_list_basic.json`, `room_availability.json`, `direct_reservation_201.json`, `direct_reservation_422_norate.json`, `direct_reservation_skipped.json`, `checkin_200_upgrade.json`, `extend_200_calendar.json`, `extend_200_held_fallback.json`, `extend_409.json`, `inhouse_guests.json`, `folio_room_upgrade.json`, `bill_payment_already_paid.json`.
**CSS scope:** all host rules live in `frontdesk.css`; everything touching the payment panel is prefixed `.frontdesk-bill` (three `checkout-*-toggle` rows, `payment-split-btn` D88, `tab-customer-section` only with `.fd-bill-tab-prefilled`); `.frontdesk-bill { width:440px; height:560px }`, `.fd-bill-grid`. Guard test `hideSectionRows.cr385.test.js` fails if a rule loses its scope or the panel renames a testid.
**testid conventions:** `fd-tab-<arrivals|departures|inhouse|rooms>`, `fd-chip-<tab>-<key>`, `fd-row-<id>-<field|action>` (`-balance`, `-badge`, `-badge-advance`, `-bill`, `-extend`…), `fd-room-detail-<tableId>` (only after a tile click), `fd-room-action-bill-<tableId>`, `fd-header-date`, `bill-room-*` (`-sgst`, `-cgst`, `-balance`), `bill-right`, `extend-*`, `nights-lines-*`, `checkin-room-select`, `toggle-allow-early-checkin`, `channel-manager-tab-<0..4>` (index-based: 0 OTA/Sync · 1 AIOSELL Setup · 2 Room Mapping · 3 Rates & Restrictions · 4 Front Desk Rules), `rt-subtab-<rates|inv|raterestrict>`, `rt-inv-card-<code>`, `rt-inv-label-<code>`, `rt-inv-<code>-<field>`, `rt-inv-empty`, `rt-inv-push-btn`. X-10: every interactive element unique.
**Guard greps (expected output):**
1. Money guard — `grep -rn "balance_payment\|remaining_room_balance\|\* 0.05\|\* 0.18\|toISOString" frontend/src/components/pms/frontdesk frontend/src/api/services/frontDeskService.js frontend/src/api/transforms/frontDeskTransform.js | grep -v __tests__` → **exactly 2 lines in `frontDeskService.js` (~L70 comment, ~L101 `fd.append('balance_payment', '0')`)** — the M3 multipart zero fields (server prices). Anything else = regression.
2. BUG-450 guard — `grep -rnE "'executive'|\"executive\"|'suite'|\"suite\"|Executive Room" frontend/src --include=*.js --include=*.jsx | grep -v "__tests__\|/tests/\|__fixtures__\|\.test\."` → **empty**.
3. Hotspots — `git log -1 --format=%h -- frontend/src/components/order-entry/CollectPaymentPanel.jsx frontend/src/api/transforms/orderTransform.js frontend/src/api/services/pmsService.js frontend/src/components/pms/PmsCheckoutDrawer.jsx` → `642ccb8` (squashed baseline) — anything newer = hotspot touched.
4. Mockup lock — `sha256sum frontend/public/cr385-frontdesk-mockup.html` → starts `12fd0f4a343fc89d`.
5. Forbidden payload keys are asserted in `phase2/phase3/phase4.cr385.test.jsx` (booking, extend, checkout bodies).

---
## 7. Known-ignore / cosmetic (do not file again)
OG-PMS-043 `fd-header-date` "—" before hydrate · BQ-385-23 "SR ●" on every modified row · BQ-385-25 occupancy % excludes overdue guests · BQ-385-26 empty booking → red error from a 200 "skipped" · ₹0 rate cards for unpriced type/plan (rate table gap, sold-out greyed) · Rates & Restrictions labels show the room **code** until the Room Mapping tab was opened in the session (then catalogue `room_name`, e.g. EXECUTIVE / SUITE) · balance cell "…" for a few seconds after check-in / Bill open (OG-PMS-047, D85) · POS Split tile absent on a ₹0 order (amount gating, it.26 F-6) · console lines from socket / firebase messaging (pre-existing) · hard reload of a deep link passes through `/loading` · OG-PMS-046 new Today booking visible only after clicking the Today chip (D70) · legacy Departures TAB disabled until name+phone typed (BUG-449 → Cash) · room discount greyed "needs BQ-385-07" · React key warnings in legacy pages outside CR-385 (RatesTab one fixed in BUG-450).

---
## 8. Registry closure checklist (companion §7) — per module, and the closing sequence
Per module M0 (shell) · M1 (New Booking) · M2 (cancel/no-show/modify) · M3 (Check-In) · M4 (Extend) · M5 (balances/row actions) · M6 (Bill/Checkout) · M7 (Rules tab):
- [ ] `registry.json` CR-385: `files[]` = actual list (§6), `status_history` entry per module, final status; BUG-418 "FIXED + QA-VERIFIED (Beta) via CR-385 M6"; BUG-450 done.
- [ ] `control/CR_REGISTRY.md` CR-385 row (module progress + closure), FU-385-C / FU-385-D rows; `control/CONTROL_DASHBOARD.md`; `PRD.md`.
- [ ] `control/FILE_OWNERSHIP.md` — owed: all §6 new files + edited files with "CR-385 M<n> IMPL 2026-09-2x" and BUG-448/450 lines (most CR-385 entries exist — verify each; add P2–P4 files).
- [ ] Code markers present (R18) on every new file header and edited line; copy headers on `RoomTile.jsx` / `CheckInForm.jsx` (source path + line range + commit).
- [ ] Guard greps §6 as expected; hotspot log unchanged; mockup sha unchanged.
- [ ] `yarn test` cr385+bug450 128 green; full suite per OG-PMS-049 decision; `yarn build` exit 0 (without `CI=true`; with `CI=true` the repo fails on pre-existing eslint hook warnings in unrelated files).
- [ ] Sandbox restored (3 settings default, stays settled, bookings cancelled, r2/r3 untouched) + final read-back saved.
- [ ] Master checklist `frontend/public/cr385-master-checklist.html` rows M0-01…M7-04, X-01…X-15, R-01…R-08 ticked with evidence (mirror only; batch file is the record of truth).
- [ ] `control/SPRINT_STATUS.md` — add/update the `pos_pms_2` / CR-385 line: "CR-385 CLOSED <date> — Gate 5 closed, FU-385-C/D decisions: <owner words>".
**Closing sequence (exact):** Gate 6 closed by "Phase 2 + 3 + 4 smoke OK" → Phase 5 plan note → owner **"Phase 5 GO"** → regression (§4) + registry checklist (this §) → QA report `test_reports/QA_REPORT_<date>_CR385_P5_ROLE4.md` → owner sign-off words recorded verbatim → registry `CR-385` → `CLOSED` → Gate 5 closed → sprint status line → SESSION_HANDOVER with self-assessment.

---
## 9. Pitfalls we hit (do not repeat)
- **Single-session credential:** the sandbox allows one live session per account; the testing_agent opens a fresh browser context per call → OWNER_TGK kept getting logged out (it.21 aborted). Fix: owner created **QA_TGK**; one Playwright call per session, login at the top, cleanup in `finally`; API read-backs after the browser call ends.
- **Wrong branch:** a push once went to `22implement` instead of `origin/21implement`; and a forced update rewrote history. Only `origin/21implement`, only via **Save to GitHub**, never force; the owner's tracker validates every push.
- **testing_agent cannot drive the legacy Departures checkout drawer** (OG-PMS-042): it left a QA stay in-house; settle through the Front Desk (Beta) Bill or via API; legacy TAB needs typed name+phone (BUG-449).
- **Channel Manager tab testids are index-based** (`channel-manager-tab-0…4`); Room Mapping is **tab-2**, Rates & Restrictions **tab-3**, Front Desk Rules **tab-4**. A QA brief that said tab-1 produced a false "labels not applied" finding (it.27).
- **Executive room blocker:** r2 is the owner's live stay 174 → paid-upgrade leg (OG-PMS-038) deferred 5 times; never check it out for QA; ask the owner.
- **Arrivals chips:** after saving a Today booking the tab stays on the first non-empty chip (Late) — click `fd-chip-arrivals-today` (it.22/24).
- **`fd-room-detail-<tableId>` exists only after a tile click** (it.26 probe inconclusive).
- **Full jest never finishes** because two CR-358-P4 script tests call `process.exit` → use `--testPathIgnorePatterns cr358p4` (OG-PMS-049).
- **`CI=true yarn build` fails** on pre-existing eslint hook warnings elsewhere; plain `yarn build` is the reference.
- **Credential sprawl:** never write a credential literal into a script/report; read `memory/test_credentials.md` (the it.26 agent left one in `test_reports/c6_readback.py` — scrubbed).
- **`playwright` python is not preinstalled**; `pip install playwright` + `executable_path='/usr/bin/google-chrome'` works for local read-only self-checks; `networkidle` never settles (socket polling) — use `domcontentloaded` + selector waits.
- **Do not clone into /tmp, do not move the frontend, do not infer the preview URL** (read `frontend/.env`, never print it).
